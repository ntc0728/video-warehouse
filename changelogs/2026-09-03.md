# Changelog 2026-09-03 — 历史页记录卡 UI 整改（Demo 阶段）

> 状态：方案 A + 可看性缓存 + live-badge 绿/红 已落地真实代码（`tsc -b` 通过，dev 预览 http://127.0.0.1:3002/ 待用户真机验收）。超宽封顶 5 列按用户要求进代办 + 明日提醒（未硬编码，当前用 `max-width:1680px` + `auto-fill` 软封顶）。

## 历史页 demo v3 — 仅方案 A + 批量方框悬浮封面 + 排列布局说明
- 用户反馈：要知道 card 排列布局；批量模式方框应**悬浮在封面图上**；**只保留方案 A**；在 demo 落地大屏适配 + token 整改。
- demo 改造：
  - 移除 B/C/D，仅留 **方案 A（横版 16:9）**：左封面 + 右 4 行（标题跑马灯 / 类型独占一行 / 来源+集数 / 状态左+时间右同行）。
  - **批量方框悬浮封面**：`#cbBatch` 勾选后，`.check` 浮于 `.x-media`（封面）左上角（白边+暗底，选中填充主色+封面轻压暗），点击切换选中；批量态下删除键自动隐藏。LIVE 角标移封面右上避免重叠。
  - **卡片排列布局说明块**：居中容器 `max-width:1920px` + `auto-fill minmax(20rem,1fr)`；列数随视口（390=1/1024=3/1440=4/1920=5），**超大屏显式锁 5 列**（`@media min-width:2560→1600px、3440→1500px`）；列间距 `clamp(12→24)`；大屏根字号收缩→卡片 rem 等比缩；附 CSS 布局示意图。
  - 行距回调 token：card gap/padding=`--space-sm`、body 行距=`--space-md`、body 上下 padding=`--space-xs`，卡片高回落到 ~115–120px（适中）。
  - 大屏适配机制与 v2.2 一致（根字号 clamp + 媒体查询 + 内容封顶居中 + 页面最小宽度底线 14px）。
- 验证（Playwright 真实视口）：6 视口零横滚零报错；列数封顶 **5**(3840/2560/1920)、4(1440)、3(1024)、1(390)；根字号 16/15/14px；card 高 115–120px；批量方框 `insideMedia=true` 且可点选 `.on`、删除键 `display:none`；长标题跑马灯触发。
- 涉及文件：`changelogs/demos/demo-history-card-2026-09-03.html`（同步更新 README Demo 索引）。

## 落地到真实代码（2026-09-03 已实施，tsc -b 通过，dev 预览 http://127.0.0.1:3002/）

### 1. RecordCard 方案 A + 批量方框悬浮封面 + 跑马灯
- 文件：`src/components/RecordCard/RecordCard.tsx`
  - `RecordCardItem` 新增 `available?: boolean`（true=可看 / false=无法观看 / undefined=未检测），`RecordCard.tsx:42`。
  - 标题溢出检测：`ResizeObserver` 量 `scrollWidth > clientWidth+1` → `titleOverflow`，`RecordCard.tsx:83`；渲染带 `is-overflow` 类 + 跑马灯双份文本 `RecordCard.tsx:162`。
  - live-badge：`liveBadgeClass`/`liveBadgeText` 按 `available===false` 切换 绿 LIVE / 红 无法观看，`RecordCard.tsx:95-98`、`148`。
  - 批量方框：`batchMode &&` 渲染浮于封面左上，`RecordCard.tsx:119`；`handleCardClick` 在 `batchMode` 时 `e.preventDefault()` + `onToggleSelect()` 不导航，`RecordCard.tsx:220-237`（满足"批量模式点击 IPTV 不跳转"）。
- 文件：`src/components/RecordCard/RecordCard.css`
  - `.history-page .record-card` flex 横版 16:9、`min-height:108px`、`align-items:center`、gap `--space-sm`。
  - 媒体 `aspect-ratio:16/9`、`flex:0 0 132px`；4 行 body（title / type-row / meta-row / foot-row 状态左+时间右 `space-between`）。
  - 跑马灯：`@keyframes rc-marquee` translateX -50%；`.record-card__title.is-overflow:hover .record-card__title-track` 触发。
  - live-badge 定位右上（`top/right --space-2xs`），避免与左上批量方框重叠。

### 2. live-badge 绿/红（全局 token）
- 文件：`src/assets/styles/index.css:937-951`
  - `.record-card__live-badge` 背景改 `var(--color-success)`（绿）；新增 `.is-unavailable` 背景 `var(--color-danger)`（红）。

### 3. 收藏页 iptv-card-cover__badges 同步
- 文件：`src/components/IPTVChannelCard/IPTVChannelCard.tsx:137-138` live-badge 按 `availability===false` 切 绿 LIVE / 红 无法观看。
- 文件：`src/pages/Collections/index.tsx` 解构 `channelAvailability`（`84`）并传入 `availability={channelAvailability[ch.id]}`（`470`）。

### 4. IPTV 可看性缓存（持久化 + 每次处理更新 + 进页同步）
- 文件：`src/stores/useIPTVStore.ts`
  - `channelAvailability: Record<string,boolean>` 接口声明 `42`；初始 `{}` `105`。
  - `checkAvailability` 成功路径合并写入 `channelAvailability`（`401`：`set({channelAvailability:{...get().channelAvailability, ...results}})`）；失败路径同样写 false（`checkAvailability` 实现 `307` 起）。
  - `partialize` 持久化 `channelAvailability`（`477-483`，写入 localStorage `iptv-store`）。
- 历史页：`src/pages/History/index.tsx` 解构 `channelAvailability`（`177`），iptv 分支 `available: channelAvailability[ch.id]`（buildRecordCardItem），`useCallback` deps 含 `channelAvailability`（`658`）。
- 收藏页：见 §3。
- 约定：历史/收藏页**只读取缓存**渲染，不自行计算可看性。

### 5. 超宽封顶 5 列（未硬编码，进代办 + 明日提醒）
- 当前 `History.css:300-310`：网格 `repeat(auto-fill, minmax(340px,1fr))` + `max-width:1680px; margin:0 auto`（软封顶，超宽自动降列宽不超 5 列）；app 端恒 2 列（`data-device="app"`）。
- 硬 5 列封顶（≥2560px 锁 5 列，参考 demo 的 `@media min-width:2560→1600px、3440→1500px`）按用户要求**进代办事项 + 明日提醒**（2026-09-04 10:00 自动化），未写入真实代码——用户"无真机无法查看真实情况"，先看效果再定。

### 6. 尺寸微调（用户预览后反馈：宽度没变 + 16:9 封面致上下空白过大）
- 封面不再用 `aspect-ratio:16/9` 收缩成矮图：`.record-card__media` 改 `align-self:stretch` 充满卡片高度，配 `object-fit:cover` 裁切源图（视频/台标均 cover）。Card 内上下空白=padding（`--space-sm`）即与左右一致，四向等距。
- `.record-card` `min-height:108px → 120px`（加高）；`--record-card-media-w:132px → 120px`。
- `.record-card__body` 加 `align-self:stretch` + 去 `padding:var(--space-2xs) 0`（改 0），使正文四向 inset 与封面一致。
- 网格收窄：`minmax(340px,1fr)→minmax(280px,1fr)`、`max-width:1680px→1500px`，超宽屏卡片由 ~414px 降到 ~293px（仍 5 列内，硬封顶代办未动）。

### 7. 竖版卡片 → 用户要求改回横版（2026-09-03 16:45 回退）
- ⚠️ 用户明确反对竖版（"谁让你改成竖版结构的？改回横版结构"）。本节此前"唯一成立布局是竖版"的结论作废，回退到横版。
- `RecordCard.css` 整体改回横版：`.record-card` `flex-direction:row` + `align-items:stretch` + `gap:var(--space-sm)` + `padding:var(--space-sm)` + `min-height:120px`（更高）；`.record-card__media` `flex:0 0 140px` + `aspect-ratio:16/9` + `align-self:center`（严格 16:9 居中，横版下封面上下必有留白，这是几何代价）；`.record-card__body` `flex:1`、`justify-content:center`、`padding:0`（四向 inset 与封面统一为 --space-sm）。
- 网格：`minmax(200,1fr)→minmax(260,1fr)`（横版需更宽容纳右侧正文，但仍比早期 280 更窄）、`gap:var(--space-md)`（间距加大）、`max-width:1180→1500`（≤5 列软封顶）。
- 几何说明（重申）：横版 + 严格 16:9 封面 + 更窄 + 更高 不可同时完美成立——封面固定 140px 宽→16:9 仅 ~79px 高，卡高 120px 时封面上下各留 ~20px 空白。若用户要消除该空白，只能改 `align-self:stretch`+`object-fit:cover` 裁切（封面非严格 16:9）。待用户拍板。
- 验证：dev 3002 HTTP 200；tsc -b 通过。未 commit（待验收）。

### 8. 封面顶满卡高（用户指定最优解，16:53 轮）
- 用户指令：设置最小高度后，**封面图高度至少=卡片 min-height，宽度按 16:9 自适应，右侧区域宽度自适应调整**——这是消除上下留白又不破坏 16:9 的最优解。
- `RecordCard.css`：`.record-card__media` 去固定宽/居中，改 `flex:0 0 auto` + `align-self:stretch` + `aspect-ratio:16/9`（高度顶满 120px 卡高→宽度自动 ~213px，严格 16:9，零上下留白）；`.record-card__body` `flex:1 1 0` 自适应右侧剩余宽度。
- `History.css` 网格放宽以容纳更宽的封面：`minmax(260,1fr)→minmax(360,1fr)`、`max-width 1500→1900`（5 列软封顶：1900 下每列 ~366px = 封面213 + 右侧~117）。
- 代价：卡片整体比"更窄"诉求更宽（16:9 封面顶满高度天然占 ~213px）。用户以"最优解/无留白"优先于"更窄"。
- 验证：dev 3002 HTTP 200；tsc -b 通过。未 commit（待验收）。

### 9. 跑马灯触发修复（16:55 轮）
- 问题：原跑马灯仅在 `.record-card__title.is-overflow:hover` 触发（只命中标题文字本身），悬浮整卡不触发 → 用户感知"未实现"。
- 修复：`RecordCard.css` 触发改为 `.record-card:hover .record-card__title.is-overflow .record-card__title-track`（悬浮整卡即播放）。
- 移动端/小屏改为「进入视口才播放」：`RecordCard.tsx` 加 `IntersectionObserver` + `cardRef`/`inView` state，进视口给卡片加 `is-in-view` 类、离开移除；`RecordCard.css` 仅在 `@media(width<=767px)` 与 `html[data-device='app']` 下用 `.record-card.is-in-view .record-card__title.is-overflow .record-card__title-track` 播放（离开视口即停，避免全部长标题常驻滚动）。桌面仍走 hover（不自动播）。
- 验证：dev 3002 HTTP 200；tsc -b 通过。未 commit（待验收）。

### 10. 正文反超封面修复（17:0x 轮·用户实测纠偏）
- 用户实测：某视口下卡片 402 宽、封面 188、card 高 120 → 正文 = 402−188−12−6 = **196 > 封面 188**，正文确实比封面宽（与"正文明显过大/主体空白"吻合）。
- 根因：`.record-card__body` 为 `flex:1 1 0` 且网格用 `minmax(360px,1fr)` 的 `1fr` 拉伸——列数少时每列被撑宽，弹性正文吃掉全部多出宽度 → 正文随视口变宽、反超锁高的封面。
- 修复：网格改**固定列宽** `repeat(auto-fill, 380px)` + `justify-content:center`（余白居中，不再 1fr 拉伸）；`.record-card__body` 加 `max-width:200px` 兜底。封面高度锁定 min-height(120, border-box)→封面恒 188×106(16:9)，正文恒 172×106，**封面>正文 且全视口恒定**。
- 列数：900/1024→2、1280/1536→3、1920/2560→4（≤5 列软封顶仍满足）。
- 验证：补 `box-sizing:border-box` 重置后 Playwright 实测 900–2560 全视口 card 380 / 封面 188 / 正文 172 恒定；`npx tsc -b` 通过。未 commit（待验收）。

### 11. 去除裸 px、全部收进 token（17:20 轮·用户纠正规范违规）
- 违规：上轮用了裸 `380px`（`History.css` 网格列宽）/`1900px`（网格容器 max-width）/`120px`（`RecordCard.css` min-height）/`200px`（正文 max-width），违反项目"尺寸一律走 variables.css 流体 clamp token，无则自建（--layout-* 命名）"硬规。
- 修复：在 `variables.css` 新增一组 RecordCard 尺寸令牌（集中在卡片/布局宽度段）：
  - `--layout-record-card-width: clamp(280px, 17.5rem + 4vw, 380px)`（列宽 280→380 流体，超宽屏封顶 5 列）
  - `--layout-record-card-min-height: clamp(108px, 6.75rem + 0.5vw, 120px)`（卡片最小高 108→120 流体，封面 16:9 据此算宽）
  - `--layout-record-card-body-max: clamp(160px, 10rem + 1.5vw, 200px)`（正文最大宽 160→200 流体）
  - `--layout-record-card-grid-max: 1900px`（网格容器硬封顶 5 列；微元素固定值，注释标明）
- 引用点改为 `var(--layout-record-card-width)` / `var(--layout-record-card-grid-max)` / `var(--layout-record-card-min-height)` / `var(--layout-record-card-body-max)`。保留的 px 仅限 `@media` 视口断点（合规）与 1px/2px 边框、颜色常量。
- 验证：dev 3002 实际吐出 CSS 已为 `repeat(auto-fill, var(--layout-record-card-width))` 与 `max-width:var(--layout-record-card-grid-max)`；`npx tsc -b` 通过。未 commit（待验收）。

### 12. 网格填满整行 + 超宽封顶 5 列 + 大屏 token 放大（17:25 轮·用户再批）
- 用户两问：(1) `repeat(auto-fill, 380px)` + `justify-content:center` + 容器 `max-width:1900px` → 超 1900 视口网格被锁 1900 居中，**两侧大量空白**；(2) >2560 与 TV 一律 **4 列**（1900 容不下第 5 列），违背"超宽封顶 5 列"，且完全没兑现"大屏适配 + token 整改"原始诉求（1080p 和 4K 长得一样）。
- 根因：容器 `max-width:1900px` 把网格死锁在 1900 居中 → 侧边空白 + 永远塞不下 5 列。
- 修复（`History.css` + `variables.css`）：
  - 网格改 `repeat(auto-fill, minmax(var(--layout-record-card-width), 1fr))` + 删 `max-width`/`justify-content:center` → 列随视口 1fr 填满整行（**平均分配，无侧边空白**）。
  - 新增 `@media (min-width: 2080px){ .history-grid{grid-template-columns:repeat(5,1fr)} }` → **超宽/4K/TV 强制 5 列**（1fr 填满，无侧边空白），落实"超宽封顶 5 列"。≤480→1 列、app→恒 2 列规则保留。
  - 删除 `--layout-record-card-grid-max` 令牌（不再需要）。
  - 尺寸 token 改为随视口流体放大（大屏适配）：`--layout-record-card-min-height: clamp(108px, 5rem + 3.5vw, 240px)`（大屏封面 16:9 同步变宽，避免大屏内部留白且始终 ≥ 正文宽）；`--layout-record-card-body-max: clamp(160px, 10rem + 3vw, 360px)`（大屏放宽，始终 ≤ 封面宽）。
- 实测（Playwright 带 border-box，900→3840，真像素）：侧边空白恒为 0；列数 1280→3 / 1536→4 / 1920→4 / ≥2080→5（含 TV 4K）；封面始终 > 正文；4K(3840) 卡片 738、封面 381、正文 275、内部右空白约 52px（≈7%，5 列封顶使 4K 卡偏宽的固有代价，已最小化）。
- 注：项目 `variables.css` 本就存在一套"大屏 4K 放大"令牌（text-base 15→30px、space 翻倍，约 499–556 行），属同一思路；本次仅补齐 RecordCard 尺寸令牌的视口缩放。
- 验证：`npx tsc -b` 通过；dev 3002 实际吐出 CSS 已为 `repeat(auto-fill,minmax(var(--layout-record-card-width),1fr))` + `@media(min-width:2080px){repeat(5,1fr)}`；无裸尺寸 px（仅视口断点合规）。未 commit（待验收）。

### 构建结论
- `npx tsc -b` 通过（exit 0，无类型错误）；`npx vite build --emptyOutDir false` 通过（exit 0）。
- dev 预览：`http://127.0.0.1:3002/`（3001 被占，Vite 自增）。
- 提交：待用户预览验收后再 commit（用户明确"先看下效果，然后再做打算"）。

## 待办（落地到真实代码）— 进展
- [x] 改 `RecordCard.tsx/css`：4 行结构 + 批量方框悬浮封面 + 跑马灯 + live-badge 绿/红
- [x] 改 `History.css`：网格 `auto-fill minmax(var(--layout-record-card-width),1fr)` 填满整行 + `@media(min-width:2080px){repeat(5,1fr)}` 超宽/TV 封顶 5 列（§12，已落真实代码，非软封顶）
- [ ] 全局前置：项目 `variables.css` 引入根字号变量 + 卡片相关 token 转 rem（大屏根字号收缩需此前置，全局推广时再做）
- [x] 一并改收藏页 `IPTVChannelCard` + `Collections`（同类 live-badge 逻辑同步）
- [x] 补本文件「旧↔新 + 文件:行号 + 构建结论」

## 历史页记录卡整改 Demo v2（4 方案对比）
- 日期：2026-09-03
- 问题：历史页 RecordCard 无跑马灯（css 仅 ellipsis，组件内无 marquee 逻辑）；卡片在超宽屏被拉到 500–700px 太宽；类型/来源/集数挤一行不清晰。
- 旧逻辑：`RecordCard.css:125-134` 标题 `text-overflow:ellipsis`；`History.css` 硬写 `repeat(2/3, minmax(0,1fr))`。
- 新逻辑（demo 内）：
  - 横版(A/D) 重排 4 行：标题(跑马灯) / 类型独占一行 / 来源+集数 / 时间+状态同行。
  - 卡片收窄(~323–407px) + 加高(112→115px A / 118px D)，`--record-card-min-h:112px` 走项目 token。
  - 非标题文本 `nowrap` 全显；只有标题走跑马灯（JS 量宽写 CSS 变量，动画交 `@keyframes`，`ResizeObserver`+`fonts.ready` 重测）。
  - 列数封顶随视口：A/D 容器 1920→5 列、B 1500→7 列、C 2200→4 列（auto-fill minmax）。
  - **本轮追加**：去横向滚动条（`body`/`stage` `overflow-x:hidden` + 视口模拟改 `max-width` 不再 `width`）；`x-body` 行距 `space-xs→space-sm`；IPTV 台标 `logo-fill`（object-fit:cover）充满左侧父级（D 1:1 零裁切、A 16:9 品牌色铺底）。
- 涉及文件：`changelogs/demos/demo-history-card-2026-09-03.html`
- 关联 Demo：`demos/demo-history-card-2026-09-03.html`
- 构建：纯 HTML demo，无 build；Playwright 真实浏览器冒烟：四方案×五视口零 JS 报错、零横向滚动、IPTV 台标尺寸==父级、超宽列数封顶(A5/B7/C4/D5)。

## 待办（用户拍板后落地）
- [ ] 选定横版方案 A(16:9) 或 D(1:1 台标零裁切)
- [ ] 改 `RecordCard.tsx/css`：4 行结构 + 跑马灯组件化 + `--record-card-min-h`
- [ ] 改 `History.css`：网格 `auto-fill minmax` + 容器封顶 max-width
- [ ] 是否一并改收藏页 `.record-card`（同类名复用）
- [ ] 补本文件「旧↔新 + 文件:行号 + 构建结论」

## 历史页 demo v2.2 — 大显示器缩放 + 选 A + foot 换位 + 悬停跑马灯
- 用户反馈：状态在左/时间在右；24/27/32 寸大显示器（非TV、近距离）元素显大=**全局问题**，先改历史页再推广全局；选方案 A；标题溢出悬停滚；提示页面最小宽度限制。
- 调研（CSS-Tricks + 掘金）：大屏适配共识 = **rem + 动态根字号**，`clamp()` 锁上下限；超大屏**反向收缩**根字号；内容 `max-width` 封顶居中。
- demo 落地（全局可推广机制）：
  - `--root-font:clamp(14px,0.3vw+11.2px,16px)`；`html{font-size:var(--root-font)}`；`@media(min-width:2560px){15px}`、`(min-width:3440px){14px}`。
  - 卡片关键尺寸全改 **rem**（min-h 7rem / media 8.75rem / gap.padding .75rem / 字号 1rem·.8125rem），随根字号等比缩放；网格 `minmax` 改 rem，容器 `max-width` 封顶。
  - 页面最小宽度底线 = clamp 14px 下限；内容封顶不铺满大屏。
  - foot 换位（状态左/时间右）；标题溢出默认 hover；视口新增 3840/2560/1920。
  - 修 390px 横滚：对比表 `.card{overflow-x:auto}` 内部滚动。
- 验证：Playwright 真实视口，4 方案×6 视口零横滚零报错；根字号 1920=16/2560=15/3840=14；媒体宽 140/131/123 等比；foot 首元素=状态；hover 默认检测溢出。
- **推广前置**：项目 `variables.css` 的 `--text-*/--space-*` 多为 px/clamp(px)，须先引入根字号变量 + 将卡片相关 token 转 rem，否则只缩根字号不影响 px 写死元素。

## 历史页 demo 终稿 — RecordCard 整改（竖版主体 + 移动端 <480 横版自动切换）
- 背景：上一轮已落地方案 A（横版主体 16:9 封面 + 4 行正文 + 跑马灯 + 批量栏钉底）。本轮用户拍板：移动端/视口 <480px 时 grid 退化为 1 列，**单卡切回横版**——封面在左、右侧信息逐行排布（标题 / 类型 / 来源 / 集数各自独立一行，集数+时间同行两端对齐）；IPTV 无集数时「时间」单独成行贴右。
- 旧实现：仅桌面/平板横版，没有 <480 专门布局——1 列下卡片被网格拉成宽幅竖向视觉，体感"浪费空间、信息层级不清"。
- 新实现（双形态 = 默认竖版 + <480 横版）：
  - **DOM 二选一**：`RecordCard.tsx` 内 `meta-row`（默认显示，桌面用）与 `meta-col`（默认 `display:none`）并存——不靠 CSS wrap 拉伸徽标成整行横条，也不依赖 JS 断点。
  - **CSS 断点切换**：`@media (width < 480px)` 内 `.record-card` 改 `flex-direction:row` + `align-items:stretch` + `gap:var(--space-sm)`；`.record-card__media` `flex:0 0 124px` + `aspect-ratio:132/92` + `align-self:center`；`.record-card__body` `flex:1 1 0` + `justify-content:center`；`.record-card__meta-row` 隐藏、`.record-card__meta-col` 显示为 `flex column`（`align-items:flex-start` 避免徽标被 stretch 成整行横条）；`.record-card__meta-bottom` 显式 `width:100%` 保证 `time margin-left:auto` 推到 col 右缘实现两端对齐。
  - **桌面 ≥480 完全不受影响**：`.meta-col` 仍 `display:none`；`.meta-row` 仍按 §10 主体竖版规则。
- 涉及文件：
  - `src/components/RecordCard/RecordCard.tsx` — `.record-card__meta-col` 新增（与 `.record-card__meta-row` 二选一显隐）。
  - `src/components/RecordCard/RecordCard.css` — 文末 `@media (width < 480px)` 切换块（含 `align-items:flex-start` / `meta-bottom width:100%`）。
  - `changelogs/demos/demo-history-card-2026-09-03.html` — 顶部 h1 + ⑤ 说明卡「移动端 <480 自动切横版」+ 第二个 `.stage`（iframe srcdoc 视口固定 375px）；`narrow-template` 内改为**预渲染 4 张静态卡片**（规避 iframe srcdoc 内 `<script>` + `<\/script>` 不执行的渲染 bug）。
- 验证（headless Chromium 真像素）：
  - `history-narrow-probe.cjs` 390/479/480/768 视口：dir row(<480)/column(≥480)、media 124×86、col 显示/row 隐藏、time 贴右 timeRightGap=0、ep/time 同 top、IPTV 无 ep 时 time 独立成行。
  - `demo-narrow-smoke.cjs` iframe 内：4 卡渲染、首卡 `flexDirection=row`、media 124px、`meta-col display=flex`、`meta-row display=none`、零 pageerror。
  - 截图 `narrow-iframe.png` 目检通过：卡1 进度条 + 第12集 / 今天 18:30 同行两端对齐；卡3/4 IPTV 时间独立成行贴右。
- 构建：`npx tsc -b && npx vite build --emptyOutDir false` 双绿（TSC=0 / VITE=0）。
- E2E 影响：grep `scripts/*.spec.ts` 仅引用 `.record-card` / `.record-card__delete` / `.history-grid` 等，无重复 meta 元素断言 → HIS-062 (375→1列) 等用例不受影响。
- 提交：本次（横版切换）独立 commit；与四合一改动（主体竖版）一并入栈。

## 补丁（2026-09-03 晚 · progress 字号 + 桌面每档 +1 列）
- 用户反馈两则：① 封面上进度文本 `record-card__progress-text` 字号太小要调大；② 视口 >1024 时每一档 grid 多加一列，TV 端不变。
- 改动一（进度文本字号）：
  - 旧：`RecordCard.css` `.history-page .record-card__progress-text` `font-size:var(--text-2xs)`（移动 9→11px / 桌面 1080p 11px）。
  - 新：`var(--text-xs)`（移动 12px / 桌面 1080p 12px），同一行尾注释标注调档原因。文件:行号 → `src/components/RecordCard/RecordCard.css:118`。`bottom`/`right`/`line-height:1` 不动（文字自底向上生长，不侵进度条）。
- 改动二（grid ≥1024 每档 +1 列，TV 恒 5 列不变）：
  - 旧：`History.css` 断点阶梯 1024–1439→3 / 1440–1919→4 / 1920–2559→5 / 2560–3839→6 / ≥3840→7；TV `html[data-device="tv"]` 恒 5。
  - 新：1024–1439→4 / 1440–1919→5 / 1920–2559→6 / 2560–3839→7 / ≥3840→8；**TV 规则原样保留**（`History.css:342-344`，选择器特异性高于纯类断点，任意 TV 视口仍 5 列）。480–1023→2、<480→1 不变。
  - 注释块同步：`History.css:295-304` 阶梯说明更新 + 标注「当日二次调整」。
  - E2E 同步：`scripts/history.spec.ts` HIS-062 期望 1280→3 列改 **4 列**（`history.spec.ts:233`），标题/节注释同步（1280→4、600→2、375→1）。
- 验证：
  - `grid-plusone-probe.cjs`（headless 真像素）：390→1 列 row / 768→2 / 1024·1280→4 / 1440→5 / 1920→6 / 2560→7 / 3840→8；**TV(data-device=tv)@3840→5**；progressFont 恒 12px（text-xs），全绿。
  - HIS-062 E2E 通过（1280→4、600→2、375→1）。
- 构建：`npx tsc -b && npx vite build --emptyOutDir false` 双绿（TSC=0 / VITE=0）。

- ⚠️ 补档（用户反馈「漏了一个：768–1024：3 列」，上条「480–1023→2 不变」「768→2」描述作废）：
  - 旧：`History.css` 无 768 断点，`@media(min-width:480px)` 的 2 列直接覆盖到 1023。
  - 新：480 档上限缩至 767（`History.css:311-315`）；新增 `@media(min-width:768px)` → 3 列，覆盖 768–1023（`History.css:316-320`）；1024+ 仍接 4/5/6/7/8；TV 恒 5 不变。
  - 注释块同步：`History.css:297-298` 拆出 768 档行。
  - demo 镜像同步：`changelogs/demos/demo-history-card-2026-09-03.html`（内嵌网格阶梯插 768 规则 + stage-foot 档位文字补 `≥768px:3`）。
  - E2E 同步：`scripts/history.spec.ts` HIS-062 新增 **900→3** 断言点（600→2 保留、归属 480–767），标题/节注释更新（1280→4、900→3、600→2、375→1）。
- 验证（补档后）：探针 tablet-768→3 / desktop-900→3 / 1024→4 … 其余档位不变全绿；HIS-062 E2E 通过（5.0s）；build 双绿（TSC=0 / VITE=0）。

## 补丁（2026-09-03 夜 · 全站卡片 gap 统一 + 卡片列数大屏梯度 + 竖版卡标题跑马灯）
- 需求：① video card 间距统一 `var(--space-lg)`；② 竖版卡标题超宽 → 桌面 hover 跑马灯无缝滚动（与下一段内容保持 `--space-lg` 间距）、移动端自动播放；③ `--card-cols`/`--continue-cols`/`--iptv-cols` 大屏分档。
- 落地（文件:行号为主改点）：
  - **gap 统一 `--space-lg`**：TMDBMovieRow 横滚行（`.tmdb-movierow-scroll` gap+卡宽公式两处）、首页 continue 行、VideoCard 网格+骨架、Home 骨架行、Detail/Person/Player 相似网格、IPTV 网格（`index.css` `.iptv-channel-grid`）+ SkeletonIPTVCard。
  - **列数**（`variables.css` Card Columns v6 块 + 文件末尾 TV 覆盖）：
    - `--card-cols`/`--row-cols`：<768=3 / ≥768=5 / ≥1024=6 / ≥1280=8 / ≥2560=9 / ≥3840=10；**TV 恒 8**。
    - `--continue-cols`：<768=2 / ≥768=3 / ≥1280=6 / ≥2560=7 / ≥3840=8；**TV 恒 6**。
    - `--iptv-cols`（`index.css`）：<768=2 / 768=3 / **1024–1279=4 / 1280–1439=5 / 1440–2559=6** / ≥2560=7 / ≥3840=8；**TV 恒 5**。
  - **标题跑马灯**（VideoCard.tsx + VideoCard.css）：JS 仅测溢出与段宽 → 写 `--marquee-distance`（首段文本宽 + gap 间距）；CSS `@keyframes` translateX 无缝滚（两段同构，第二段补位）；`.video-card-title` 容器 `overflow:hidden` + `mask-image` 渐变遮罩；桌面 `:hover`/`:focus-within` 才播，`≤767px` 与 `html[data-device="app"]` 自动播；`--space-lg` 作两段间距（一份文本时 flex 无兄弟 gap 无副作用）。
- ⚠️ **token 曲线修复被用户否决并已回退（勿再动）**：曾按授权修 `variables.css:166-177` spacing 块——11 条 `--space-*` 的 vw 系数原为 px-per-px 裸值（0.0407vw 漏乘 100，≥768px 恒卡 MIN：桌面 `--space-lg`=12px 而非契约 28px）。修复使全站间距整体放大，**用户明示「改回去」**：spacing 块已 1:1 恢复原状（含裸值 bug），`scripts/home.spec.ts` HOME-052（767 视口 + <24px 断言）/HOME-053（≥12px 断言）同步回退 HEAD 原版。列数注释中的「间距已统一为 --space-lg」描述仍适用，但桌面解析 12px 是现状，**非疏漏**。
- 验证：vite build 绿；spec 与 HEAD 一致无需重跑。未 commit。

- **历史页 iptv 卡 live 角标移至左上角**（2026-09-03 夜，用户追加）：`RecordCard.css:85-94` 由右上（top/right）改为左上（top/left，与 IPTV 频道卡 iptv-card-cover__badges 位置一致）；`RecordCard.tsx` live-badge 渲染条件加 `!batchMode`——批量勾选框同在左上（z4），复用 IPTVChannelCard「批量隐藏角标」行为契约避免重叠。删除按钮右上、hover 出现，无冲突。验证：tsc 0 / vite build 绿；iptv.spec.ts IPTV-041 仅断言徽标存在性，不受影响。

## 补丁（2026-09-03 夜 · HeroBanner 移动端文本距图左/下缘过远修复）
- 用户反馈（收回初版「padding 偏大」表述）：**移动端 `.hero-banner__text` 离 banner 左侧、下方过远，桌面端正常**。
- 根因：`.hero-banner__text-slide` 的 `padding: var(--space-xl)` 与 `.hero-banner__text` 自身 `margin-left: var(--space-lg)` / `margin-bottom: var(--space-md)` **双重叠加** → 文本距图左 28px / 距下 24px；桌面图宽占比小无感，390px 的 16:9 矮图上留白过大。（初版只把 slide padding 收 4px 未打在点上，作废。）
- 新（仅 `@media (width <= 767px)` + `html[data-device="app"]` 副本内双收，桌面 >767px 原值不动）：
  - `.hero-banner__text-slide { padding: var(--space-md); }`（16→8px）→ `HeroBanner.css:641-643`；app 副本 `:660-662`。
  - `.hero-banner__text { margin-left: var(--space-sm); margin-bottom: var(--space-xs); }`（12/8 → 6/3px）→ `HeroBanner.css:645-647`；app 副本 `:664-666`。
  - 效果：距左 28→≈14px、距下 24→≈11px。
- 观感 demo：`changelogs/demos/demo-hero-text-slide-padding-2026-09-03.html`（重写为 OLD 28/24 ↔ NEW 14/11 对比，黄虚线标注 slide padding 测量框；初版 16↔12 内容被覆写），changelogs/README.md 索引描述同步。
- 构建：`npx tsc -b && npx vite build --emptyOutDir false`（npm run build 撞 safe-delete dist 清空保护，已知坑）。
- E2E：无类名/结构变更，无 spec 引用该间距，无需改动。

## 补丁（2026-09-03 深夜 · HeroBanner 标题位 logo 加载时序重构）
- 用户反馈：首页 banner 昨天新增 detail-hero-logo 显示逻辑后，**滑动时才判断并显示，且滑动 banner 变卡**；要求 logo 加载与 banner 预加载保持一致、显示标题/logo 的判断提前。
- 根因（HeroBanner.tsx 旧 useHeroLogos）：首屏仅串行 prefetch 前 6 项；第 7+ 项靠 activeKey 按需补拉，而 activeKey 在滑动结束（displayIndex 变化）才变 → `/images` 请求落在滑动瞬间，数百 ms 后才 setLogos → ① 决策未决期渲染 `<h1>`、回来才跳 `<img>`（「滑到才判断 + 闪变」）；② logo **像素文件从不预加载**（缓存只存 file_path），决策落地时 `<img>` 首次挂载现下载现解码、且 setLogos 触发整颗组件重渲染，正撞 600ms 滑动动画 → 卡顿。
- 新方案（commit 01666e0，与背景图 preloadRange 预取同节奏）：
  - `useHeroLogos` 签名 `(items, activeKey, active)` → `(items, focusIndex=displayIndex, active)`（`HeroBanner.tsx:165`）：移除「首屏串行 6 + activeKey 按需」两 effect，改为 ① **焦点 ±HERO_LOGO_WINDOW(3) 窗口并发预取**（`:194-213`，未决才发、inflight 去重、滑入前决策已落地 → 滑动期零请求零 setState）；② **空闲补齐**（`:215-255`，requestIdleCallback 串行补全列表剩余条目，setTimeout 退化 + cancelled/clearHandle 兜底，任何滑法不再遇「未决」）。
  - `fetchHeroLogo` 拿到 file_path 即 `preloadImage(buildImageUrl(path,'w342'))`（`:152-156`，与 renderText `<img>` 同 URL）→ 挂载即命中缓存，无现下载解码。
  - 移除 `heroLogoFailed` state：文件加载失败改 `dropLogo(key)`（缓存记 null 不再重试 + 移出就绪映射，一次 setState 回落标题，`HeroBanner.tsx:259-271`）；渲染只读「已确认」映射（`:311`、renderText `logoPath = (logoKey && heroLogos[logoKey]) || undefined`）稳定不闪变。
- 行为：请求总量不变（每 key 一次 /images），仅时机提前分散；首屏不再一次性串行 6。桌面 hover 预览项在缩略图窗口内、已被窗口覆盖。极端快速连滑超出窗口且首轮无缓存时仍可能短暂标题后补正，空闲补齐完成后全决。
- 时序 demo：`changelogs/demos/demo-hero-logo-prefetch-2026-09-03.html`（模拟 20 项列表 / RTT600ms，双轨道对比「滑到才发→标题闪变跳 logo」vs「窗口预取+空闲补齐→滑到即 LOGO」，含请求计数），README 索引同步。
- 构建：`npx tsc -b && npx vite build --emptyOutDir false` **双绿**。E2E：无类名/结构变更，无 spec 引用内部预取行为，无需改动。

## 补丁 2（2026-09-03 深夜 · HeroBanner 空闲补齐改缓慢递进）
- 用户反馈（v2，commit 01666e0 之后）：空闲补齐「不要调用剩余全部 logo」，密集拉取导致滑动 banner 仍卡，要求缓慢递进补齐、不要一次性获取。
- 根因（HeroBanner.tsx 旧 ② effect，commit 01666e0）：`requestIdleCallback(cb, { timeout: 3000 })` 链式推进——浏览器一空闲就近乎无间隔连续 step（每步间隔 ≈ 网络 RTT，几百 ms），剩余十几项 `/images` + 像素预热在几秒内全部打出；且推进不感知 focusIndex 变化，撞上轮播/滑动动画抢主线程与带宽 = 卡。语义上虽是「串行」，空闲帧下推进速率 ≈ RTT，观感等同一次性拉完。
- 新方案（commit b21771f）：空闲补齐 effect 依赖加 `focusIndex`（`HeroBanner.tsx:263`）——每次滑动/轮播切换重建 effect → 计时器重置，先静默 `HERO_LOGO_IDLE_COOLDOWN_MS(1200ms)`（`:130`/`:245-248`）才可能推进，滑动活跃期天然零请求零 setState；静止后按 `HERO_LOGO_IDLE_STEP_MS(2000ms)`（`:128`/`:253`）固定间隔链式补、每步一项，补齐摊成数十秒长尾。移除 `requestIdleCallback` 的 timeout:3000 逼迫机制（`HeroBanner.tsx:220-263`）。
- 窗口预取（±3）未动——滑动期间必要的前置拉取仍在窗口范围内小量并发。
- demo 同步：`changelogs/demos/demo-hero-logo-prefetch-2026-09-03.html` 右侧轨道 idle 间隔 420ms→1800ms、新增 `N.lastMove` 滑动静默标记（`stepNew` 每次切换更新、`idleFill` 静默期内跳过）。
- 构建：`npx tsc -b && npx vite build --emptyOutDir false` **双绿**。E2E：无类名/结构变更，无 spec 引用内部预取节奏，无需改动。
