# 改动记录 2026-09-15


---

## 本地记忆提炼为共享协作文档 + 自研 skills 归档（commit fe6cd28）

- 问题：主用户本地「跨会话仍成立」的工程结论只存在于个人记忆层 `.workbuddy/memory/ref-*.md`（该目录被 `.gitignore` 排除、不入库），协作者 clone 后拿不到这些约定；同时本项目自研 skill 只装在主用户机器上，协作者无对应能力。
- 旧逻辑：`.workbuddy/memory/MEMORY.md` 只放不变量索引，细则在 `ref-css-layout.md`（断点宪法 / 流体 token / 高度体系 / 列数梯度 / CSS 陷阱 / 跑马灯 / 基线隔离验证法 / stylelint 口径）与 `ref-code-runtime.md`（播放器自愈链 / 跨页签同步 / `reqSeqRef` 竞态 / 逻辑分页 / E2E 假绿坑 / 工具链陷阱）——两者均为个人记忆，**不进仓库**；`docs/agents/` 虽有 patterns.md / pages.md 覆盖「模式层」，但上述「细则层」无对应共享文档。skills 亦无仓库归档。
- 新逻辑：
  1. 新建 `docs/agents/css-layout-conventions.md`、`docs/agents/runtime-conventions.md`，把 `ref-*.md` 里跨会话仍成立的结论**提炼**（非搬运）为团队共享分片文档；
  2. `AGENTS.md` 与 `docs/agents/index.md` 索引表各增两行指向；
  3. `docs/agents/RULES-MANIFEST.md` 增加 `skills/**` 应入库行，并写明「个人记忆提炼进 docs、不得为共享而入库 `.workbuddy/`」与 skills 归档约定；
  4. 新增 `skills/`：自研 skill 原始目录 + `project-skills.zip` + `README.md`（安装说明），剔除 `_meta.json` / `_user_meta.json` / `_icon.png` 等本机元数据；
  5. 个人记忆层 `.workbuddy/memory/` 仍保持被忽略，未入库。
- 涉及文件：
  - `docs/agents/css-layout-conventions.md`（新增）
  - `docs/agents/runtime-conventions.md`（新增）
  - `AGENTS.md`、`docs/agents/index.md`、`docs/agents/RULES-MANIFEST.md`（索引/清单更新）
  - `skills/fullscreen-overlay-portal/`、`skills/check-environment/`、`skills/project-skills.zip`、`skills/README.md`（新增）
- 关联 Demo：（无，纯文档 / 资源归档，无观感改动）
- 构建：`tsc -b` 通过；`vite build --emptyOutDir false` ✓ 2296 modules / 47.77s（`VITE_EXIT=0`）
- 提交：`fe6cd28`（12 文件，+1539），已推送 `origin/master`

---

## skills 归档扩至 7 个（含第三方协同工作流）+ 补 changelog 片段

- 问题：上一批 skills 归档只收了自研 2 个（`fullscreen-overlay-portal`、`check-environment`），协作者仍缺本项目协同流程依赖的第三方工作流 skill（E2E 调试、设计取向、TDD、方案追问、会话交接）；且上一批（`fe6cd28`）为纯文档改动，**未留 `changelogs/_pending/` 片段**，与「每日改动流水唯一出口」约定不符。
- 旧逻辑：`skills/` 仅 2 个自研目录，`skills/README.md` 明确排除第三方 skill；`changelogs/_pending/` 为空，`fe6cd28` 的改动只存在于 commit message 中，跨会话追溯缺对照记录。
- 新逻辑：
  1. 补录 5 个第三方协同工作流 skill：`playwright-cli`、`frontend-design`、`tdd`、`grill-me`、`handoff`——目录名按各自 `SKILL.md` 的 `name` 字段规整（去掉 `-3__skillhub` / `-tool__skillhub` 安装后缀）；
  2. 剔除 `_meta.json` / `_user_meta.json` / `_skillhub_meta.json` / `_icon.png` 等本机安装元数据，只保留 `SKILL.md` 与随附脚本；
  3. 重打包 `skills/project-skills.zip`（10 条目 / 33051 B）并重写 `skills/README.md`（7 行清单表：来源列区分「自研」与「第三方（含上游链接）」、安装说明、版权与可移除声明）；
  4. `docs/agents/RULES-MANIFEST.md` 的 `skills/**` 行与备注段同步为「自研 2 + 第三方 5」，并在第 6 节基线追加 2026-09-15 记录；
  5. 按「每日改动流水」约定补 `changelogs/_pending/` 片段，push 前跑 `changelog-collect.mjs` 合并归档。
- 涉及文件：
  - `skills/playwright-cli/`、`skills/frontend-design/`、`skills/tdd/`、`skills/grill-me/`、`skills/handoff/`（新增）
  - `skills/README.md`（重写）、`skills/project-skills.zip`（重打包）
  - `docs/agents/RULES-MANIFEST.md`（清单与备注同步）
  - `changelogs/_pending/2026-09-15-1243-extract-local-memory-to-shared-docs.md`、`changelogs/_pending/2026-09-15-1315-skills-archive-expand-to-7.md`（本片段）→ 合并入 `changelogs/2026-09-15.md`
- 不收录：金融数据类第三方 skill（NeoData 金融搜索、WeStock Data）与本视频项目无关，避免仓库噪声。
- 关联 Demo：（无，纯文档 / 资源归档）
- 构建：`tsc -b` 通过；`vite build --emptyOutDir false` ✓（`VITE_EXIT=0`）

---

## 三端设计规范审查报告（只读审查，未改代码）

- 日期：2026-09-15
- 问题：用户反馈「页面的文本灰蒙蒙、颜色设置很奇怪、只有角标或类型标签才该用这类颜色；项目缺少基础规范」。经量化确认三层根因。
- 旧逻辑（被审查的现状）：
  - `--color-text-tertiary: #999`（暗色 `#666`）对比度仅 **2.85:1 / 2.87:1**，却在 `color:` 中出现 **167 次**且用于承载信息（`Chart.css:259 .chart-row__desc`、`Settings.css:772 .settings-menu-item__desc`、`VideoCard.css:549` 时长）。
  - 彩色语义挪用：`VideoCard.css:344-352` 年份+类型徽标固定危险红底 + 白字（3.76:1）；`:367-375` 三种状态色为硬编码 Tailwind 色 `rgba(59,130,246/.85)` 等（不随主题，白字在浅色封面上低至 2.04:1）。
  - `index.css:588` `:root:not([data-device="tv"]) :focus-visible { outline: none !important }` → 非 TV 全站无键盘焦点（WCAG 2.4.7 AA 失败）；`--focus-ring` / `--shadow-focus` 因此近乎零引用。
  - 桌面段2 字号逆视口递减：实测 1920 正文 **13.00px** < 平板 768 的 14.00px；2560 处 `--text-xs` = `--text-2xs` = **11.88px**（档位塌缩）。
  - TV 1080p 正文 15px / 角标 11px（10-foot 建议正文 ≥18sp）；TV rem 曲线实测 −6.25%（header 71.25 vs 文档 76；4K 142.5 vs 152）。
  - 移动端最小字号 **9.05px**、`--comp-btn-min-height` **34.66px**（HIG 44pt）、`.mobile-tab-bar` 无 `env(safe-area-inset-bottom)` 且 `index.html` 无 `viewport-fit=cover`。
  - 基础规范缺口：无字重/行高/层级/时长 token（z-index 60 个字面量、`transition` 内联时长字面量 ≈250 处、字重 6 种含 800/900）；死 token **50/290（17%）**。
- 新逻辑（本次产出）：新增 `docs/DESIGN-REVIEW-2026-09-15.md`——含外部标准基线（WCAG 2.2 / M3 / HIG / 10-foot UI）、P0/P1/P2 问题清单（每条附文件:行号与实测值）、三端符合度矩阵、建议的《基础设计规范基线 v1》token 提案（含达标色值对照）、分批修复路线图与 7 项待拍板项。
- 涉及文件：`docs/DESIGN-REVIEW-2026-09-15.md`（新增，只读审查，**未改动任何源码**）
- 关联 Demo：无（非观感改动；后续 B1/B3/B4 落地时按规范补 Demo）
- 构建：未改代码，无需构建
- 备注：审查用临时脚本已删除；Playwright 实测页面加载在当前环境崩渲染进程，改用「注入源 CSS + setContent」方式取得 token 解析值（方法已写入报告附录 C）。

---

## 三端设计规范实施方案（文字四档 / 字号阶梯 / TV 10-foot 基线 / 移动端）

- 日期：2026-09-15
- 背景：承接同日《三端设计规范审查报告》，用户对报告 §七 三项关键项拍板：① `tertiary` 提档**接受**；② 桌面段2 字号**按行业标准合理设计、不设可读性下限**；③ TV 字号**符合行业主流标准即可**。
- 旧逻辑（被本方案取代的写法）：
  - 报告 §五 提议的「`clamp(下限, 递减曲线, 上限)`」字号下限钳制 → **作废**（用户明确不要下限钳制）。
  - `--color-text-tertiary: #999`（2.85:1）承担信息性文字；`--color-danger` 底 + 白字角标 3.76:1；`.video-card-status--*` 硬编码 Tailwind 色随封面变化低至 2.04:1。
  - 桌面段2 字号逆视口递减（1920 正文 13.00 < 平板 14.00）；2560 处 `--text-xs` = `--text-2xs` = 11.88（塌缩）。
  - TV 1080p 正文 15px / 角标 11px（10-foot 实践下限正文 ≥18sp）；TV rem 曲线实测比文档低 6.25%（根字号 15px ≠ 设计假设 16px）。
  - `--comp-btn-min-height` 移动端 34.66px（HIG 44pt）；`.mobile-tab-bar` 无安全区且 viewport 无 `viewport-fit=cover`。
- 新逻辑（本次产出）：新增 `docs/DESIGN-PLAN-2026-09-15.md`——7 个批次（B0 基线 / B1 色彩 / B2 焦点 / B4 根字号解耦 / B3 字号阶梯 / B5 TV / B6 移动端 / B7 工程化），每批含文件:行号、可直接粘贴的 CSS、实测预期值、验收标准、回归风险与回滚方式。关键设计：
  - **小四档恒定**：`2xs/xs/sm/base` = 11/12/13/14px 全端不流动（仅乘 `--ui-scale`），恒定即消除逆递减、档位塌缩与 rem 循环；移动 390 `--text-2xs` 由 9.05 → 11px。
  - **大四档标准档**：`lg/xl/2xl/3xl` 段2 改为「768 连续 → 1440 命中 16/18/22/28 → 冻结」，全部 `clamp` 斜率 `±0.09926vw` 经公式校验在 768 处精确等于段1 末值。
  - **`--root-font-size` 解耦**（B4 前置）：新增独立 token 复刻现状根字号曲线，`html` 改引用它，使 `--text-base` 调整不再带动全仓 ≈200 处 rem 消费点；实测 8 组视口偏差 0.000%。
  - **状态色双档**：新增 `--color-{success,warning,error,info}-text`（#237804 / #a05c00 / #cf1322 / #0958d9），一个值同时满足「浅底文字」与「白字角标底」；`#ad6800` 实测仅 4.41 已排除。
  - **封面角标统一口径**：`rgba(0,0,0,0.6)` 中性玻璃底 + 白字（任意封面图下最坏 5.74:1），色义改由圆点承载 → 红色只保留「危险 / LIVE」。
  - **TV 10-foot 基线**：1080p 正文 15 → 20px、次要 18、角标 14、标题 30/36，4K 保持 2×；TV 字号块由 rem 改写为 px（TV 根字号被钉 15px，rem 写法会产生第二次「文档值≠实测值」偏差）。
  - **发现的两个既有缺陷**（本方案顺带修复）：`.stylelintrc.json` 存在两个同名 `declaration-property-value-disallowed-list` 键 → 后者覆盖前者，`width/height: 100vw/100vh` 禁令**实际失效**；`index.html` 无 `viewport-fit=cover` 导致 3 处 `env(safe-area-inset-*)` 恒为 0（安全区策略名存实亡）。
- 涉及文件：`docs/DESIGN-PLAN-2026-09-15.md`（新增）、`.gitignore`（新增 `!docs/DESIGN-PLAN-*.md` 与 `!docs/design/` 白名单）。**本轮未改动任何源码**。
- 关联 Demo：待 B1/B3/B5 落地时按规范补 `changelogs/demos/demo-design-ladder-2026-09-15.html`（方案 B0-2 已列）
- 构建：未改代码，无需构建
- 备注：全部对比度与字号读数由临时脚本按 WCAG 2.2 公式与项目 clamp 曲线重算（非估算），脚本与产物已删除，公式已收入方案附录 A。方案 §十二 仍留 5 项待用户确认（正文取 14px、大四档提升、action-blue 方案、移动按钮高度、viewport 缩放解禁），均附推荐值且不阻塞 B1/B2/B4。

---

## 启动骨架按路由变形（消除非首页刷新的「结构错构」）

- 日期：2026-09-15
- 问题：在 `/iptv`、`/collections`、`/browse` 等任意非首页硬刷新（F5 / 直输 URL / Capacitor 冷启动）时，都会先看到一套**首页形态**的启动骨架（hero 16:9 扁条 + 一行竖版海报卡），随后跳变到目标页真实结构。观感上像「先闪一下首页再变成别的页」。
- 旧逻辑：`#boot-splash` 是 `index.html` 里写死的一套 DOM（`.bs-hero` + 一行 8 张 `.bs-card`，仅随视口 `--card-cols` 分档），**没有按路由分支的代码**。原因是单入口 SPA：全站只有一份 `index.html`（`dist/` 内亦然），Vite dev 与 Cloudflare Pages 的 SPA fallback 把任意路径都返回同一份 HTML；`main.tsx:58` 的摘除条件只看 `#root.childElementCount > 0`，与 `location.pathname` 无关。因此「非首页刷新看到首页骨架」是必然结果。
- 新逻辑：`index.html` 内联脚本（零依赖、不引用任何外部资源、无 `100dvh`、保持 `pointer-events:none`）读取 `location`（含 hash 路由 `#!` 与 `#/` 两种形态）判定页型，**在 HTML 解析期就地重建骨架 DOM**，10 种形态：

  | shape | 触发路径 | 骨架形态 |
  | --- | --- | --- |
  | `home` | `/` | 复用静态 HTML 原骨架，不改建 |
  | `iptv` | `/iptv` | 左栏分类 + 频道网格（`--iptv-cols` 分档） |
  | `play` | `/iptv/play` | 播放器骨架 + 提示文案「正在打开播放器…」 |
  | `browse` | `/browse` | 筛选条 + 卡网格 |
  | `chart` | `/chart` | 筛选条 + 榜单行 |
  | `collections` | `/collections` | 双分区头 + 记录卡网格 |
  | `history` | `/history` | 分区头 + 记录卡网格 |
  | `detail` | `/detail/*` | hero + 文本块 |
  | `person` | `/person/*` | 人物头 + 文本块 |
  | `plain` | 其余（`/settings`、`/source-checker`…） | 仅顶栏 + 居中「正在启动…」，中性兜底 |

  - 列数分档与真实网格**逐档对齐 token**：海报网格 `3 →4(768) →5(1024) →6(1280) →7(1440) →8(1920)`（`--card-cols`）；频道网格 `2 →3(768) →4(1024) →5(1440)`（`--iptv-cols`）。
  - 设备档与 `AppLayout` 的 `data-device` 同源语义，新增 `#boot-splash[data-cols="tv"|"app"]` 覆盖档，**恒定不随视口流动**（tv：海报 8 / 频道 5；app：海报 3 / 频道 2）。
  - 封面比例按卡型分：`.bs-row` 海报 2:3 / `.bs-row--channel` 频道 3:2 / `.bs-row--record` 记录卡 16:9 + 两行文字条。
  - IPTV 左栏阈值与真实页一致：`1024` 起显示，默认 `display:none`。
- 涉及文件：`index.html`（+199 / −12，脚本 5670 字符，全部内联在 `<style>` / `<script>`，零外部请求）
- 关联 Demo：无（启动骨架为纯 HTML/CSS 结构，形态核对以逐路由几何读回为准）
- 构建：`vite build` 通过（15.19s），产物 `dist/index.html` 已含 `data-shape` / `bs-rail` / `bs-split`。`npm run build` 首步 `tsc -b` 报**既有**错误 `src/components/LazyImage/LazyImage.tsx(236,5): TS2322 Type 'number' is not assignable to type 'Timeout'`（与本次改动无关，绕过方式：直接调 `vite build`）。
- 验证：沙箱内 Playwright/Chromium 合成器不产帧（`page.screenshot` 与 CDP `Page.captureScreenshot` 均挂），改用 **jsdom 离线跑分支脚本 + 几何读回**，17 个用例全绿 + 列数交叉核对 0 漂移：

  ```
  OK  /  /iptv  /iptv/play  /collections  /history  /browse  /chart
  OK  /detail/603  /person/1234  /settings      （shape 与期望全一致）
  OK  /  /#/  /#/iptv  /collections/            （hash 与尾斜杠形态）
  OK  / [tv]  /iptv [tv]  /iptv [app]           （设备档覆盖）
  390/768/1024/1280/1440/1920/2200/2560/3840 海报 3-4-5-6-7-8-8-8-8  频道 2-3-4-4-5-5-5-5-5
  IPTV 左栏阈值 1024 起显示 OK / 默认 display:none OK
  列数漂移档数=0   ALL_PASS
  ```
- 备注：改动已随 `59ab4e8 docs(design): …+ 启动骨架/白名单留痕` 入库（并行会话一并提交）。

---

## B7 工程化收口（含 B1–B6 三端设计规范落地）

- 日期：2026-09-15
- 背景：承接同日《三端设计规范审查报告》与《实施方案》，用户拍板 5 项待决（正文取 14px / action-blue 走 A / 移除缩放禁令 / 大屏不设可读性下限 / TV 对齐行业标准）后进入实施。本片段记录 **B1–B7 全量落地**，其中 B1–B6 已分批 commit+tag（`design-b1`…`design-b6`），本片段为当日统一留痕。

### 旧逻辑 → 新逻辑（按批次）

| 批 | 旧 | 新 |
| --- | --- | --- |
| B1 | `--color-text-tertiary: #999`（2.85:1）全站 167 处承载信息 → 灰蒙蒙 | `#6b6b6b`（白底 5.33:1）；新增 `--color-text-disabled: #bfbfbf` 作唯一 <4.5:1 档；暗色 `#666→#8c8c8c` |
| B1 | 状态色一档多用：`--color-success` 既作填充又作文字（白底 2.27:1） | 双档：fill 档（`--color-*`）只管填充；新增 `*-text` 档（`#237804/#a05c00/#cf1322/#0958d9`）承载文字/图标。`--color-danger: #ef4444→#cf1322` |
| B1 | 封面角标彩色半透明底 + 白字（浅封面可低至 2.04:1） | 统一 `.badge-chip` 中性深玻璃底 `rgba(0,0,0,.6)` + 白字（最坏 5.74:1），色义改由圆点承载 |
| B2 | 非 TV 焦点环被 `outline:none!important;box-shadow:none!important` 抹掉（违反 WCAG 2.4.7） | 恢复 `outline: 2px solid var(--color-primary)` + `offset: 2px`；>1440px 卡片由 box-shadow 改 outline 口径 |
| B4 | `html { font-size: var(--text-base) }` → 改字号连带全仓 ≈200 处 rem | 新增 `--root-font-size` 解耦（9 组视口实测 diff 为空） |
| B3 | 段2 字号逆视口递减（1920 正文 13 < 平板 14）、2560 档位塌缩 | 小四档恒定 11/12/13/14；大四档段2「768 连续 → 1440 命中 16/18/22/28 → 冻结」；相邻档差 ≥1px |
| B5 | TV 正文 15px、角标 11px（10-foot 实践下限 ≥18sp） | TV 8 档改 px 基准 `clamp(A,(A/19.2)vw,2A)`：1080p 正文 20 / 4K 40；一级栏 `136px→208px`(TV) 配套放开 `max-width 44%→52%` |
| B6 | `--comp-btn-min-height` 移动端 34.66px；tab 28px；viewport 带 `user-scalable=no` | 44px / 36px；viewport 改 `viewport-fit=cover`，移除缩放禁令（WCAG 1.4.4） |
| B7 | `.stylelintrc.json` 存在两个同名 `declaration-property-value-disallowed-list` → 后者覆盖前者，`100vw/100vh` 禁令**实际失效** | 合并为单一数组，修复后暴露 22 处违规并全部清理（`Detail.css` 全出血场景显式 `stylelint-disable-next-line` 豁免） |
| B7 | z-index 全仓 154 处裸数字（1…10000） | 新增 `--z-*` 12 档阶梯；迁移 29 处页级浮层站点（数值不变、零渲染变化） |
| B7 | `prefers-reduced-motion` 用 `[class*="loading"]` 子串匹配恢复动画 → 把装饰性 fade-in 也强制成 infinite 循环，与「禁用装饰性动画」意图相反 | 改为显式白名单 5 个功能性无限旋转指示器 |
| B7 | TV 兜底 `--safe-area-x/y: 3vw/3vh`，注释称「与滑块默认 3 对齐」，实际 store 默认 `tvOverscan: 5` | 改 `5vw/5vh`，注释同步为「预设 0/5/10/15/20」 |
| B7 | 颜色/字号/z-index 全靠 review 人肉把关 | 护栏：ESLint 规则扩到 `style={{}}` + TS 颜色常量；新增 `scripts/design-audit.mjs`（CSS 层裸 hex / fill 档误用 / 裸 z-index / 字重 800-900），**基线棘轮**只拦新增 |

### 护栏机制（本轮新增，可复用）

- `pnpm run lint:design` → `scripts/design-audit.mjs --strict`；存量违规快照在 `scripts/design-audit-baseline.json`（666 条），只拦新增。
- 清债后跑 `pnpm run lint:design:update` 收紧基线（本轮 689 → 666）。
- `lint:all` 顺序改为 `lint:design → lint:css → lint → build`。
- 规范真源：`docs/design/BASELINE.md`（一页速查）+ `docs/design/a11y-CHECKLIST.md`（提交前自查）。

### 涉及文件

- 源码：见 front-matter `files`（22 个 CSS + `index.html` + 1 个 ESLint 规则）
- 文档/工具：`docs/design/BASELINE.md`、`docs/design/a11y-CHECKLIST.md`、`scripts/design-audit.mjs`、`scripts/design-audit-baseline.json`、`package.json`、`.gitignore`（新增两条 `!scripts/design-audit.*` 白名单）

### 验证

- `npx tsc -b` ✅ / `npx vite build --emptyOutDir false` ✅（沙箱清 dist 撞批量删除保护，用标准替代命令）
- B4 零回归：Playwright 注入源 CSS 探针，改前/改后 9 视口 × 7 token diff 为空
- B3 实测 6 视口 × 8 档；B5 实测 TV 1080p/4K 逐档；B6 实测 390 触摸目标 34.66→44.00、tab 28→36.00
- `node scripts/design-audit.mjs` → 4 项 enforce 检查全绿、无新增违规

### 构建

`npx tsc -b && npx vite build --emptyOutDir false` → pass

### 未做（下沉待办）

- B7.4 存量债清理：CSS 裸 hex 266 处、fill 档语义色误用文字 48 处、其余裸 z-index 125 处、字重 800/900 8 处、内联时长字面量 442 处。已全部纳入基线棘轮，不再增长；建议后续按模块分批清并逐步收紧基线。
- `.stylelintrc.json` 存量 1617 处违规（`order/properties-order` 665 为主）为**改动前既有**，`lint:css` 长期为红，未在本轮处理。
- TV 块内 rem 写法的「文档注释值 ≠ 实测值」历史漂移（如 `--comp-input-height-tv` 注释 58px、实测按根字号 15px 折算）未清理。
