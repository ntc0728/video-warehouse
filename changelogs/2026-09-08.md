# 2026-09-08 · 收藏/历史页桌面 rail 布局整改 + <1024 面板只包 card

> 方案 demo：`changelogs/demos/demo-record-left-rail-2026-09-08.html`
> 实施提交：`020614a`（9 files, +1081/-70）
> 范围：仅收藏/历史页 record 线；IPTV 页大改、workflow、design token 分档等另一条线不在本次 commit

## 1. 桌面端 rail 布局（≥1024 非 TV/App）

- `src/components/RecordShell/RecordShell.tsx`：新增 `contentTitle` / `contentMeta` props；把 `{children}` 包进 `.record-content`，使 `.record-main` 在 ≥1024 用 grid 时只有「aside / content」两个 cell。
  - 修复点：不加该层时 RecordFilterPanel / 滚动哨兵 / 批量操作栏 / 弹窗各自占一个 grid cell，rail 布局下错乱。该层同时延续「父链 flex 拉伸」约定（空状态/加载态整页垂直居中依赖它）。
- `src/components/RecordShell/RecordShell.css`：≥1024 rail 布局去卡片 chrome（border/radius/background/shadow 全清）、左栏 176px 固定、`gap: var(--space-lg)`；rail-item 统一视觉（`.status-tab` / `.category-segmented__item` / `.record-filter-chip` / `.record-sort-btn` / `.action-btn` 照抄 demo `.rail-item` 规格）。
- `src/assets/styles/animations.css`：`.page-transition[data-variant="soft"] > .record-page`（含 `[data-revisit="true"]`）豁免入场动画——入场感交由内容区卡片 stagger（`.video-card-grid` / `.history-grid` 已有）承接，左栏 rail 不参与。

## 2. <1024 与移动端：面板只包 card

- `src/components/RecordShell/RecordShell.css`：面板样式从 `.record-main` 迁到 `.record-content`（三处：移动端 / app 恒移动 / 桌面 ≥768），`.record-main` 只保留布局属性 + `padding: 0`。rail 裸露在外，距离页面边的安全边距由外层 `.page-padding`（index.css:617）提供。
- **关键回归与修复**：面板所在层必须 `min-height: auto`（三处 `.record-content` 均加，覆盖基础定义 `min-height: 0`）。
  - 根因：`.record-content` 基础定义是 `flex: 1 1 0`（flex-basis 0）+ `min-height: 0`。面板在 `.record-main`（`min-height: auto`）上时被内容撑开；搬到 `.record-content` 后高度坍缩为 flex-grow 分配的「剩余空间」，内容溢出面板底部（overflow visible）→ card「脱标」、不撑开 `.record-main`。
  - Playwright 实测（向 `.record-content` 注入 30×100px = 3000px 测试内容）：
    | 视口 | 面板高度 | 内容溢出 | `.record-main` 高度 |
    | --- | --- | --- | --- |
    | 900 | 683 → **3175** | 2317 → **-2** | 733 → **3224** |
    | 390 | 744 → **3163** | 2256 → **-1** | 790 → **3208** |
    溢出为负 = 面板完全包住内容（余 1-2px border/padding）。
  - 空态回归：900 视口 main 733 / content 683（flex-grow 仍填满剩余空间）；1280 rail 布局 main 732 / aside 467 / content 156 正常。空状态垂直居中链未破坏。
- 教训：**把「面板样式」迁移到别的元素时，必须同步检查该元素的 `min-height`**。`min-height: 0` 是给 flex item 收缩用的（空状态居中链需要），但一旦该元素变成有 border/背景的容器，min:0 就会让内容溢出容器边界——面板所在层永远要 `min-height: auto`。

## 3. rail 通用规则特异性回归（COL-031）

- 现象：桌面 1280 视口「更多筛选」按钮（`.action-btn--filter`）可见，COL-031 失败。
- 根因：rail 布局 rail-item 通用选择器 `html:not(app):not(tv) .record-actions .action-btn`（特异性 `(0,2,1)`）与 ≥768 块的隐藏规则 `html:not(app) .record-actions .action-btn--filter`（特异性 `(0,2,1)`）**同特异性、定义在后** → 后者胜，把 `display: none` 覆盖成 `flex`。
- 修复：`RecordShell.css` 在 rail 块通用规则之后以更高特异性 `(0,3,1)` 显式重申 `.action-btn--filter { display: none }`。诊断确认 `display: none`、rect 0×0。

## 4. 配套改动

- `src/components/RecordCard/RecordCard.css`：封面通铺出血——卡片 `padding` 归零、`.record-card__body` 补回 padding；同时修复 `.history-page .record-card:hover`（`(0,3,0)`）压过单独选中态的优先级问题。
- `src/pages/Collections/index.tsx`：`showChannels` 门控，状态筛选（`statusFilter`）不混入 IPTV 频道项。
- `src/pages/History/index.tsx` + `History.css`：移动端时间节点滚动吸附（`top: var(--space-sm)`），且**仅吸附态**加 `background: var(--color-background)`（新增 `.is-stuck` class + `useLayoutEffect` 滚动检测；非吸附时背景透明避免遮挡下层内容）。
- `src/pages/Collections/Collections.css`：`.record-main > .record-content > ...` 选择器路径补一层（配合 RecordShell 新增的 `.record-content` 包裹）。

## 5. E2E 状态（scripts/history + collections + collection-cross-tab）

- **16 passed / 2 failed**。已修 1 个真回归（COL-031 filter 按钮），另 2 个失败为 rail 整改的 spec 期望过时（非代码回归），未擅自改 spec：
  1. `HIS-062: 列数随视口变化` — 期望 1280 视口 5 列，实际 4 列。rail 左栏 176px + gap 吃掉内容区宽度，1280 档列数自然降档。需二选一：更新 spec 期望，或调整历史页列数分档。
  2. `COL-031`（第 114 行起）— 期望 1280 视口清空按钮收进「⋯」溢出菜单，实际直接显示。rail 布局起点由 `>1440` 下移到 `≥1024`，1280 已落在 rail 区间，溢出菜单降级逻辑不再适用。需更新 spec 分档断言（1280 与 1440 现在同属 rail）。
- 构建：`npx tsc -b` 0 错 / `npm run build` ✓ 13.65s。

## 6. 未纳入本次 commit（另一条工作线，按指示不碰）

IPTV 页大改线（`src/pages/IPTV/*`、`UniversalPlayer/*`、`IPTVChannelCard/*`、`iptvService*`、`useIPTVStore*`、`iptvOrgService*`、`channelLogo*`、`types/iptv.ts`）、design token 分档（`variables.css` / `index.css`）、`.github/workflows/*`、`docs/KNOWLEDGE.md`、`src/services/database.ts`、`src/components/RecordCard/RecordCard.tsx`（`is-available` 属于可用性检测移除线）、`changelogs/demos/demo-iptv-rail-2026-09-08.html` —— 全部保留在工作区未 stage。

- ⚠️ `src/pages/IPTV/index.tsx` 中本会话曾顺手删过一行未用解构（`ensureSourceChannels`，IPTV 大改移除调用后残留），按「只处理我修改过的代码」未纳入本次 commit，工作区保留，需 IPTV 线一并处理。
