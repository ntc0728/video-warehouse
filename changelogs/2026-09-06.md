# Changelog · 2026-09-06

## Demo：CategoryQuickAccess 方案 A2 · 热门分类拓展面板（真实 TMDB 数据，待审阅）

**【诉求】** 在方案 A 基础上拓展：「全部分类」改跳 browse；点击导航 chip 在下方展开面板，显示该分类的 genre 分类 + 该分类最热门搜索值（含热度/搜索值）；默认显示热度最高的分类类型及各自最热门搜索值；数据全部真实 TMDB 接口获取，不写死。

**【demo 实现】**（`changelogs/demos/demo-category-quick-access-a2-2026-09-06.html`，token 与 v2.2 逐字对齐，未动真实源码）：
- **导航条**：首页 / 电影 / 剧集 / 综艺 / 动漫 / 纪录片 / 排行榜 + 「全部分类 →」（点击 → `/browse`，不再 hover 下拉）。各分类 chip 带 🔥 热度徽标（真实 trending 聚合计数）。
- **默认「首页」面板 = 分类热度榜**：`/trending/all/day` 按分类桶（media_type + genre_ids）聚合 Σpopularity 排序——热度最高的分类类型排行 + 每分类最热门搜索值（海报 + 标题 + 🔥popularity + ★评分）+ 热度条；点击行展开对应分类。
- **分类面板**：点击 chip 展开—— genre 分类行（电影/剧集拉 `/genre/{type}/list?language=zh-CN` 全量，综艺/动漫/纪录片为固定 10764/16/99）+ 最热门搜索值双列榜（`/trending/{movie,tv}/day` 或 `/discover?with_genres=X&sort_by=popularity.desc`）；**点不同 genre chip 实时切换榜单**（discover 真实请求，内存缓存）。
- **数据全部运行时真实获取**：右上角请求日志可见每笔 API 耗时；token 默认取仓库 `.env` 只读 token（可覆盖，localStorage 持久化）。

**【网络兼容发现（对真实落地关键）】** 实测当前网络 `api.themoviedb.org` / `image.tmdb.org` 不可达，而 TMDB 官方备用域名 **`api.tmdb.org` 完全可用**（真实数据验证：trending top1「求救信号」🔥112.9、discover 综艺 top1「我独自生活」）。demo 已做双域名自动回退（api.tmdb.org 优先）；海报加载失败优雅隐藏。真实落地时 tmdbService 的 API_HOST 需评估同样的双域名策略。

**【待拍板】** ① 面板展开时 hero **下移**（demo 现状）还是**覆盖** hero；② 热度口径用 popularity 原始值还是归一化（如 🔥1234）；③ 分类热度榜的桶规则（综艺/动漫/纪录片按 genre_ids 归类是否合意，跨类重复计入如何处理）；④ 默认面板常驻（首页加载即显示）还是保持折叠、点击才展开。

## Demo v2：方案 A2 按四点拍板重写（`demo-category-quick-access-a2-2026-09-06.html` 覆盖）

**用户拍板**：① 面板展开 hero 下移；② 热度 = popularity 原始值；③ 综艺/动漫/纪录片也要有自己的分类；④ 多分类命中重复展示即可；⑤ 默认面板首页加载即显示；⑥ 下方内容 3 行、1 行 3 列，首页只显示前 3 分类，右侧显示 2 个内容。

**【旧逻辑】** v1：默认面板为 5 行分类纵列；综艺/动漫/纪录片面板无子分类（单标签）；热榜双列排行 10 条。

**【新逻辑】**
- **默认「首页」面板（加载即显示）**：前 3 个热度分类（1 行 3 列卡：排名 + Σpopularity 热度 + 热度条 + 该分类最热门搜索值海报/标题/🔥）+ **右侧全站最热 2 个内容卡**（backdrop 大图 + 🔥 + ★）。
- **分类面板**：子分类行 + 主区 **3 行 × 3 列 = 9 个内容卡**（海报卡 + 排名角标 + 🔥popularity + ★）+ **右侧该榜 top2 内容卡**。
- **综艺/动漫/纪录片真实交叉子分类**（discover `with_genres=主,副` AND，全部实测有数据）：综艺 全部/家庭亲子(10764,10751 总794)/谈话(10764,10767 总871)/新闻(10764,10763 总101)；动漫 全部/动作冒险/喜剧(4902)/科幻&奇幻/儿童；纪录片 全部/历史/犯罪/音乐(12626)/战争；排行榜子分类 = 日榜/周榜/电影榜/剧集榜四种 trending 窗口。
- 多分类命中重复计入（桶规则天然如此）；hero 下移 = 面板在文档流中推挤。

## Demo v2.1：用户两条反馈修订（同名 demo 覆盖）

① **首页默认面板**：每个分类卡右侧从「top1 单条」改为**显示 3 条热门数据**（排名 + 28×40 小海报 + 标题 + 🔥popularity，取该分类 trending 聚合 top3）。
② **分类面板内容卡**：原 2:3 大竖版海报（列宽 ≈200px、图高 ≈300px）确实过大——改为**紧凑横排卡**：排名内联 + 海报 54×80（w185）+ 标题/年份/★ + 右侧 🔥 热度列，3 行 × 3 列布局不变；右侧 2 个内容卡 backdrop 从 w780 降为 w300、min-height 150→128。

## Demo v2.2：去掉面板右侧栏（用户选择方案 1）

用户问「每个 chip 下方最右侧为何有 2 个 card」——源头是上一轮拍板消息里「右侧显示2个内容」一句，被我理解为面板右侧固定 2 张大图内容卡。经确认该理解非用户本意，用户选择**去掉右侧栏**：面板主区（首页 3 分类卡 / 分类面板 3×3 内容卡）占满整行；sideCard 函数、`.qa2__body/.qa2__side/.sidecard-*` 样式与全部引用删除；catgrid/hotgrid 直接作为面板主体。跳转说明同步更正。

## Demo v2.3：修复「不是 3 行 × 3 列」

根因：v2.1 加的 `@media (width < 1500px)` 降级规则——用户浏览器窗口不足 1500px 时分类面板被降为 2 列、首页 catcard 被降为竖排 1 列。demo 本就只针对 >1280 桌面，整条降级规则删除：catgrid 恒 1 行 3 列、hotgrid 恒 3 行 × 3 列。

---

## 方案 A2 落地：CategoryQuickAccess >1280「频道导航 + 热门分类面板」

**【旧逻辑】** >1280 桌面为 7 个彩色渐变圆卡（扁平化置于 hero 上方）：彩色盘与 B 站风 hero 气质冲突、仅消费 3 个 TMDB genre、占高 ~90px。

**【新逻辑】** 按 [实施方案](design-docs/2026-09-06-category-quick-access-a2-实施方案.md)（同日 demo v2.3 拍板形态）落地，≤1280/TV/app 圆卡分支零改动：
- **导航条**：首页/电影/剧集/综艺/动漫/纪录片/排行榜（lucide 图标 + `--icon-md`）+「全部分类 →」（跳 `/browse?category=all`）；chip 带 🔥 热度徽标（Flame 图标 + trending 聚合计数）；chip 点击不跳转、展开面板。
- **默认「分类热度榜」加载即显示**：前 3 分类卡（1 行 3 列）= 排名 + Σpopularity + 热度条 + 各分类 top3 热门搜索值（小海报 + 标题 + Flame 热度）；数据复用 store trending（**首屏零新增请求**）。
- **分类面板**：子分类 chips + 3 行 × 3 列 9 个紧凑内容卡（海报 54×80 w185）；电影/剧集子分类 = 全量 genre list（懒加载）；综艺/动漫/纪录片 = 真实交叉组合（`discoverCategory`，`vote_count.gte` 放宽 50→10）；排行榜 = 日/周/电影/剧集榜。
- **跳转**：hotcard/catcard 行 → `/detail/{composite-id}`；多分类命中重复计入（拍板）。

**涉及文件**：`CategoryQuickAccess.tsx`（双分支重写）、`CategoryQuickAccess.css`（末尾追加 `@media (width>1280px)` 块，旧规则逐字节未动）、新增 `categoryPanelData.ts`（分类定义/桶聚合/面板缓存）、`tmdbService.ts`（新增 `discoverCategory`）、`useTMDBStore.ts`（3 个纯 mapper 加 export）、`tmdb-mock-data.ts`（trending genre_ids 按索引分桶）、`home.spec.ts`（新增 1.3c 六用例 HOME-070~075）。

**构建/测试**：`npm run build` ✓；home 48（+6）+ cross-page 17 = **65 passed**；`run-tests.ps1` 映射 `1\.3` 已自动覆盖 1.3c。

## A2 微调：面板高度锁定 + catcard 海报调大 + 面板去装饰（用户反馈）

① **catcard 封面调大**：`cqa-catcard__poster` 28×40 → **54×84**（与 hotcard 海报同宽体系），catcard 改纵向 flex + list `space-between` 均匀分布 3 行。
② **切换 chip 高度锁定**：宽屏块注入 `--cqa-row-h / --cqa-sub-h / --cqa-grid-h` 变量——hotgrid `grid-template-rows: repeat(3, row-h)` 定高，catgrid 高度 = hotgrid 高度 + 子分类行高度（默认面板无子分类行，由 catcard 吸收），两面板总高恒等，切换零跳动；head/subgenres 同步定高（行高 1.2/1.5 固定）。
③ **`.cqa-panel` 去装饰**：删除 background/border/border-radius/box-shadow，面板内容直接落在页面底色上（卡片自带 surface+边框）。

## A2 微调二：catcard 海报回调 48×72 + hotcard 改横版封面（用户反馈）

① **catcard 海报 54×84 → 48×72**：84 高在 1280~1360 视口下超出父容器（catgrid 定高公式在小区间给列表的空间不足 3×84），回调后全视口有富余；list/row 加 `min-height: 0` 防溢出兜底。高度锁定不受影响（catgrid 总高仍由 calc 变量推出）。
② **hotcard 封面竖改横**：海报 w185 竖图 → **backdrop w300 横图（96×54，16:9）**，无横版时回退海报；行高基准变量 `--cqa-row-h` 同步改为 `54px + 2×space-xs`，catgrid 高度公式自动跟随，两面板等高锁定保持。

## A2 微调三：横版封面调大 + catcard 横版化 + 无横版占位约定（用户反馈）

① **hotcard 横版调大**：96×54 → **120×68**，行高变量 `--cqa-row-h` → `68px + 2×space-xs`，面板整体高度随之增加（拍板③）。
② **catcard 行改横版**：竖版海报 w92 → **backdrop w300 横图 96×54**。
③ **无横版占位约定（不回退竖版）**：catcard/hotcard 封面仅在 `backdropPath` 存在时渲染 LazyImage，否则渲染 `--empty` + `thumbnail-skeleton-bg` 骨架占位 span（与 HeroSideCard 空态处理方式一致）。
④ **HeroBili 侧卡同约定**：`itemCardCoverUrl` 原本无横版时回退竖版海报（检查结果），现改为返回空串 → 走既有 `--empty` 骨架占位分支；`posterUrl` 仍用于收藏封面字段，不受影响。构建 ✓；1.3c + 1.2（HeroBili 段）9 passed。

## A2 修复：切换 chip 高度变化的三个残留根因（用户反馈二次修复）

上次锁定的盲区：① **子分类 chips 折行**——电影/剧集 20 个 genre chip 折成多行、综艺/排行榜仅 1 行，行数差异即高度差异 → 改 `flex-wrap: nowrap + overflow-x auto`（单行横向滚动，滚动条隐藏）；② **加载中间态塌陷**——点 chip 先渲染「正在加载…」短文本再换网格 → `.cqa-panel__loading` 定高 `--cqa-grid-h` 且子分类未就绪时保留 subgenres 行容器（行内文案）；③ **head 长副标题窄视口折行** → `flex-wrap: nowrap` + 副标题 ellipsis。兜底：`.cqa-panel` 增加 `min-height = head + 子分类行 + 网格` 的 calc——任何状态都不低于恒定值，面板总高对所有 chip 与所有加载态严格一致。

## A2 修复：面板数据未按热度倒序（用户反馈）

根因：「全部」（电影/剧集）与排行榜面板数据源为 `/trending` 接口，返回的是 TMDB「趋势序」而非 popularity 降序，热度数字看起来像随机排列。修复：`fetchCategoryPanel` 在缓存前统一 `[...items].sort(popularity desc)` 再截取 9 条——所有面板（trending/discover 混合来源）展示顺序恒为热度倒序，与 🔥 数值严格一致；分类热度榜 top3 本就有序，不受影响。

## A2 微调四：导航徽标改 Σpopularity 口径（用户拍板 A）

chip 热度徽标从「趋势榜命中条目数」改为与默认面板分类卡**同口径的 Σpopularity**，≥1000 以 **k** 单位缩写（1 位小数、去尾 .0，如 2.4k；不足 1000 取整）。`heatCount` → `heatSum`，新增 `formatHeatSum`；仍复用同一份 /trending/all/day 聚合，零额外请求。

## Demo：分类导航 × HeroBili 融合方案（用户反馈「两模块强行拼接」，待拍板）

违和三根因：① chrome（文字导航+白卡面板）与沉浸式 hero 两套模块语言背靠背；② 左右边界不对齐（page-padding vs hero 内 grid）；③ 热度面板 delay hero 主角。两候选：**甲（推荐）**导航带并入 hero 卡内部顶端 + 面板 overlay 覆盖 banner（hero 恒高、零推挤；与「默认面板加载即显示」拍板冲突，可把热度榜下移为 hero 下方常规内容行保住该结论）；**乙** 现状对照。

## 融合方案甲落地：导航带并入 HeroBili 卡 + overlay 面板 + 热度榜常驻行（拍板实施）

**【旧逻辑】** 分类导航/面板/hero 三块独立堆叠（「强行拼接」违和），面板推挤文档流。

**【新逻辑】**
- **HeroBili 成为一个整体卡**（surface+边框+圆角+投影）：首行 = 频道导航带（`CategoryQuickAccessWide`，`.hero-bili__grid` 包住 banner/右栏）。
- **overlay 面板**：点击 chip 在导航带下缘展开（`position: absolute` 锚定 hero 卡，`--cqa-nav-h` 顶偏移），覆盖 banner 不推挤；再点同 chip / 点击外部（document mousedown 判定）/ Esc 收起；高度锁定公式保留。
- **hero 下方常驻「分类热度榜」行**（`CategoryHeatRow`，加载即显示、与 TMDBMovieRow 同层级）：3 张 catcard，点卡 `open(key)` 打开对应 overlay 并平滑回顶。
- **「首页」chip** = overlay 内的热度榜面板（与其他 chip 同一位置、只差内容）。
- 状态经 `useCategoryOverlayStore`（zustand）共享导航带与常驻行；≤1280/TV 圆卡分支零改动。

**涉及文件**：`CategoryQuickAccess.tsx`（三分支重构）、`CategoryQuickAccess.css`（宽屏块改 overlay/热度行）、`HeroBili.tsx`（卡内嵌导航带 + __grid 包裹）、`HeroBili.css`（hero 卡化）、`Home/index.tsx`（去掉 hero 上方旧块、内容区加热度榜行）、`home.spec.ts`（1.3c 重写 7 用例）。

**构建/测试**：`npm run build` ✓；home 47 + cross-page 17 + regression-detail 21 = **85 passed**。

## A2 交互收敛：方案 A + 方案 1（用户拍板）

- **方案 A**：「首页」chip 不再弹出热度榜面板（消除 overlay 与常驻行的同屏重复）——点击=收起一切面板（面板未开时无操作），激活态=无面板打开时；overlay 只服务 6 个分类 chip。
- **方案 1**：热度榜常驻行的分类卡点击改为**跳转 `/browse?category=xxx`**（与移动端圆卡、全部分类同语义，替代原「打开远处 overlay」的断裂交互）；条目行仍跳详情。
- 最终交互表：分类 chip=overlay；首页 chip=收起；热度榜分类卡=跳 browse；条目=跳详情；全部分类=/browse?category=all。
- `useCategoryOverlayStore` 移除 open；测试：HOME-076 重写 + 新增 HOME-077，home 48 + cross-page 17 全绿。

## A2 微调五：继续观看行上移至分类热度榜之前（用户拍板）

个人内容（继续观看，全页转化价值最高、行业惯例贴顶）优先于探索型内容（分类热度榜，日更榜单不惧靠后）。`Home/index.tsx` 内容区两块顺序互换，叙事流：hero → 继续观看 → 分类热度榜 → TMDB 各类榜单。

## A2 微调六：热度榜行间距对齐模块体系 + 换一换内嵌右栏（用户反馈）

① `.cqa-heat-row` 补 `margin-bottom: var(--space-lg)`——与 TMDBMovieRow 模块间距体系（模块间 = --space-lg）一致，上下间隙不再忽大忽小。
② `hero-bili__shuffle` 原绝对定位在右栏外侧（`left: 100% + gap`），hero 卡化后落入卡片 padding 区、贴边难看——改为 `.hero-bili__right` 内部 grid 占位列（`minmax(0,1fr) auto`，垂直居中），按钮回到卡内容区，不再贴 `hero-bili__grid` 边缘。

## A2 微调七：热度榜行上下 margin 改为 var(--space-sm)（用户拍板）

## A2 微调八：换一换按钮与第一行 card 顶对齐（用户拍板）

`.hero-bili__right` grid `align-items: center → start`。

## A2 微调九：宽屏分类面板四项整改（用户确认全部落地）

**四项改动**（对照 demo：`demos/demo-cqa-tune-2026-09-06.html`）：

1. **副标题去术语 + 口径 tooltip**：`cqa-panel__sub` / `cqa-heat-row__sub` 由「热度 = TMDB popularity（实时）/ Σpopularity」等开发者术语改为用户视角（「按热度排序 · 点卡片看详情」「今日各分类最热 · 点分类卡进入」），并删掉不准确的「实时」承诺；口径说明保留为 **Info 图标悬停 tooltip**（新增局部 `InfoTip`，radix Tooltip，内容 Portal + `pointer-events:none`——不拦截 mousedown，避免误触发 overlay「点击外部收起」）。
2. **切子分类加载过渡**：abort 竞态保护保留（last-write-wins 正确）；呈现由「整网格替换为『正在获取…』文案」改为**保留旧网格 + 降沉 0.45 + 小电视「更新中…」徽标**原位替换；仅**换分类**时清空旧内容走首载小电视文案（`prevCatKeyRef` 区分）。
3. **catcard 封面 96×54 → 120×68**：与 overlay hotcard 同规格，消除双规格不一致与封面挤压；顺带删除 `.cqa-catgrid` 死代码 height 规则（引用 `.cqa-overlay` 上的 `--cqa-*` 变量，热度榜场景取不到值，height 实际一直是 auto）。
4. **配色回归站内主题**：bili 粉在该组件是全站孤岛（grep 仅此处使用），且 09-05 demo 本身选中态就是黑底白字——回归：导航 item 改 pill chip、选中态 `--color-tab-active-bg/text`（黑底白字，暗色自动反转）、hover 中性；排名/图标/标题全中性灰白；**热度值/热度条/Flame 图标 = 唯一语义强调色，改暖橙 `--color-warning`**（与「热」语义一致）。卡片 hover 边框 `--color-border-strong`，条目行 hover 加 `--color-surface-hover` 背景。

**涉及文件**：`CategoryQuickAccess.tsx`（InfoTip + 加载状态机 + 副标题）、`CategoryQuickAccess.css`（宽屏块配色/尺寸/加载态）、`home.spec.ts`（HOME-077 改点卡头——封面加大后卡片几何中心落在条目行、行语义=跳详情；新增 HOME-079 tooltip / HOME-080 保留旧网格）。

**构建/测试**：`npm run build` ✓；home 1.3c 段 **10 passed**（含 2 新用例）。

## A2 微调十：微调九三项修正（用户反馈）

1. **chip pill 收一号**：`--comp-tab-height` → `calc(… - var(--space-sm))`、横向 padding `--space-md → --space-sm`，选中态黑底方框不再撑满导航行高。
2. **选中 chip hover 底色失效**：`.cqa-nav__item:hover` 的 `surface-hover` 同特异性后写覆盖了 `--on` 黑底——`--on` 与 `--on:hover` 合并声明保持黑底白字。
3. **「更新中」小电视看不清（根因）**：降沉 `opacity:.45` 写在 `.cqa-panel__grid` 包裹层上，徽标是子元素被一起压暗（opacity 不可被子级抵消）——改为仅 `.cqa-hotgrid` 降沉；徽标改为实底胶囊（surface 背景 + 边框 + `--shadow-md`）悬浮居中，行内排布。demo 同步修正。

**构建/测试**：`npm run build` ✓；home 1.3c 段 **10 passed**。

## A2 微调十一：面板客户端分页（20 条 → 9 条/页）+ 末页「查看更多」

**问题**：overlay 面板原本只展示 9 条（slice(0,9)），接口单页实返回 20 条，11 条数据被白白丢弃；用户想看更多只能跳 browse，路径断裂。

**旧逻辑**：`fetchCategoryPanel` 截取 top9，面板无翻页能力。
**新逻辑**：
- `categoryPanelData.ts`：截取上限放宽为 `PANEL_MAX_ITEMS = 20`（TMDB 单页上限），缓存与排序逻辑不变。
- `CategoryQuickAccess.tsx`：新增 `panelPage` 状态，客户端分页 9 条/页（3×3 网格与 overlay 高度锁定不变）；面板右下角新增 `cqa-panel__pager`（上一页/下一页 + 「n / N」指示），排名按页偏移（`rankOffset`，第 2 页从 10 起）；切分类/子分类时页码重置为 1；末页出现「查看更多」按钮（`goBrowseMore`：携分类 + 当前子分类 genreIds 跳 browse 对应筛选，overlay 先收起）。
- 修复合入：`goBrowseMore` 补 `activeCat.key === 'home'` 守卫——`buildBrowseUrl` 的 `CategoryKey` 不含 `'home'`，原实现 tsc 编译报 TS2345（面板只在非 home 分类下渲染，守卫同时完成类型收窄）。

**涉及文件**：`CategoryQuickAccess.tsx`、`categoryPanelData.ts`、`home.spec.ts`（HOME-081 分页/查看更多用例）。

**构建/测试**：`npm run build` ✓；home 1.3c 段 **12 passed**（含 HOME-081）。

## A2 微调十二：面板竖向排列 + 高度公式补 pager（承接微调十一）

1. **竖向排列**：`.cqa-hotgrid` 加 `grid-auto-flow: column` → 1/2/3 在第 1 列自上而下，榜单「先纵后横」阅读动线；末页不满时（如 2 条）填充第 1 列，右下角查看更多平衡视觉。
2. overlay 高度公式追加 `--cqa-pager-h`（切页 overlay 总高恒定不变）。
3. 补充记录：`CategoryQuickAccess.css` pager 样式、`home.spec.ts` 新增 HOME-082（竖排 boundingBox 几何断言：1/2 同列 y 递增、4 在第 2 列）、demo `demos/demo-cqa-pager-2026-09-06.html`（已登记索引）。

**构建/测试**：`npm run build` ✓；home 1.3c 段 **12 passed**。

## A2 收藏按钮统一：全 card 红底已收藏态 + 去抖动 + hover 动效（用户需求）

1. **已收藏背景色统一为 `var(--color-danger)`（#ef4444 实底）+ 白色实心心形**：`VideoCard`（`.video-card-fav-btn.active`，原橙图标浅红底+橙圈）、`IPTVChannelCard`（`.iptv-card-favorite.active`，同旧橙系）、`HeroBili` side card（`.hero-side-card__fav.is-on`，原黑底红心）三处统一；TSX 图标改 `fill="currentColor"`，颜色全部交给 CSS（未收藏 = 白描边、已收藏 = 白实心）。
2. **点击收藏 card 不再抖动**：移除两卡片的 `animate-pop-bounce` 弹跳（scale 1→1.35 的 keyframe 即用户感知的抖动）及配套 `isAnimating` 状态/定时器（VideoCard / IPTVChannelCard）。
3. **hover 动效统一**：三处按钮 hover = 底色加深 `rgba(0,0,0,0.7)` + `scale(1.1)`；已收藏 hover 保持红底（`.active:hover`/`.is-on:hover` 锁 `--color-danger`），仅放大。HeroBili 补 transition transform。
4. `--color-favorite-active*` token 保留（Detail/Player 页收藏按钮仍在使用，不在本次 card 范围）。

**涉及文件**：`VideoCard.tsx/.css`、`IPTVChannelCard.tsx/.css`、`HeroBili.css`。

**构建/测试**：`npm run build` ✓；home + collections + history + iptv + iptv-player **86 passed / 2 failed（HIS-062、HIS-051，git stash 基线验证同样失败，属既有问题非本次回归）**。

## A2 收藏按钮定稿修正（用户反馈，承接上一条）

1. **图标收一号**：HeroBili side card 图标 `size="sm" → "xs"`，按钮改为与 VideoCard 完全一致的 `--icon-lg` 圆形 + `padding: 0`（原 padding+sm 档偏大）。
2. **已收藏回归「外圈不变、心形变红」**：撤销上一条的红底方案——三处 `.active`/`.is-on` 改为仅 `color: var(--color-danger)`，底色保持半透明黑圈；hover 加深+放大对两态同样生效。
3. **取消收藏后按钮不消失（根因）**：点击会在按钮上留下焦点，卡片 `:focus-within` 持续命中 → `hover-visible` 常显。修复：`handleFavorite` 鼠标点击（`e.detail > 0`）后 `blur()`；键盘 Enter（detail=0）不 blur，键盘可达性保留。VideoCard/IPTV/HeroBili 三处统一。

**构建/测试**：`npm run build` ✓；home + collections + iptv + iptv-player **77 passed**。

## A2 v2 热度榜页 /chart 详细设计 + Demo（待拍板，未落代码）

**背景**：v1 复用 browse 方案讨论后，用户要求展开 v2（独立榜单页）并配合真实接口出 demo。

**Demo**：`demos/demo-chart-page-2026-09-06.html`（80KB 自包含，已登记 Demo 索引）。数据 = 2026-09-06 真实 TMDB 快照（`/discover/{movie,tv}?sort_by=popularity.desc` 6 分类 × 2 页 + `/trending/all/{day,week}`，经 api.tmdb.org 备用域名拉取）。

**设计要点**（详见 demo 顶部说明卡）：
1. 路由 `/chart?category=…`（Keep-Alive 成员，URL 可分享）；入口：热度榜行标题 → /chart，**三张分类卡整卡 v2 起改跳 /chart**（v1 为跳 browse），面板「查看更多」保留跳 browse。
2. 三层分工不重复：/chart=沉浸看完整榜单；browse=筛选探索；overlay 面板=hero 内快速预览。
3. 榜单行 = 排名 + 160×90 横版封面 + 标题/年份/类型徽标/一行简介 + 热度值(暖橙)+评分；top3 排名暖橙强调；灰白结构 + 暖橙唯一语义强调色。
4. 无缝滚动（IntersectionObserver 哨兵 + 每页 20、排名跨页连续 + 到底提示）；趋势榜 tab 今日/本周切换。
5. **接口口径两个坑（实现时必处理）**：① trending 趋势序不可按热度控序（综艺/动漫/纪录片无 trending 端点）→ 榜单统一 discover 口径；② discover 分页拉取间 popularity 漂移会乱序/重复——demo 实测 movie 40 条抓到 1 条跨页重复 + 边界乱序，demo 快照已按 popularity 重排 + 按 id 去重，线上实现需在「追加后合并排序去重」。

**验证**：agent-browser 实测截图（桌面列表/无缝滚动 39 行排名连续/趋势榜分段切换/移动 400px/暗色），修 2 处 demo bug（徽标模板字面量丢失花括号、徽标中文换行）。

## A2 顶栏融合落地：cqa-nav 并入 StickyHeader + hover 面板语义（demo 拍板）

1. **结构拆分**：`CategoryQuickAccessWide`（导航带+面板一体）拆为 `CategoryQuickAccessNav`（渲染进 StickyHeader 中央、搜索框左侧）与 `CategoryQuickAccessPanel`（HeroBili 卡顶渲染，`top: 0` 贴 header 下缘 = mega-menu）；store 去 toggle 改 hover 语义（`open/close/scheduleClose/cancelClose`，120ms 延迟缓冲穿越 nav↔panel 空隙）。
2. **chip 热度徽标移除**，Σ 热度值移到面板头「今日最热」右侧（`.cqa-panel__heat` 暖橙 Flame，排行榜无 Σ 口径不显示）；chip 间距统一 `--space-xs` 档（demo 拍板 4px）；导航带卡片化背景/边框/定位全部移除。
3. **面板交互**：hover chip 展开；移出导航/面板延迟收起；Esc / 点击外部收起（nav 与 panel 分处 header/hero，`closest('.cqa-nav,.cqa-overlay')` 判定内侧）；**页面滚动立即收起**（监听 AppLayout CustomScrollbar）；滚离 hero 后悬停 chip 先回顶再展开（800ms 豁免期防自己触发的滚动关掉面板）。
4. **跨页一致**：chips 在宽屏桌面所有页面常驻；其他页面无面板载体 → chips 为纯导航——点分类直达 `/browse?category=…`（携子分类 genre）、点「首页」回首页；路由切离首页自动收起面板。
5. **搜索框大屏封顶**：`SearchBox.css` ≥1281px header 搜索框 `min(token, 480px×ui-scale)`，不再随视口涨到 627~800px；chips 存在时搜索框 `margin-left:auto` 靠右。
6. **踩坑**：`.cqa-nav` 若可收缩（min-width:0）chips 会溢出到搜索框下方拦截点击——必须 `flex: 0 0 auto`；分页条上间距 `--space-xs → --space-sm`（用户反馈贴太近）。

**涉及文件**：`CategoryQuickAccess.tsx/.css/index.ts`、`StickyHeader.tsx/.css`、`SearchBox.css`、`HeroBili.tsx`、`home.spec.ts`（1.3c 全面改 hover 语义 + 新增 HOME-084 滚动收起 / HOME-085 跨页导航）。

**构建/测试**：`npm run build` ✓；home 1.3c **15 passed**；cross-page + browse + settings **66 passed**。

## v2 热度榜页用户反馈三调（2 列横向排序 / 去 max-width / 加载文本提示）

**用户反馈**（实施后首轮目检）：
1. `.chart-card` 不应设 max-width——全局已有内容宽度约束。
2. 榜单一行 2 列、排名横向排序（1、2 同行），非单列纵排。
3. 滑动到底调接口时只有骨架占位图，缺文本提示。

**修改**（`Chart.css` + `Chart/index.tsx`）：
1. 删除 `.chart-card` 的 `max-width: 960px`（跟随全局）。
2. `.chart-list` 改 `grid-template-columns: repeat(2, minmax(0,1fr))`（grid 默认 row flow = 横向排序）；到底提示/哨兵/加载块 `grid-column: 1/-1` 跨列；移动端 ≤639 与 app 恒单列。
3. 加载态改为 `.chart-list__loading`（2 个带边框骨架行 + 「正在加载更多…」文本，aria-live=polite）。
4. **顺带修正首屏热度序**：TMDB `sort_by=popularity.desc` 与返回 popularity 字段有出入（真实接口页内即乱序，如 406.5 排在 477.1 前）→ 首屏也走 mergePage 去重 + 重排，保证热度值单调（截图验证 851.9→482.6→477.1→406.5…）。

**验证**：`npm run build` ✓；chart.spec 5 passed；真实接口截图目检（2 列/单调热度序/品牌兜底 MonitorPlay+kinoTV 生效）。

## A2 顶栏融合微调：chip 文本/面板卡间距/标题跑马灯（用户反馈）

1. **chip 文本对齐右侧导航样式**：`.cqa-nav__item` 补 `font-weight: 600`、激活态 700（与 `.sticky-header__nav-item(--active)` 一致）。
2. **面板 card 间距拉大**：hotgrid gap `sm/md → md/lg`（行 8.5→11.8、列 11.8→16.3），3 列 1fr 下 card 随之稍窄更透气；`--cqa-grid-h` 公式同步（行 gap 2×space-md），overlay 高度锁定不变。
3. **标题悬浮跑马灯**：新增 `HotCardTitle`（ResizeObserver 溢出检测，`--marquee-x` = 溢出宽度注入）；非溢出不滚动（保留 ellipsis），悬浮 card 时 `.is-overflow` 触发 `cqaHotMarquee`（去程→停留→滚到尾→停留→回程循环）；reduced-motion 关闭。

**构建/测试**：`npm run build` ✓；home 1.3c **15 passed**。

## v2 热度榜页用户反馈四调（返回顶部按钮 + tooltip 小手光标）

**用户反馈**：① 页面缺少返回顶部按钮；② `.chart-info-tip` 悬浮应显示小手（原 cursor: help）。

**修改**：
1. `Chart/index.tsx` 复用 `BackToTopButton`（@/components/common，与 Browse/History 同款玻璃感按钮，Portal 挂 body、滚动 >280px 显示、平滑回顶、路径切换自动重置）。
2. `.chart-info-tip` `cursor: help → pointer`。

**验证**：`npm run build` ✓；chart.spec 5 passed。

## 设置页大屏布局方案 Demo（待拍板，未落地）
- 日期：2026-09-06
- 问题：设置页桌面端（≥768px）大屏下排列分散——标题在左、6 个胶囊 tab 挤在右上、内容整卡铺满被拉长
- 旧逻辑：`.settings-desktop-topbar`（Settings.css:412）顶部横排：左标题 + 右侧 `margin-left:auto` 胶囊 TabBar；内容区无限宽
- 新逻辑：产出 4 方案可交互对比 Demo（未改代码）：A 现状基线 / B 左侧竖排导航（用户想法，+内容限宽 860px，★推荐）/ C 窄图标栏 72px / D 保持顶栏 + 整卡限宽居中；demo 内 tab 可点、容器宽度 900–1760px 可拖
- 涉及文件：无（仅 demo）
- 关联 Demo：demos/demo-settings-desktop-layout-2026-09-06.html
- 构建：不涉及（无代码改动）

## 历史网格单列塌缩修复 + 收藏/Browse 网格回归 --card-cols 等分（与首页对齐）

**问题**：① 桌面 ≥1280 历史页横版记录卡每行只显示 1 张（应 4 列 290px）；② 收藏页/Browse 的 `.video-card-grid` ≥1280 走 auto-fill 290px 固定轨道（卡宽 290px），与首页横滚行（--card-cols 等分，1440 下 7 列 ≈180px）尺寸脱节。

**旧逻辑**：
- `.history-group-body { max-width: 2200px; margin-inline: auto }`（History.css）——`.history-group` 是 flex column，flexbox 规范下子项设 auto margin 即放弃 stretch 退化为 fit-content；auto-fill 网格的 max-content 只算一轨 → 容器塌缩 290px 单列，再被 auto margin 居中，视觉上像刻意设计实为 bug。
- `.video-card-grid` ≥1280 被 `repeat(auto-fill, var(--card-size))` + 居中接管（阶段 C「卡片封顶」），与首页 `--card-cols`（3/4/5/7/5/5 梯度）体系割裂。

**新逻辑**：
- `.history-group-body` 显式 `width: 100%`：恢复撑满父级（1208px），auto-fill 正常算出 4 轨；2200 上限内的居中不受影响。历史页布局设计（auto-fill 290px）保持不变。
- `.video-card-grid` 删除 ≥1280 auto-fill 断点覆盖，恒走 `repeat(var(--card-cols), minmax(0,1fr))` 等分——收藏页/Browse 与首页行同梯度同卡宽；>1280 间距本就由 index.css「卡片间距统一」块覆盖为 `--hero-side-gap`，与首页行一致。
- `.skeleton-grid`（加载骨架）同步删除 ≥1280 auto-fill 覆盖，保持与真实网格同构（骨架→内容不跳变）。

**涉及文件**：`src/pages/History/History.css`、`src/components/VideoCard/VideoCard.css`、`src/components/VideoCard/SkeletonCard.css`

**验证**：`npm run build` ✓；浏览器实测（1440px）：历史页 `.history-group-body` 290→1208px、`.history-grid` computed `290px×4` 列；收藏页 `.video-card-grid` computed 7 列等分（176.6px×7，卡宽 177px）与首页行同公式；E2E spec 无列数/卡宽断言（grep 核实），无测试需同步。

## A2 顶栏融合修正：browse 页 chips 双向关联（用户反馈）

问题：browse 页点 chips 只改 URL，chip 无选中态、与页面状态脱钩（数据层其实有随 URL refetch，缺的是状态高亮同步）。
修复：Nav 读取 `location.pathname/search`——browse 场景 `browseCategory` 派生自 URL `category` 参数，chip 选中态 = `browseCategory === key`（FilterBar 改分类 → URL 变 → chip 高亮跟随，双向）；`all` 时高亮「全部分类」；重复点击同分类 URL 完全一致则跳过 navigate（不压历史栈）。

**构建/测试**：`npm run build` ✓；home 1.3c **16 passed**（新增 HOME-086）。

## 收藏/历史页筛选面板布局 Demo（待拍板，未落地）
- 日期：2026-09-06
- 问题：用户提案「取消『更多筛选』按钮，RecordFilterPanel（animate-fade-in）常驻显示」；顺带征集其它 UI 调整点（card 不改）
- 评估：B 方案（全常驻）可行、实现最省，但①顶栏多占 ~90px（历史页算珠时间轴下更挤）②排序 6 chips 低频全铺开偏吵③IPTV tab 下状态筛选无效仍显示被放大
- 产出：5 方案对比 Demo——A 现状基线 / B 面板常驻（提案原样）/ C ★推荐：状态 chips 常驻并入顶栏 + 排序收纳胶囊弹层 / D 全行化 / E 附加：清空操作降级「⋯」菜单 + IPTV tab 隐藏无效 chips
- 涉及文件：无（仅 demo）
- 关联 Demo：demos/demo-record-filter-layout-2026-09-06.html
- 构建：不涉及（无代码改动）

## A2 顶栏融合再修正：非首页 chips 与页面完全解耦（用户澄清）

上一条「双向关联」理解反了。澄清后的语义：chips 在其他页面**仅视觉常驻**（导航栏跨页一致），与当前页面**零关联**——
点分类 chip 不改 URL、不导航、无选中态；唯一例外「首页」chip 仍为回首页入口。撤回 browseCategory 派生/高亮跟随/防重压栈逻辑。
测试：HOME-085 改断言「点分类 URL 不变、无高亮」；HOME-086 重写为「browse 页点 chip URL 不变、零关联」。

**构建/测试**：`npm run build` ✓；home 1.3c **16 passed**。

## A2 顶栏融合定稿：面板全局挂载，chips 全端统一 hover 语义（用户拍板）

最终语义（所有页面一致）：hover chip → 开面板；点 chip → 开面板；「首页」chip → 收起面板（不导航）；移出导航/面板延迟收起；页面滚动 / Esc / 点击外部 → 收起。

1. **面板脱离 hero**：`CategoryQuickAccessPanel` 改由 **AppLayout 全局挂载**（StickyHeader 之后），自门控（宽屏 >1280 非 TV 且非移动布局，否则 null）；CSS 改 `position: fixed; top: var(--header-height); left/right:0; margin-inline:auto; max-width: content-max-width; z-index:49`（header z-50 之下）——跨页 mega-menu，不再依赖首页 hero 载体。**踩坑**：挂载点迁移后忘改 CSS，absolute 面板在非首页被 sticky header 压住拦截点击。
2. **Nav 简化**：删 isHome 分支/回顶逻辑/豁免期（fixed 面板不随滚动走，回顶无意义），滚动收起全页生效。
3. 回归：home 1.3c **16 passed**（HOME-085/086 重写为跨页开面板断言）+ cross-page/browse 41 passed；tsc 除并行会话 RecordShell 在途 WIP 外零错误。

## 收藏/历史页顶栏方案 C+E 落地：状态 chips 常驻 + 排序弹层 + 清空降级 + IPTV 隐藏无效筛选

**拍板**：demo-record-filter-layout-2026-09-06.html 方案 C（状态常驻 + 排序收纳）+ 方案 E（清空降级 + IPTV 隐藏）。

**问题**：「更多筛选」折叠按钮让状态筛选/排序可见性为零；「清空收藏/清空历史」危险操作与高频按钮同权重并排；IPTV tab 下状态/排序无效仍显示。

**旧逻辑**：融合 tab 行右侧「更多筛选/清空/批量管理」三按钮；点更多筛选展开 RecordFilterPanel（状态 chips + 排序 chips 两行，桌面约 90px）。

**新逻辑**：
- `RecordShell` 新增 `inlineFilter` / `overflowActions` props：桌面（≥768 非 app）融合 tab 行内嵌「分隔线 + 状态 chips ×N + 排序胶囊弹层」（宽度实测 1440 下总需求 989px / 可用 1296px，一行放下；1024–1279 由 aside flex-wrap 自然换行兜底）；排序弹层点外部/Esc 关闭、选中即收起并更新胶囊文案。
- 「⋯」溢出菜单收纳清空操作（红色危险项 + 确认弹窗保留），与高频按钮物理隔离。
- E-②：IPTV tab 下内嵌筛选条整段隐藏（状态/排序仅作用于影视项）。
- 移动端（≤767 / app 恒移动）零改动：「更多筛选/清空」按钮与 RecordFilterPanel 折叠面板保留（桌面 CSS display:none），溢出菜单与内嵌筛选条桌面专属。
- E2E 同步：COL-031 改写为「内嵌筛选条 + 排序弹层 + 溢出菜单」断言；HIS-061 改写并新增 IPTV tab 隐藏/恢复断言。

**涉及文件**：`src/components/RecordShell/RecordShell.tsx`（SortMenu/OverflowMenu/usePopoverClose + inlineFilter 渲染）、`RecordShell.css`（@import RecordFilterPanel.css 自持 + 内嵌条/弹层/显隐规则）、`src/pages/Collections/index.tsx`、`src/pages/History/index.tsx`（传新 props）、`scripts/collections.spec.ts`、`scripts/history.spec.ts`

**验证**：`npm run build` ✓；浏览器实测（1440 桌面）：两页一行放下（oneLine=true）、排序弹层选/关/文案更新、⋯ 菜单含清空项、IPTV tab 下筛选条隐藏切回恢复；移动 390：更多筛选/清空按钮照旧、内嵌条与溢出菜单隐藏。E2E：collections+history 15 passed；HIS-051/HIS-062 基线也挂（stash 验证与本次无关，记忆库既有记录）。

---

## 2026-09-06 · Detail 推荐网格对齐首页 --card-cols 等分

**问题**：详情页底部「相关推荐 / 你可能还喜欢」竖版 VideoCard 网格在 ≥1280 桌面与首页卡片尺寸脱节——网格走 `repeat(auto-fill, var(--card-size))` 固定 290px 轨道 + 居中，卡片比首页（--card-cols 等分，1440 下约 180px）明显偏大偏疏。

**旧逻辑**：`Detail.css` `.detail-recommend-row` 基础为 `--card-cols` 等分但被「阶段 C」≥1280 覆盖为 auto-fill 固定轨道（`Detail.css:1508` 原）；gap `--space-lg` 也与首页行 `--space-md`（基础）/`--hero-side-gap`（>1280）不一致。

**新逻辑**：删除「阶段 C」≥1280 auto-fill 覆盖，网格恒走 `repeat(var(--card-cols), minmax(0,1fr))` 等分（列数随全站 3/4/5/7/5/5 梯度）；基础 gap 改 `--space-md`，并在 `index.css`「>1280 卡片间距统一」块把 `.detail-recommend-row` 加入 `--hero-side-gap` 组——与首页 TMDBMovieRow / Browse / Collections 完全同款公式。

**涉及文件**：`src/pages/Detail/Detail.css`（删阶段 C 块 + gap 调整）、`src/assets/styles/index.css`（间距统一块追加选择器）

**验证**：`npm run build` ✓。另：detail-hero-poster 偏大系 `--layout-detail-poster-w` token（clamp 277→352px，2/3 比例）所致，为密度校准 v2.2 有意取值，本次未动。

---

## 2026-09-06 · detail-hero-poster 恢复旧尺寸曲线

**问题**：详情页 banner 右侧海报桌面端特别大（1440 视口约 312px 宽、1920 达 337→352px 封顶）。

**旧逻辑**：`variables.css` 段2（≥768）`--layout-detail-poster-w: clamp(277.333px, 237.28813px + 5.21418vw, 352px)`——系 `868f4cb`（2026-09-04 阶段A 三段式 token 重定义）把原本 `clamp(8rem, 2rem + 8vw, 22rem)`（≈128px@768 / 147px@1440 / 186px@1920）整体抬高了 2 倍多。

**新逻辑**：段2 恢复 `868f4cb` 之前的旧曲线，按现行架构表达为 `calc(clamp(128px, 32px + 8vw, 352px) * var(--ui-scale))`（px 等价 rem 旧值 + ui-scale 乘子）；TV 端独立 token（110→160px）与段1（<768，海报 display:none 不可见）均不动。

**涉及文件**：`src/assets/styles/variables.css`（段2 一行）

**验证**：`npm run build` ✓。

## A2 顶栏融合补充：「首页」chip 其他页特殊处理（用户拍板）

「首页」chip 双语义：首页 = 收起面板；其他页 = 收起面板 + 跳转回首页（导航入口）。分类 chips 行为不变（全页面 hover/点 chip 开面板）。
测试：HOME-085 更名并断言 browse/设置页点「首页」chip → URL 回 '/'、面板收起。

**构建/测试**：`npm run build` ✓（tsc 仅剩并行会话 RecordShell/Settings 在途 WIP 报错）；home 1.3c **16 passed**。

---

## 2026-09-06 · 修复 CategoryQuickAccessPanel Rules of Hooks 崩溃

**问题**：宽屏分类面板关闭→hover 打开的下一帧报「Rendered more hooks than during the previous render」白屏，由 ErrorBoundary 兜底重建组件树。

**旧逻辑**：`CategoryQuickAccess.tsx` 中 `goBrowseMore = useCallback(...)` 写在 `if (!activeCat || activeCat.key === 'home') return null;`（原 387 行）之后——面板关闭渲染时该 hook 不执行，打开的下一帧 hook 数从 51 变 52，违反 Rules of Hooks。

**新逻辑**：`goBrowseMore`（连同其依赖的 `currentSub` 派生）上移到组件内所有提前 return 之前；`useWideCategoryPanel` 之后的 hook 调用顺序恒定。

**涉及文件**：`src/components/CategoryQuickAccess/CategoryQuickAccess.tsx`

**验证**：`npm run build` ✓；E2E home+cross-page 73 条中 72 passed，HOME-085 并行跑挂、单跑 passed（时机抖动非回归）。
