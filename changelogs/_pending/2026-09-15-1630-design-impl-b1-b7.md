---
date: 2026-09-15
module: design-system
type: feat
build: pass
files:
  [
    'src/assets/styles/variables.css',
    'src/assets/styles/animations.css',
    'src/assets/styles/index.css',
    'src/assets/styles/skins.css',
    'src/components/Layout/Layout.css',
    'src/components/StickyHeader/StickyHeader.css',
    'src/components/common/AppLoading.css',
    'src/components/common/BackToTopButton.css',
    'src/components/common/OverlayScrollbar.css',
    'src/components/RecordShell/BatchActionBar.css',
    'src/components/SearchBox/SearchBox.css',
    'src/components/SourceManager/SourceManager.css',
    'src/components/StillsLightbox/StillsLightbox.css',
    'src/components/UniversalPlayer/UniversalPlayer.css',
    'src/components/ui/Drawer.css',
    'src/components/ui/PullToRefresh/PullToRefresh.css',
    'src/pages/Detail/Detail.css',
    'src/pages/Detail/components/SourceDetectPill.css',
    'src/pages/Home/Home.css',
    'src/pages/IPTV/IPTVPlayer.css',
    'src/pages/PlayerLab/PlayerLab.css',
    'src/pages/PlayerMobileLab/PlayerMobileLab.css',
    'index.html',
    '.stylelintrc.json',
    'eslint-rules/no-hardcoded-colors.js',
    'scripts/design-audit.mjs',
    'scripts/design-audit-baseline.json',
    'docs/design/BASELINE.md',
    'docs/design/a11y-CHECKLIST.md',
    'package.json',
    '.gitignore',
  ]
demo:
---

## B7 工程化收口（含 B1–B6 三端设计规范落地）

- 日期：2026-09-15
- 背景：承接同日《三端设计规范审查报告》与《实施方案》，用户拍板 5 项待决（正文取 14px / action-blue 走 A / 移除缩放禁令 / 大屏不设可读性下限 / TV 对齐行业标准）后进入实施。本片段记录 **B1–B7 全量落地**，其中 B1–B6 已分批 commit+tag（`design-b1`…`design-b6`），本片段为当日统一留痕。

### 旧逻辑 → 新逻辑（按批次）

| 批 | 旧 | 新 |
| --- | --- | --- |
| B1 | `--color-text-tertiary: #999`（2.85:1）全站 167 处承载信息 → 灰蒙蒙 | `#6b6b6b`（白底 5.33:1）；新增 `--color-text-disabled: #bfbfbf` 作唯一 <4.5:1 档；暗色 `#666→#8c8c8c` |
| B1 | 状态色一档多用：`--color-success` 既作填充又作文字（白底 2.27:1） | 双档：fill 档（`--color-*`）只管填充；新增 `*-text` 档（`#237804/#a05c00/#cf1322/#0958d9`）承载文字/图标。`--color-danger: #ef4444→#cf1322` |
| B1 | 封面角标彩色半透明底 + 白字（浅封面可低至 2.04:1） | 统一 `.badge-chip` 中性深玻璃底 `rgba(0,0,0,.6)` + 白字（最坏 5.74:1），色义改由圆点承载 |
| B2 | 非 TV 焦点环被 `outline:none!important;box-shadow:none!important` 抹掉（违反 WCAG 2.4.7） | 恢复 `outline: 2px solid var(--color-primary)` + `offset: 2px`；>1440px 卡片由 box-shadow 改 outline 口径 |
| B4 | `html { font-size: var(--text-base) }` → 改字号连带全仓 ≈200 处 rem | 新增 `--root-font-size` 解耦（9 组视口实测 diff 为空） |
| B3 | 段2 字号逆视口递减（1920 正文 13 < 平板 14）、2560 档位塌缩 | 小四档恒定 11/12/13/14；大四档段2「768 连续 → 1440 命中 16/18/22/28 → 冻结」；相邻档差 ≥1px |
| B5 | TV 正文 15px、角标 11px（10-foot 实践下限 ≥18sp） | TV 8 档改 px 基准 `clamp(A,(A/19.2)vw,2A)`：1080p 正文 20 / 4K 40；一级栏 `136px→208px`(TV) 配套放开 `max-width 44%→52%` |
| B6 | `--comp-btn-min-height` 移动端 34.66px；tab 28px；viewport 带 `user-scalable=no` | 44px / 36px；viewport 改 `viewport-fit=cover`，移除缩放禁令（WCAG 1.4.4） |
| B7 | `.stylelintrc.json` 存在两个同名 `declaration-property-value-disallowed-list` → 后者覆盖前者，`100vw/100vh` 禁令**实际失效** | 合并为单一数组，修复后暴露 22 处违规并全部清理（`Detail.css` 全出血场景显式 `stylelint-disable-next-line` 豁免） |
| B7 | z-index 全仓 154 处裸数字（1…10000） | 新增 `--z-*` 12 档阶梯；迁移 29 处页级浮层站点（数值不变、零渲染变化） |
| B7 | `prefers-reduced-motion` 用 `[class*="loading"]` 子串匹配恢复动画 → 把装饰性 fade-in 也强制成 infinite 循环，与「禁用装饰性动画」意图相反 | 改为显式白名单 5 个功能性无限旋转指示器 |
| B7 | TV 兜底 `--safe-area-x/y: 3vw/3vh`，注释称「与滑块默认 3 对齐」，实际 store 默认 `tvOverscan: 5` | 改 `5vw/5vh`，注释同步为「预设 0/5/10/15/20」 |
| B7 | 颜色/字号/z-index 全靠 review 人肉把关 | 护栏：ESLint 规则扩到 `style={{}}` + TS 颜色常量；新增 `scripts/design-audit.mjs`（CSS 层裸 hex / fill 档误用 / 裸 z-index / 字重 800-900），**基线棘轮**只拦新增 |

### 护栏机制（本轮新增，可复用）

- `pnpm run lint:design` → `scripts/design-audit.mjs --strict`；存量违规快照在 `scripts/design-audit-baseline.json`（666 条），只拦新增。
- 清债后跑 `pnpm run lint:design:update` 收紧基线（本轮 689 → 666）。
- `lint:all` 顺序改为 `lint:design → lint:css → lint → build`。
- 规范真源：`docs/design/BASELINE.md`（一页速查）+ `docs/design/a11y-CHECKLIST.md`（提交前自查）。

### 涉及文件

- 源码：见 front-matter `files`（22 个 CSS + `index.html` + 1 个 ESLint 规则）
- 文档/工具：`docs/design/BASELINE.md`、`docs/design/a11y-CHECKLIST.md`、`scripts/design-audit.mjs`、`scripts/design-audit-baseline.json`、`package.json`、`.gitignore`（新增两条 `!scripts/design-audit.*` 白名单）

### 验证

- `npx tsc -b` ✅ / `npx vite build --emptyOutDir false` ✅（沙箱清 dist 撞批量删除保护，用标准替代命令）
- B4 零回归：Playwright 注入源 CSS 探针，改前/改后 9 视口 × 7 token diff 为空
- B3 实测 6 视口 × 8 档；B5 实测 TV 1080p/4K 逐档；B6 实测 390 触摸目标 34.66→44.00、tab 28→36.00
- `node scripts/design-audit.mjs` → 4 项 enforce 检查全绿、无新增违规

### 构建

`npx tsc -b && npx vite build --emptyOutDir false` → pass

### 未做（下沉待办）

- B7.4 存量债清理：CSS 裸 hex 266 处、fill 档语义色误用文字 48 处、其余裸 z-index 125 处、字重 800/900 8 处、内联时长字面量 442 处。已全部纳入基线棘轮，不再增长；建议后续按模块分批清并逐步收紧基线。
- `.stylelintrc.json` 存量 1617 处违规（`order/properties-order` 665 为主）为**改动前既有**，`lint:css` 长期为红，未在本轮处理。
- TV 块内 rem 写法的「文档注释值 ≠ 实测值」历史漂移（如 `--comp-input-height-tv` 注释 58px、实测按根字号 15px 折算）未清理。
