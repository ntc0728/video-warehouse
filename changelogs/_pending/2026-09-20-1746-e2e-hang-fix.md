---
date: 2026-09-20
module: scripts/e2e
type: fix
build: npm run build 通过；e2e-suite 全绿 228.5s/300s（阶段A dev 行为全套 + 阶段B preview 契约）
files:
  - playwright.config.ts
  - scripts/global-setup.ts
  - scripts/e2e-skeleton.mjs
  - scripts/e2e-suite.mjs（新增）
  - scripts/e2e-vite-server.cjs（新增）
  - scripts/fixtures/mock-tmdb.ts
  - scripts/fixtures/test-env.ts（新增）
  - scripts/fixtures/iptv-seed.ts
  - package.json
  - AGENTS.md / docs/agents/testing.md
  - scripts/{detail,home,player,player-failover,regression,verify-grid,iptv-player,boot-splash}.spec.ts
---

# E2E 卡死根治 + 端口/进程安全红线 + 5 分钟硬预算

## 旧 ↔ 新

| | 旧 | 新 |
| --- | --- | --- |
| 僵尸 dev server（TCP 通/HTTP 死） | globalSetup 首个 goto 挂满超时，整轮十几分钟零输出 | globalSetup 前置 HTTP 探针 8s fail-fast 并指向跑批器 |
| `npm run test:e2e` | 裸 `playwright test`（无清场、无封顶、复用僵尸） | `e2e-suite.mjs` 两阶段：A=dev 行为全套（契约/取证除外），B=preview 骨架契约；串行、健康轮询、双超时封顶 |
| 全量时长 | 无预算概念（实测 6-7min，且随时可能无限挂） | **≤300s 硬预算**，看门狗到点击杀判失败；AGENTS 红线 #6 |
| 整轮无限挂死 | `globalTimeout` 未设（0） | config 层封顶 CI 40min / 本地 30min |
| TMDB mock 未命中 | `route.continue()` 逃逸公网吃满超时 | 未命中返回 404（确定性）；`TMDB_MOCK=false` 真实模式不变 |
| storageState 占位代理 | `your-*.example.com` DNS 空挂 | 未配 .env.local 时用 `127.0.0.1:1`（毫秒级 ECONNREFUSED） |
| IPTV 注入源 + iptv-org 主干 | 每轮真拉数 MB m3u / 7.5MB channels.json（慢且 flaky） | `test-env.ts` 单一事实源 + 确定性 mock（40 频道 6 组主干 / 小 m3u / 空 EPG / 测速探针），代理编码形态 URL 同样命中 |
| 任意公网逃逸 | 无兜底 | 网络守卫 catch-all：非本地/非测试桩/非 .env.local 配置代理主机 → 立即 abort |
| E2E 端口 | 与用户 `npm run dev` 共用 3001 | **OS 动态分配空闲端口**（`E2E_PORT` 仅显式覆盖），永不与用户服务抢端口 |
| 收尾杀进程 | `killPort` 按端口占用者盲杀 + `taskkill /IM chrome.exe` 连用户浏览器杀 | server 经 `e2e-vite-server.cjs` wrapper 起（命令行即标记），只杀「带标记且探活无响应」的僵尸；浏览器只杀 ms-playwright/临时 profile 特征（实测用户 20 个 chrome 零命中） |
| 本地并发 | workers 4 打满真实代理 / 后改 2 保守 | hermetic 后 workers=3（4 实测仍挤出时序 flaky）+ retries=1 |
| globalSetup 注入 | 固定 sleep 3.6s | 「连续两次读到注入值」收敛轮询（上限 4s，正常 <1s） |
| goto 无 waitUntil | ~22 处默认 `load` | 统一 `domcontentloaded` |
| boot-splash 取证截图 | 25 条 ×5.3s 混在默认套（≈2.3min） | 移出默认套，`test:e2e:shots` 按需 |
| boot-splash.spec 模块 hold | 2500ms 固定（28 条 ×5.3s） | 1200ms（仍大于 warm 态加载耗时，骨架窗口不变） |

## 存量缺陷（卡死年代全量从没跑完过，本轮暴露 6 处）

- 细节修复B：`.iptv-page .iptv-skeleton` 只匹配移动档，rail 档根类名 `.iptv-skeleton--rail` → 永不命中；选择器改两档并集。
- home 1.3c ×3：09-18 进入过渡覆盖层复用真实 rail 组件 → 短暂双 `.cqa-heat-row`/双份 1..20 排名；作用域改 `.home-page:not(.home-skeleton)`。
- FIX-101：mock 下挂载变快，`#root > *` first 命中 toast 的 hidden aria-live 容器；改 `#root > *:visible`。
- BROWSE-013：TMDB 空结果回退 CMS 聚合走真实代理，并发 >12s；fixture 对契约专用关键词 `zzzxxxnotexist` 直接返回空结果超集。
- IPTVP-023：一级栏像素带按真实数据校准（130,135)；hermetic 档实测稳定 136 → 重校准 (130,140)，相对断言原样保留。

## 关键发现

- **守卫必须最先注册**：Playwright 多路由「后注册优先」（types.d.ts:4054），
  守卫 `**/*` 放最前，随后源 mock/TMDB/各测试自建 route 才能盖过它。
- **模式分裂是套件既有属性**：cross-tab 动态 import `/src/*`（dev-only）、骨架契约 B 要生产
  单包 CSS（preview-only）、boot-splash 取证靠 dev 模块 hold。单模式全绿是不可能的，
  e2e-suite 两阶段正是按此切分。
- mock 瞬时返回会抹掉「加载中」形态窗口 → mock 规则统一 600ms 模拟时延（测速 1500ms），
  既保住时序类断言又远快于真实网络。
