# 2026-08-18

## 播放器交互修复计划落地（docs/player-fix-plan.md：C1-C5 冲突 · G1-G10 缺口 · R1-R6 冗余）

- 日期：2026-08-18
- 问题：`docs/player-interaction-audit.md` 审计出播放器交互冲突/缺口/冗余共 20 余项，此前 8-17 已修复一部分（commit 8a1c213），本次按 `docs/player-fix-plan.md` 补齐剩余项并解决计划内部矛盾。
- 冲突修复（C）：
  1. **C1 播放键语义统一**：ControlBar PlayButton disabled 由 `(isBuffering && !isPlaying) || isPlayerLoading` 改为 `isPlayerLoading && !isReadyToPlay`；注释与 togglePlay（缓冲中不暂停）一致。计划内部「缓冲中按钮可暂停 vs togglePlay 缓冲守卫」矛盾按字面方案裁决：缓冲中不暂停为单一真相。
  2. **C2 静音统一**：`usePlayerCore` 新增 `toggleMute()`（当前有声→记 `mutedVolume` 再静音；静音→恢复记忆值，无记忆兜底 1）；VolumeControl 喇叭、键盘 M（点播/IPTV 两分支）、MobileMoreSheet 静音钮全部改走它，消除「0↔1 满音量」与「0↔记忆值」双路径语义不一致。
  3. **C3 seek 策略统一**：键盘 `debouncedSeek` 守卫 `isBuffering || isPlayerLoading` → `isPlayerLoading && !isReadyToPlay`（缓冲中允许 seek，与进度条拖拽一致）；`useLongPress` onSeek 由直接改 `video.currentTime` 改为走 `playerCore.seek`；长按禁用由 isBuffering 改为 `isPlayerLoading && !isReadyToPlay`。N5 同步放开 useLongPress 的 `mode==='iptv'` 早退（IPTV DVR 窗口内同样支持长按 seek）。
  4. **C4/R2 全屏统一**：`lib/fullscreen.ts` 新增 `toggleFullscreen(container, videoElement?, hasError?)`；三处入口（FullscreenButton / F 键 / 双击 usePlayerClickHandler）共用；hasError 守卫经 FullscreenButton hasError prop（PlayerHeader/ControlBar 透传）三处一致。
  5. **C5/R4 渲染异常边界**：PlayerErrorBoundary 新增 `onRetry` prop（重试递增 retryCount 触发重建），错误 UI 加「重试」按钮；注释明确仅兜底渲染异常、不处理播放错误（后者走 hasError → PlayerCore overlay）。
- 缺口补齐（G）：
  1. **G1 进度条缩略图**：新增 `ControlBar/ThumbnailPreview.tsx`（可选 `thumbnails` 源 + `timeToUrl`；无源降级为时间戳文本），ProgressBar 新增可选 `thumbnails` prop，tooltip 内嵌预览。
  2. **G2 音量条全模式**：VolumePopup 渲染条件 `mode==='iptv'` → 全模式；ControlBar 新增 `onVolumePopup` prop，VolumeControl 滑杆/喇叭变化时调 `showVolumePopupWithTimer`（键盘/遥控器原有路径不变）。
  3. **G3/G6 移动端更多设置**：MobileMoreSheet 新增音量卡片（百分比滑块 + 静音钮）+ 画中画项（`document.pictureInPictureEnabled` 约束、`isPiP` 显「开启/退出」）。
  4. **G4 切清晰度提示**：`switchLevel` 内 `playerToast('清晰度：'+label)`；`getResolutionLabel`（'1080p' 小写）抽到 `lib/utils.ts`，ResolutionSwitch / MobileMoreSheet / switchLevel 三处共用（HLSAdapter 的 getQualityLabel '1080P' 不同不动）。
  5. **G5 IPTV 自动恢复扩展**：维持方案 A（现状，仅手动重试），待产品确认，未改代码。
  6. **G7-G10 移动端亮度/音量纵向滑动**：新增 `hooks/useTouchGesture.ts`（原生 touch 监听，touchmove `{passive:false}` 以便 preventDefault；左半屏=亮度、右半屏=音量；8px 纵轴阈值锁方向；上滑增值；亮度 0.1-2 写 `video.style.filter=brightness()`，音量 0-1 走 playerCore.setVolume）+ `MobileUI/BrightnessVolumeIndicator.tsx`（柱状图+图标+百分比，松手 1.2s 淡出）。仅 `isMobileLayout && mode==='video'` 挂载。与 useLongPress 通过共享 `verticalGestureActiveRef` + 10px 位移取消 pending 长按协调。
- 冗余清理（R）：
  1. **R5 控制栏可见性单一出口**：`usePlayerControls` 新增 `syncAutoHide({isPlaying,isBuffering,hasError})`；UniversalPlayer 错误状态 effect 改走它，消除散落的 setControlsVisible/clearTimeout 重复逻辑。
  2. **R6 循环模式常量**：`LOOP_CYCLE` 从 useKeyboardShortcuts + LoopButton 本地定义收敛到 `lib/utils.ts` 共享。
- 涉及文件（18 改 + 3 增）：
  - 新增：hooks/useTouchGesture.ts、MobileUI/BrightnessVolumeIndicator.tsx、ControlBar/ThumbnailPreview.tsx
  - 修改：hooks/{usePlayerCore,useKeyboardShortcuts,useLongPress,usePlayerControls}.ts、lib/{utils,fullscreen}.ts、modules/usePlayerClickHandler.ts、ControlBar/{ControlBar,VolumeControl,FullscreenButton,LoopButton,ResolutionSwitch,ProgressBar}.tsx、MobileUI/MobileMoreSheet.tsx、PlayerCore.tsx、PlayerHeader.tsx、UniversalPlayer.tsx + UniversalPlayer.css
- 验证：`npm run build`（tsc -b && vite build）通过；player.spec.ts 21 过；iptv.spec.ts 13 过；iptv-player.spec.ts 6 过。
- 待办：G5 产品确认；真机验证亮度调节（video filter 在部分平台可能被 HDR/色彩空间覆盖）。

## 历史页 UI 整改落地（changelogs/2026-08-18-history-plan.md + demos/demo-history-redesign-2026-08-18.html）

- 日期：2026-08-18
- 问题：历史页沿用旧的收藏页双段布局（影视/IPTV 分段 + VideoCard 竖版网格），与 `changelogs/demos/demo-history-redesign-2026-08-18.html` 原型要求（融合 Tab、统一横版卡、状态筛选、排序、批量管理）不符。
- 旧逻辑：顶部 `.category-segmented` 影视/IPTV 分段；`historyVideos`/`iptvHistory` 各自渲染 `.video-card-grid`（VideoCard 竖版卡）；无状态筛选/排序；批量管理在底部。
- 新逻辑：
  1. **融合 Tab（综合/视频/IPTV）**：`RecordShell` 新增可选 `fusedCategories`/`actions` prop（不传保持原行为，Collections 零波及），渲染 `StatusTabs`（`record-status record-status--fused` 胶囊化）+ 顶部操作按钮组（更多筛选/清空历史/批量管理）。综合 = 视频+IPTV 合并按 `_histTime` 排序。
  2. **统一横版 RecordCard**：新建 `src/components/RecordCard/`（video 16:9 海报 / IPTV 圆台标+LOGO 候选链+3 级回退 / 视频底部进度条 / LIVE 角标 / 未看完·橙 已看完·绿），复用全局 `.record-card` 勾选/删除类名，CSS 作用域 `.history-page`。
  3. **「更多筛选」折叠面板**：状态 chips（全部/未看完/已看完，仅视频）+ 排序（最近/最早观看，默认最近）。IPTV 不涉及进度恒显示。
  4. **网格列数**：`.history-grid` ≤375→1 列、376–677→2 列、≥678→固定 4 列。
  5. 删除逻辑按 mainTab：单条按 kind 区分；批量用 videoIdSet 判定；清空 all 时清视频+IPTV。
- 踩坑：`.history-grid` 只写了 `grid-template-columns` 漏写 `display: grid` → 实际渲染 block 单列、卡片全宽（HIS-051 探针 1012px 宽），已补 `display: grid` + `gap: var(--space-sm)`。
- 涉及文件：新增 `src/components/RecordCard/{RecordCard.tsx,RecordCard.css,index.ts}`；修改 `src/pages/History/index.tsx`+`History.css`、`src/components/RecordShell/RecordShell.tsx`、`scripts/history.spec.ts`（HIS-025/040 改融合 Tab 选择器，新增 8.7 段 HIS-060/061）。
- 验证：`pnpm run build` 通过；history.spec.ts 10 过、collections.spec.ts 6 过；stylelint 对 RecordCard.css/History.css 零错误（px 单位已按 fluid token 改造）；AGENTS.md 测试计数表同步 8→10。
- 待办：无。

## 历史页网格列数整改：4 列 → 最多 3 列（视口/设备自适应）

- 日期：2026-08-18
- 问题：历史页横版 RecordCard 网格固定 4 列（≥678px 桌面），卡片拥挤（每卡仅 ~248px，media 132px 占一半以上，正文只剩 ~110px）。
- 旧逻辑：`.history-grid` ≥678→4 列、≤677→2 列、≤375→1 列，断点与 RecordShell（768）不对齐；media 恒 132px（≤480 缩 96px）。
- 新逻辑：
  1. **列数**：≥768 桌面→**3 列**（最多 3 列，不随超宽屏膨胀）；481–767 平板/移动→2 列；≤480 手机→1 列（全宽横版卡）；app 恒移动→恒 2 列副本。断点 768 对齐 RecordShell 桌面断点，480 为手机档。
  2. **media 随列数自适应**：桌面 3 列→132px、平板/移动 2 列→104px、手机 1 列→124px（全宽卡媒体略大）+ app 恒 104px 副本。
- 踩坑：3 列下行数变多，末组高度 = ceil(N/3)×行高，HIS-051 的「末组 top > 折叠线」seed 需重新配平：更早组 20→14 条（末组 5 行，max scroll 时 top=270，仍 > 折叠线）。
- 涉及文件：`src/pages/History/History.css`、`src/components/RecordCard/RecordCard.css`、`scripts/history.spec.ts`（新增 8.8 段 HIS-062 列数断言：1280→3 / 600→2 / 375→1；HIS-051 seed 20→14）。
- 验证：`pnpm run build` 通过；stylelint 两文件零错误；history.spec.ts 11 过（含新 HIS-062）、collections.spec.ts 6 过。
- 待办：无。

## 历史页筛选/卡片微调：3 列收敛到 ≥1280、排序 chips↔下拉自适应、状态圆点、卡片加高

- 日期：2026-08-18
- 问题：① 上一轮「≥768 桌面→3 列」仍显拥挤，用户要求 3 列仅 ≥1280px 生效；② 卡片高度偏小；③ 排序控件在宽容器下应显示 chips（最近观看/最早观看/名称A-Z/名称Z-A/评分高到低/评分低到高）而非下拉框；④ record-actions 与 status-chips 间距偏紧；⑤ 状态 chip 需文本左侧彩色圆点（全部=黑、未看完=橙 #d97706、已看完=绿）；⑥ 375px 小视口下排序下拉框过宽；⑦ 切换筛选条件要有动画效果。
- 旧逻辑：网格 ≥768→3 列/481–767→2 列/≤480→1 列；排序恒走 Select 下拉框；状态 chip 无圆点；卡片 media aspect 132/78、正文 padding `--space-2xs`；record-actions/status-chips gap 分别 `--space-xs`/`--space-2xs`。
- 新逻辑：
  1. **列数收敛**：`.history-grid` base 恒 2 列，`@media (width >= 1280px)` + `html:not([data-device="app"])`→3 列；app 恒 2 列；≤480→1 列。
  2. **卡片加高**：media aspect 132/78→132/92；正文 gap `--space-xs→--space-sm`、padding `--space-2xs→--space-xs`；标题字号 `--text-base→--text-lg`；卡 padding `--space-xs-plus→--space-sm`。
  3. **排序 chips↔下拉自适应**：`.history-filter-panel` 加 `container-type: inline-size`；默认渲染 `.history-sort-chips`（6 个胶囊，flex wrap）+ `.history-sort-dropdown`（`display:none`）；`@container (max-width: 720px)` 时隐藏 chips、显示下拉框并 `min-width:0` 压缩；`@media (width <= 375px)` 时 trigger `min-width: var(--layout-select-w-mobile)`。app 恒窄容器→恒走下拉框。
  4. **间距**：`.record-actions` gap `--space-xs→--space-sm`；`.history-status-chips` gap `--space-2xs→--space-sm`。
  5. **状态圆点**：`.history-status-chip` 改 inline-flex，前置 `<span class="history-status-chip__dot">`；默认黑（全部）→`[data-status="unfinished"]` #d97706、`[data-status="finished"]` `--color-success`，is-active 时转反色。
  6. **6 项排序**：SortKey 扩为 recent/oldest/name-asc/name-desc/rating-desc/rating-asc；名称取 title（视频）/name（IPTV）按 localeCompare('zh')；评分取 `_histRating`（视频）IPTV 恒 0。
  7. **评分数据链路**：`HistoryRecord` 新增 `rating?`；`useUserStore.updateHistoryProgress` 参数与 addHistory 合并分支透传；Player 播放中 `updateHistoryProgress` 补传 `tmdbDetail?.vote_average`。
  8. **动画**：`.history-content` key 由 `mainTab` 扩为 `${mainTab}-${statusFilter}-${sortBy}`，切换任何筛选条件都重放 `animate-fade-in`。
- 涉及文件：`src/pages/History/History.css`+`index.tsx`、`src/components/RecordCard/RecordCard.css`、`src/types/store.ts`、`src/stores/useUserStore.ts`、`src/pages/Player/index.tsx`、`scripts/history.spec.ts`（HIS-062 注释断点 768→1280）。
- 验证：`pnpm run build` 通过；stylelint 零错误；history.spec.ts 11 过、collections.spec.ts 6 过、useUserStore vitest 8 过。
- 待办：无。

## 收藏页 UI 整改落地（changelogs/2026-08-18-collections-plan.md：方案 B 融合 Tab + 分区，视频卡角标重组）

- 日期：2026-08-18
- 问题：收藏页沿用旧「影视/IPTV 分段 + statusTabs 状态标签页 + record-edit-row（排序 Select + 批量按钮）」布局，与历史页已落地的融合 Tab 体验不一致；视频卡竖版海报（2:3）与 IPTV 卡竖排封面（3:2）宽高比不同，无法同网格混排，需分区。
- 旧逻辑：`activeTab`（video/iptv）+ `statusFilter`（全部/未观看/正在看/已看完）双状态；`statusTabs` 顶层展示；排序 Select 与批量按钮在 `record-edit-row`；两网格各自独立 tab 切换。
- 新逻辑（方案 B，决策：状态筛选进「更多筛选」面板与历史页一致、综合下选状态时影视分区仅显对应状态直播恒全量、更多筛选仅状态 chips + 排序不扩维度）：
  1. **融合 Tab（综合/视频/IPTV）**：Collections 传 `fusedCategories` + `actions`（更多筛选/清空收藏/批量管理），移除 `activeTab`/`statusTabs`/`record-edit-row`（排序移入面板）。
  2. **分区渲染**：综合 = 「影视」竖版海报墙（`.video-card-grid`）+「直播」原项目 `IPTVChannelCard` 竖排卡（`.iptv-channel-grid`）两个 section（`.collection-section-head` 标题 + 计数），各用原生网格；video/iptv 单 tab 只显对应区。
  3. **视频卡角标重组**：`VideoCard` 新增可选 `status` prop（收藏页传 `video._status`）：传时封面左下=「年份+类型」合并双徽标（`.video-card-badges-bl`）、右下=观看状态（`.video-card-status`，未观看蓝/正在看橙/已看完绿）；不传（Browse/Home/Detail 等）保持原「左下年份+右下类型」路径一字不改。
  4. **状态模型**：`mainTab` 三态；`statusFilter` 仅过滤影视项（直播恒全量）；综合搜索用独立 `searchByTab.all` 关键词同时过滤两区；融合 tab 计数 = 过滤后视频数 / IPTV 全量；批量删除按 `videoIdSet` 判别视频/IPTV，清空按 mainTab 分支（all 清两类）。
  5. **卡片出场动画保留**：`.video-card-grid`/`.iptv-channel-grid` 子项 nth-child stagger（`fadeInUp`）+ 容器 `key={mainTab}` 重挂载重放，与整改前一致；搜索/筛选/排序不重挂载不误触动画。
- 踩坑与结构调整（样式作用域，历史页整改遗留的隐藏坑）：
  1. **通用样式抽离**：历史页的 `.record-actions`/`.action-btn`/`.record-status--fused` 原定义在 `History.css`（`.history-page` scoped），收藏页复用会「无样式裸奔」——已迁至 `RecordShell.css`（去前缀），两页共用。
  2. **「更多筛选」面板抽公共组件**：新建 `src/components/RecordFilterPanel/`（状态 chips + 排序 chips↔下拉容器查询，圆点色经 `--chip-dot-color` 传入），`History` 先改引用（行为不变）再给 Collections 复用，消除两份面板代码。
  3. **IPTV 懒加载守卫**：`mainTab !== 'video'`（综合也展示直播分区）而非原 `activeTab === 'iptv'`。
  4. **VideoCard `_status` 类型收窄**：`CollectionVideoItem._status` 收敛为 `ConcreteVideoStatus`（不含 'all'），避免 TS2322。
- 涉及文件：
  - 新增 `src/components/RecordFilterPanel/{RecordFilterPanel.tsx,RecordFilterPanel.css,index.ts}`
  - 修改 `src/pages/Collections/{index.tsx,Collections.css}`、`src/components/VideoCard/{VideoCard.tsx,VideoCard.css}`、`src/pages/History/{index.tsx,History.css}`（面板改引公共组件、删除已迁样式）、`src/components/RecordShell/RecordShell.css`（迁入通用样式）
  - 测试 `scripts/collections.spec.ts`（COL-031 改断言：排序移入面板、宽容器 chips）、`scripts/history.spec.ts`（HIS-061 选择器 `.history-*`→`.record-filter-*`）
  - `AGENTS.md` 测试计数表：history 10→11（含历史页遗留漏同步），聚合行重算（VideoCard 110 / RecordShell 17 / StatusTabs 17 / 新增 RecordFilterPanel 17）
- 验证：`npm run build` 通过；ESLint 改动文件零错误；stylelint 改动 5 个 css 仅 2 处属性顺序违规已修（存量 380+ 全项目违规与本次无关）；collections.spec.ts 6 过、history.spec.ts 11 过、browse.spec.ts + detail.spec.ts 45 过（VideoCard 默认路径回归验证）。
- 待办：未 commit（本次改动与历史页整改存量改动同批待提交）。

## 历史页 RecordCard hover 边框 + IPTV 台标改充满容器（Tv 图标占位）

- 日期：2026-08-18
- 问题：① 卡片 hover 边框颜色未变化、无悬浮反馈；② IPTV 台标以圆形小 logo（48px 圆 + 渐变背景）居中显示，与「横版卡媒体区充满容器」的视觉不统一；失败/缺失时显示字母占位而非原 IPTVChannelCard 的 Tv 图标占位。
- 旧逻辑：`.history-page .record-card:hover` 沿用 `border: 1px solid var(--color-border-light)`（无视觉变化）；`.record-card__media--logo` flex 居中 + 圆形 48px `border-radius:50%` + 渐变背景，失败走 `immediateLetter` 字母占位。
- 新逻辑：
  1. **hover 反馈**：`border-color: var(--color-border-strong)` + `box-shadow: var(--shadow-card-hover)`。
  2. **IPTV 台标充满容器**：`.record-card__media--logo` 移除 flex/圆形样式，`.lazy-image`/`.lazy-image-fallback` `width:100%; height:100%; object-fit:cover`，保留渐变背景作加载底色。
  3. **Tv 图标占位（整改前 svg 语义）**：新增 `.record-card__media-glyph`（absolute 覆盖层 + `data-show` 切换 opacity），IPTV 分支 `onError={() => setLogoError(true)}`，`data-show={logoError || (!media && !logoCandidates?.length)}` 时显示浅灰 Tv 图标（`Icon icon={Tv}`，与 IPTVChannelCard `.iptv-card-cover__glyph` 一致）；`fallbackSrc=""` 保持无品牌占位图。
- 涉及文件：`src/components/RecordCard/RecordCard.tsx`（import Tv + useState logoError + glyph div）、`RecordCard.css`（hover + media--logo 铺满 + media-glyph）。
- 验证：`pnpm run build` 通过；stylelint 两 css 零错误；history.spec.ts 11 过、collections.spec.ts 6 过。
- 待办：无。

## 文档归档：outputs/ → changelogs/（原型与计划文档归位，删除 outputs 临时目录）

- 日期：2026-08-18
- 问题：历史页/收藏页整改的交互原型（html）与实施计划（md）此前散落在 `outputs/`（临时目录，未纳入版本库），不符合 changelogs 约定（Demo 永久留存 + 改动记录归档）。
- 旧逻辑：`outputs/history-redesign.html`、`outputs/collections-redesign.html`、`outputs/history-redesign-plan.md`、`outputs/collections-redesign-plan.md` 四个文件游离于 git 跟踪之外。
- 新逻辑：
  1. **原型（html）** → `changelogs/demos/demo-history-redesign-2026-08-18.html` / `demo-collections-redesign-2026-08-18.html`，并在 `changelogs/README.md` Demo 索引表登记。
  2. **计划（md）** → `changelogs/2026-08-18-history-plan.md` / `2026-08-18-collections-plan.md`（按日期命名，与每日改动记录同目录）。
  3. **引用同步**：两 plan 内「配套原型」路径、`changelogs/2026-08-18.md` 标题/正文、`.workbuddy/memory/2026-08-18.md` 全部改为新路径；全库 grep 确认无 `outputs/` 残留（android/app/build/outputs 为构建产物无关）。
  4. **删除临时产物**：`outputs/` 目录移除；`test-results/`、`playwright-report/` 清理。
- 涉及文件：changelogs/{README.md, 2026-08-18.md, 2026-08-18-history-plan.md, 2026-08-18-collections-plan.md, demos/demo-*-redesign-2026-08-18.html}。
- 验证：`git status` 干净（仅本次文档变更）；grep 无残留引用。
- 待办：提交并推送远程。