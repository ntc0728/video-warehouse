
## 播放器（/play）逻辑冲突修复（审查报告 2026-08-17）

- 日期：2026-08-17
- 问题：缓冲/加载期用户操作冲突 + 提示信息时机不当。详见 `docs/player-logic-conflicts-review-2026-08-17.md`。
- 修复项（13 项）：
  1. **1.1/4.4** 缓冲中暂停后播放/进度条三重锁死：pause handler 清 isBuffering；isBuffering && !isPlaying 不再导致按钮禁用持续锁死
  2. **1.3** 缓冲中单击视频误暂停：togglePlay 暂停分支加 isBuffering 守卫
  3. **4.1** canplay 无条件 setPlaying(false) 导致播放中 seek 重缓冲完成后 UI 状态瞬时被置 false：仅 video.paused === true 才写 setPlaying(false)
  4. **2.1** 「已自动跳转」提示在 loadedmetadata（缓冲期）即弹出：移入 canplay 单入口 + loadProgress 内部等待 seeked 后提示
  5. **2.2** 进度恢复双入口重复执行：删 episodeUrl effect，用 progressRestoredRef 保证每源/集只恢复一次
  6. **1.5/1.6** 键盘 seek 无缓冲守卫 + 提示先于生效：debouncedSeek 加 isBuffering/isPlayerLoading 守卫；seek() 提示改在 seeked（800ms 超时兜底）后
  7. **1.4** 缓冲中进度条一刀切禁用：移除 beginDrag 的 buffering return；ControlBar 删 buffering prop
  8. **3.1** autoPlay 静音兜底无提示无法恢复：autoMutedRef + 静音后 toast 警告 + play()/setVideoVolume() 解除 muted + store.volume 同步为 0
  9. **4.2** 点播缓冲遮罩显示延迟/丢包指标：mode==='iptv' 条件包裹 metrics div
  10. **4.3** 加载期 tooltip 显示 0:00：tooltip 渲染加 duration > 0 条件
  11. **3.2** 切集提示被线路提示竞争覆盖：lib/utils.ts 新增 suppressSourceToast/isSourceToastSuppressed；ToastTrigger src 变化检查抑制窗口；useEpisodeSwitcher 切集时调用 suppressSourceToast(300)
- 涉及文件：
  - src/components/UniversalPlayer/hooks/usePlayerCore.ts（核心逻辑）
  - src/components/UniversalPlayer/hooks/useProgressRestore.ts（提示时机）
  - src/components/UniversalPlayer/hooks/useKeyboardShortcuts.ts（键盘 seek 守卫）
  - src/components/UniversalPlayer/ControlBar/ProgressBar.tsx（拖拽禁用 + tooltip 条件）
  - src/components/UniversalPlayer/ControlBar/ControlBar.tsx（删 buffering prop）
  - src/components/UniversalPlayer/UniversalPlayer.tsx（遮罩指标 mode 条件）
  - src/components/UniversalPlayer/ToastTrigger.tsx（线路提示抑制窗口）
  - src/components/UniversalPlayer/lib/utils.ts（suppressSourceToast 工具）
  - src/pages/Player/hooks/useEpisodeSwitcher.ts（切集时调用 suppress）
- 验证：build 通过；iptv.spec.ts 13 过；iptv-player.spec.ts 6 过；player.spec.ts 20/21 过（PLAYER-M06 CMS 加载间歇超时，与本次修改无关）；vitest 358 过
- 备注：播放器逻辑改动集中在缓冲/加载期交互和提示时机，未涉及数据层和 API。
# 2026-08-17

## Android 原生 DLNA 投屏模块接入（SSDP 发现 + UPnP AVTransport 推送）

【问题】投屏 UI 与状态机（`CastSheet.tsx` + `castService.ts` 的 `window.CastBridge` 契约）自 8-15 落地后一直是「空态」——Android 端无原生桥注入，`getCastBridge()` 恒 null，`discoverCastDevices()` 恒空数组，用户无法真正投屏到 DLNA 电视/盒子。

【旧逻辑】前端仅预留契约；`android/` 整目录被 gitignore、CI 靠 `cap add android` + `cap sync android` 重建，仓库内没有任何原生 Android 代码，`android/app/src/main/java` 目录不存在（MainActivity 曾在 1a7bf78 被跟踪、17d526d 移除）。

【新逻辑】新增 `scripts/android-dlna-patch/`（原生代码唯一源，与 `android-res-patch/` 同模式），由幂等脚本 `scripts/patch-android-dlna.ps1` 在 cap sync 后复制进 android/：
1. **Capacitor 6 原生插件 `CastBridgePlugin`**（`@CapacitorPlugin(name="CastBridge")` + `@PluginMethod`，注意 Capacitor 6 注解是 `@PluginMethod` 非 `@CapacitorMethod`）：discover/connect/disconnect/setSource/play/pause/seek/setVolume 与前端契约一一对应；网络操作走单线程 Executor，discover 内置 3s 超时（SSDP + 描述拉取共用预算），resolve 不阻塞 UI。
2. **MainActivity**：`registerPlugin(CastBridgePlugin.class)` 必须在 `super.onCreate()` 之前；保留历史 textZoom 反射修复；`WebViewListener.onPageLoaded` 时 `evaluateJavascript` 注入 `window.CastBridge` shim（代理到 `Capacitor.Plugins.CastBridge`，方法内按调用时惰性取插件对象，插件未就绪时 discover resolve 空数组、其余 noop）。
3. **SSDPDiscovery**：MulticastSocket 临时端口绑定 + `setReuseAddress`，优先无线网卡 `setNetworkInterface`（wlan 优先，找不到用第一个 up+IPv4 接口），`WifiManager.MulticastLock` 保组播接收；M-SEARCH 发两次（MediaRenderer:1 + ssdp:all），400ms receive 超时轮询到 deadline；解析 USN→LOCATION 去重（上限 32）；再并行拉 device description XML（XmlPullParser）取 friendlyName + AVTransport/RenderingControl 服务 controlURL（相对地址按 LOCATION base 解析），**必须含 AVTransport 才入设备列表**。
4. **UPnPAVTransport**：SOAP POST + `SOAPAction` 头 + `CurrentURIMetaData`（DIDL-Lite videoItem，URL/标题 XML 转义）；SetAVTransportURI 成功后**自动 Play**（Play 失败仅 warning 不使 setSource 整体失败）；Play/Pause/Stop/Seek(REL_TIME HH:MM:SS)/SetVolume(RenderingControl 0-100)。
5. **AndroidManifest 合并**：追加 `ACCESS_NETWORK_STATE`/`ACCESS_WIFI_STATE`/`CHANGE_WIFI_MULTICAST_STATE` 权限 + `<application android:usesCleartextTraffic="true">`（DLNA 局域网 http 明文），缺失才加、幂等。
6. **接线三条构建路径**：`build-android.ps1`（cap sync + res patch 后调用）、`build-android.bat`（顺带补上了原本缺失的 res patch）、`release-android.yml`（cap sync 后执行）。

【踩坑】
- **PowerShell 5.1 读无 BOM 的 UTF-8 .ps1 会按 GBK 解码**，中文写进字符串字面量直接解析失败（parse error：字符串缺少终止符）。现有 `build-android.ps1` 能跑是因为中文只在注释/Write-Host 里；本脚本字符串里有中文 → 必须给文件加 UTF-8 BOM（node 写回 `\xEF\xBB\xBF` 前缀）。
- **`Copy-Item -Path "$src\*" -Destination $dest -Recurse` 在 PS5.1 会拍平父层级**：`java\com\videowarehouse\app` 被复制成 `java\videowarehouse\app`（丢掉 com）。改为 `Get-ChildItem -LiteralPath $src -Force | ForEach-Object { Copy-Item -LiteralPath $_.FullName -Destination $dest -Recurse -Force }` 逐个复制顶层子项，保结构。
- **权限匹配串重复前缀**：`$perm` 本身已含 `android.permission.`，若匹配串拼成 `"android.permission.$perm"` 会生成 `android.permission.android.permission.ACCESS_NETWORK_STATE` 脏权限 → 直接以 `$perm` 全名匹配。
- **本机 `android/app/src/main/res/` 有一批无效资源目录** `drawable-night-land-hdpi` 等（限定符顺序应为 `land-night-hdpi`，且来源不明、非 res-patch 产物），`mergeDebugResources` 直接 FAILED。CI 由 cap add 重建无此问题，本机调试需手动删除这些目录才能编译。
- `new InetSocketAddress(0, SSDP_PORT)` 构造器不存在，改 `new InetSocketAddress(0)`（临时端口，M-SEARCH 响应是单播回源端口，无需占用 1900）。

【涉及文件】
- 新增：`scripts/android-dlna-patch/java/com/videowarehouse/app/MainActivity.java`、`.../cast/{CastBridgePlugin,CastDeviceInfo,SSDPDiscovery,UPnPAVTransport}.java`、`scripts/patch-android-dlna.ps1`
- 修改：`scripts/build-android.ps1`、`scripts/build-android.bat`、`.github/workflows/release-android.yml`

【验证】`gradlew :app:compileDebugJavaWithJavac` BUILD SUCCESSFUL（57 tasks，本机 SDK 34）；`pnpm run build` 通过；补丁脚本幂等（二跑显示「无需变更」）；E2E player.spec.ts 4.11 段 mock 桥用例不受影响（前端零改动）。

【待办】真机验证：电视/盒子上实际发现 + 推送；iOS 端 Swift 插件按同名契约注入（前端零改动）；字幕实际渲染（8-15 遗留）。

## Web 端投屏分端实现（iOS Web 隐藏按钮 · 安卓 Web 走 Google Cast SDK）

- 日期：2026-08-17
- 问题：用户确认「iOS web 端隐藏投屏按钮，安卓 web 端要做，其次不允许有浏览器限制」。纯浏览器无法做 DLNA（SSDP 组播需 UDP socket，无 Web API）；Google Cast Web SDK（Chromium 内核专属，发现由内核 mDNS/BLE 完成）是安卓 Web 端的官方无限制方案。
- 旧逻辑：`CastSheet`/`HeaderActions` 仅支持原生 `window.CastBridge`（Android App DLNA），Web 端 `getCastBridge()` 恒 null → 投屏按钮无差别显示但打开即空态；无 iOS 与安卓 Web 的区分。
- 新逻辑：
  1. `castService.ts` 新增 `CastMode` 能力检测：`getCastMode()` = 原生桥优先 > Chromium 且非 iOS（`isWebCastSupported()`）→ `web` > 其余 `none`。iOS Safari/WebKit 一律 `none` → 按钮隐藏。
  2. **Google Cast SDK 集成**（`initWebCast`/`webCastRequestSession`/`webCastLoadMedia`/`webCastTogglePlay`/`webCastSetVolume`/`webCastEndSession`）：动态加载 `cast_sender.js`（幂等 Promise，失败不缓存可重试）；`CastContext.getInstance()` + 默认接收器 `CC1AD845`；`requestSession()` 弹**系统设备选择弹窗**（Cast SDK 无设备列表 API，与 DLNA 搜索列表流程不同）；`RemotePlayer`+`RemotePlayerController` 控制播放/暂停/音量；按 URL 后缀判 `contentType`（m3u8/dash/mp4）。
  3. `CastSheet` 双模式：native 走原搜索列表流程；web 打开即 `initWebCast`→`requestSession`，空态文案/说明改为「Chromecast / Google TV」，断开走 `webCastEndSession`。
  4. `HeaderActions` 新增 `castEnabled` prop（`getCastMode() !== 'none'`）——iOS Web 端投屏按钮不渲染（更多设置保留）。
- 涉及文件：src/services/castService.ts / src/components/UniversalPlayer/MobileUI/CastSheet.tsx / src/components/UniversalPlayer/MobileUI/HeaderActions.tsx / src/components/UniversalPlayer/UniversalPlayer.tsx / scripts/player.spec.ts
- 验证：`pnpm run build` 通过；vitest 358 通过；player.spec.ts 4.11/4.12/4.13 全部通过（含新增 PLAYER-M13 iOS 隐藏、M14 安卓 Web 按钮可见、M15 mock Cast SDK 全流程）。E2E 4.11/4.12 的 native 用例需在 beforeEach `addInitScript` 注入占位原生桥（按钮显隐在挂载时由 `getCastMode()` 判定，运行期注入无效）。
- 待办：真机安卓 Chrome 验证 requestSession 系统弹窗与投屏播放；生产接收器 App ID 需在 Google Cast SDK Developer Console 注册替换 `CC1AD845`。

## 投屏按需加载打包 + 部署性能优化（dist 93% 瘦身 + worker 采样/缓存）

- 日期：2026-08-17
- 问题：① 投屏代码（castService 含 Google Cast SDK 集成 + CastSheet 弹窗）被静态 import 进 UniversalPlayer，全部打进播放器 chunk，未按需加载；② dist 因 `emptyOutDir: false` 历史遗留累积到 69.6MB / 1749 个陈旧 js（每次构建只追加不清除），严重拖慢 Cloudflare Pages 部署；③ wrangler `head_sampling_rate = 1`（100% 采样）观测成本高；cors-proxy 无缓存、每请求回源。
- 旧逻辑：
  1. `UniversalPlayer.tsx` 静态 import `CastSheet` + `getCastMode`；`castService.ts` 同时含轻量能力检测与重型 Google Cast SDK 集成 → 全部进播放器 chunk。
  2. `vite.config.ts` `emptyOutDir: false`（2026-08-13 为规避本地 safe-delete 防护拦截 >50 文件批量删除而关闭）→ dist 无限累积。
  3. `worker/wrangler.toml` `head_sampling_rate = 1`；`worker/cors-proxy.js` 无缓存。
- 新逻辑：
  1. **拆分 `webCastSdk.ts`**：Google Cast SDK 集成（initWebCast/webCastRequestSession/webCastLoadMedia/webCastTogglePlay/webCastSetVolume/webCastEndSession + SDK URL/接收器常量）移入独立模块；`castService.ts` 只保留轻量能力检测（getCastMode/getCastBridge/isWebCastSupported/discoverCastDevices/connectCastDevice/disconnectCast）+ 类型，re-export `WebCastDevice` 兼容。
  2. **CastSheet 懒加载**：`UniversalPlayer` 改用 `React.lazy(() => import('./MobileUI/CastSheet'))` + `Suspense`，新增 `castMounted` state——首次点击投屏按钮才挂载（触发 chunk 拉取），之后保持挂载复用状态。投屏代码（CastSheet 7.4KB + Web Cast SDK）从播放器 chunk 拆出，仅按需加载。
  3. **恢复 `emptyOutDir: true`**：实测当前环境批量删除（60 文件）不再被 safe-delete 拦截，恢复构建前清空。dist 69.6MB/1749 文件 → **4.9MB/47 文件（93% 瘦身）**，Pages 部署体积与上传时长大幅下降。
  4. **worker 部署优化**：wrangler `head_sampling_rate` 1 → 0.1（10% 采样，降观测成本）；`cors-proxy.js` 增加 60s 短 TTL 内存缓存（仅 GET 且 2xx，`X-Cache: HIT` 标记，evict 防泄漏），降低 CMS 高频请求回源压力。
- 涉及文件：新增 src/services/webCastSdk.ts；修改 src/services/castService.ts / src/components/UniversalPlayer/UniversalPlayer.tsx / src/components/UniversalPlayer/MobileUI/CastSheet.tsx / vite.config.ts / worker/wrangler.toml / worker/cors-proxy.js
- 验证：`pnpm run build` 通过；vitest 358 通过；player.spec.ts + iptv-player.spec.ts 27 用例全部通过（含懒加载后的 M15 mock Cast SDK 全流程、M05 原生桥全流程）；eslint 投屏相关文件零新增违规（剩余 error/warning 为改动前已存在：UniversalPlayer L145 条件 hook、L456 buildProxyUrl dep）。构建产物验证：CastSheet chunk 含 gstatic/requestSession/CC1AD845（Web Cast SDK 已隔离），UniversalPlayer chunk 不含（懒加载生效）。
- 待办：cors-proxy 缓存命中率取决于请求集中度（Worker 多实例内存不共享，仅尽力命中），生产观察决定是否上 KV/边缘缓存。

## 历史页桌面端左侧算珠时间轴（sticky 常驻 + 滚动逐颗累加）

- 日期：2026-08-17
- 问题：历史页移动端有内联时间轴节点（每分组一行：圆点+名称+数量），桌面端没有时间轴，长列表下无法快速定位/感知当前分组。
- 旧逻辑：`.history-node-col` 曾用 sticky 吸顶分组行做导航，但因 keep-alive 多页 DOM + 布局测量缺陷不可靠，已还原为普通节点行；桌面端无时间轴。
- 新逻辑（桌面 `@media (width >= 768px)` + `html:not([data-device="app"])`，移动端布局零改动）：
  1. **左侧 sticky 算珠面板** `.history-timeline`：`position: sticky; top: 0` + `align-self: flex-start` + `height: calc(100vh - var(--header-height))`，宽 `clamp(7rem, 9vw, 10rem)`，`flex-shrink: 0`。⚠️ **不能 `align-items: stretch` 拉伸到与内容同高**——sticky 元素与包含块等高时无「可粘空间」，滚动时 panelTop 一路变负（面板不吸顶），这是本功能最大坑。
  2. **珠子横向布局**：时间文本左对齐面板左、数量徽标右对齐面板右、圆点钉在面板中线（`left: 50%; translate(-50%,-50%)`）；rail 竖线同钉 `left: 50%` 与圆点共线——**时间在轴线左侧、数量在轴线右侧**。珠为 `<button class="history-timeline__bead">`，绝对定位 `top:0` + `width:100%`，纵向位置由 `transform: translateY(y)` 驱动（每帧更新，无 CSS transition → 无回弹）。
  3. **纵向定位公式**（`index.tsx` `updateBeadPositions`）：`beadY_i = max(pileY_i, groupTrack_i)`，`pileY_i = BEAD_PAD(8) + i*step`（`step = 珠高 + BEAD_GAP(10)`，珠高运行时测量存 `beadStepRef`），`groupTrack_i = 分组头 getBoundingClientRect().top - 面板 top`。分组头滚过堆叠槽即「入栈」贴着顶部逐颗排开；向上滚动逐步恢复原位，回顶完全还原。
  4. **监听与 Keep-Alive**：`useLayoutEffect`（deps 含 `groupedKeys` + `location.pathname`）绑定滚动容器（`scrollContainerRef`，即 `.app-shell__scroll`）+ ResizeObserver，滚动用 `requestAnimationFrame` 节流；离开 /history 解绑、返回时 pathname 变化重新绑定并立即同步首帧定位（避免闪烁）。隐藏页 `display:none` 期间 `getBoundingClientRect().width` 为 0 → 用 `panelRect.width <= 0` 早退（桌面面板宽度恒 >0，正常参与计算）。
  5. **移动端**：面板 `display:none`，内联 `.history-node-col` 保留；点击/焦点/激活样式从 `.history-node-col` 收到 `.history-node`。桌面端隐藏内联节点、`.history-content` 改 flex（`gap: var(--space-lg)`）双列布局。
- 涉及文件：src/pages/History/index.tsx（refs/公式/监听/面板 JSX/`sourceId` 透传）、src/pages/History/History.css（面板/珠/rail/dot/count 样式）、scripts/history.spec.ts（新增 8.6 段 HIS-050/051）
- 验证：`pnpm run build` 通过；`pnpm exec playwright test scripts/history.spec.ts` 8 项全过（含新增 HIS-050 桌面面板/移动端隐藏、HIS-051 滚动堆叠无重叠 + 回顶恢复原位 ≤1px）；Playwright 探针确认 rail/dot 共线面板中线（x=233）、时间左（x=178）、数量右（x=270）、堆叠 y=8/35.75、Keep-Alive 切走再回监听重绑正常。
- 待办：暂无；方案在浮层气泡（rail 贴卡片左缘）与独立面板间已明确选独立面板。
