# 2026-09-02 — 改动记录

> 本文件覆盖 **2026-09-02 当日全部改动**（按发生顺序）：播放页侧栏抖动/滚动条槽位修复 → VOD 自动故障转移 → 跨页签 IDB 一致性修复 → BroadcastChannel 内存快照同步 → reload 脏集合合并（含单测）→ flush 同名清理 → IPTV 跨页签 storage 同步 → 设置跨页签白名单合并（含单测）→ wd 双重编码 + 前后台冻结修复 → 窗口失焦冻结修复 → 播放器缓冲优化 + IPTV 前端重构两份文档落地 → 三项收尾修复（删调试日志 / 角标下移 / 历史·收藏 CMS 源拦截）。
> 详细根因与踩坑见 `.workbuddy/memory/2026-09-02.md`。

---

## 1. HLSAdapter 缓冲策略：动态预加载 → 固定 90MB 上限 + 直播低延迟

- 日期：2026-09-02
- 问题：原实现用 `getBufferLength` + `preloadTimer` + `baseBufferLength`/`startPreload`/`stopPreload` 手动管理缓冲，逻辑脆弱、直播缓冲收敛不及时。
- 旧逻辑：`src/components/UniversalPlayer/adapters/HLSAdapter.ts` 里手维护预加载定时器与基础缓冲长度，ABR 默认估算偏低，弱网易频繁卡顿。
- 新逻辑：移除整套动态预加载逻辑；`hls.js` 配置改为
  - `maxBufferSize: 90 * 1024 * 1024`（90MB 硬上限，hls.js 自动在 30s~600s/回看区间管理缓冲，见 `:80` 注释）；
  - `abrEwmaDefaultEstimate: 1500000`（1.5Mbps 起跳，弱网首段更稳）；
  - 直播分支在 `LEVEL_LOADED` 收敛缓冲并吸收低延迟参数。
- 涉及文件：`src/components/UniversalPlayer/adapters/HLSAdapter.ts`（`maxBufferSize` `:81`、`abrEwmaDefaultEstimate` `:84`）
- 构建：`npm run build` 通过；Vitest 383 通过；player E2E 全绿。

---

## 2. MPEGTSAdapter：点播/直播分支 + 断流重连

- 日期：2026-09-02
- 问题：mpegts.js 适配层只有单一直播配置，点播（FLV/TS 直链）不支持 seek，直播断流不重连。
- 旧逻辑：`MPEGTSAdapter` 固定按直播初始化，无 `isLive` 分支、无 ERROR 重连、无点播 seek。
- 新逻辑（`src/components/UniversalPlayer/adapters/MPEGTSAdapter.ts`）：
  - `isLiveMode = options?.isLive ?? true`（`:33`），构造函数透传 `:30`；
  - 直播（`isLive=true`）：低延迟配置、不支持 seek、ERROR 后 2s 重试 `unload→load→play`，最多 `MAX_RECONNECT_COUNT = 5` 次（`:19`）并 toast 提示（`:81`~`:86`）；
  - 点播（`isLive=false`）：正常缓冲、支持 seek、失败不重连（走上层切线路/切源）；
  - `destroy()` 清理 `reconnectTimer`（`:147`~`:149`）。
- 涉及文件：`src/components/UniversalPlayer/adapters/MPEGTSAdapter.ts`
- 构建：`npm run build` 通过；playwright MPEGTS 点播/重连用例通过。

---

## 3. DashAdapter 缓冲提升

- 日期：2026-09-02
- 问题：dash.js 默认缓冲偏短，长视频拖动后恢复慢。
- 旧逻辑：`bufferTimeDefault` 等沿用 dash.js 默认小值。
- 新逻辑（`src/components/UniversalPlayer/adapters/DashAdapter.ts:36`~`:38`）：`bufferTimeDefault: 30` / `bufferTimeAtTopQuality: 45` / `bufferTimeAtTopQualityLongForm: 60`（秒）。
- 涉及文件：`src/components/UniversalPlayer/adapters/DashAdapter.ts`
- 构建：`npm run build` 通过。

---

## 4. vodParser：FLV/TS/M2TS 识别为裸流 'flv'

- 日期：2026-09-02
- 问题：`.flv/.ts/.m2ts` 直链若被 `detectSourceType` 误归为 `m3u8`，HLS 解析必败，无法走裸流降级。
- 旧逻辑：`detectSourceType` 未把 `.flv/.ts/.m2ts` 单列出来。
- 新逻辑（`src/services/vodParser.ts`）：`VALID_VIDEO_EXTENSIONS`（` :18`）与 `EMBEDDED_VIDEO_EXT`（` :21`）补充 `flv|ts|m2ts`；`detectSourceType`（` :46`）内 `/\.(flv|ts|m2ts)([?&#/;]|$)/i.test(url) return 'flv'`（` :50`）。单测 `vodParser.test.ts` 同步新增 flv/ts/m2ts 用例。
- 涉及文件：`src/services/vodParser.ts`、`src/services/vodParser.test.ts`
- 构建：Vitest 383 通过。

---

## 5. UniversalPlayer：点播裸流降级 + isLive 透传

- 日期：2026-09-02
- 问题：点播 FLV/TS 直链（detectSourceType 误归 m3u8）解析失败无兜底；MPEGTS 分支拿不到 isLive 状态。
- 旧逻辑：`type` 选择仅在 `mode==='iptv'` 下用 `currentType`，VOD 不启用裸流降级；`usePlayerCore` 未收到 `isLive`。
- 新逻辑（`src/components/UniversalPlayer/UniversalPlayer.tsx`）：
  - 裸流降级（D1，` :518`~`:532`）：HLS `manifestParsingError → BARE_STREAM` 时 `setDegradedType('flv')`（` :528`）重试同一 URL（每 URL 仅 1 次防环）；点播与 IPTV 均启用；
  - `type` 选择（` :585`）：`degradedType ?? (mode === 'iptv' ? currentType : type)`；
  - `isLive` 透传（` :595`）：`isLive: mode === 'iptv'`（VOD 为 false → MPEGTS 走点播分支）；
  - A3 直播直连失败自动切代理（` :535`）。
- 涉及文件：`src/components/UniversalPlayer/UniversalPlayer.tsx`、`src/components/UniversalPlayer/usePlayerCore.ts`、`src/components/UniversalPlayer/adapters/adapterRegistry.ts`
- 构建：`npm run build` 通过；player E2E 全绿。

---

## 6. IPTV 频道可用性预检测

- 日期：2026-09-02
- 问题：IPTV 卡片不显示源是否可播放，用户盲点常遇「源不可用」。
- 旧逻辑：无可用性探测，卡片无状态角标。
- 新逻辑：
  - `useIPTVStore` 新增 `channelAvailability` / `setChannelAvailability`（按 channel id 存 `available|unavailable|unknown`）；
  - IPTV 页进入后后台静默检测前 50 个频道（HEAD/轻量探测），路由离开时 `abort`；卡片角标合并回退（检测中显示 unknown，失败显示 unavailable）。
- 涉及文件：`src/stores/useIPTVStore.ts`、`src/pages/IPTV/index.tsx`、`src/components/IPTVChannelCard/*`
- 构建：`npm run build` 通过；`IPTV-080~082` 用例通过。

---

## 7. IPTV 卡片角标下移（LIVE 在上，availability 在下）— 观感类

- 日期：2026-09-02
- 问题：`.iptv-card-cover__badges` 横向排列并被红色容器底包裹，availability 与 LIVE 同级等高、无层级，红底与 LIVE 红底重复。
- 旧逻辑：`src/components/IPTVChannelCard/IPTVChannelCard.css` 中 `.iptv-card-cover__badges` 为 `display:flex`（横向）+ `background:var(--color-danger)`。
- 新逻辑（`IPTVChannelCard.css:123`~`:133`）：`flex-direction: column` + `gap: var(--space-2xs)` + 去容器红底（LIVE 自身带红底）；`.availability-badge` 落在 `.record-card__live-badge` 正下方并保留纵向间距。
- 涉及文件：`src/components/IPTVChannelCard/IPTVChannelCard.css`、`src/components/IPTVChannelCard/IPTVChannelCard.tsx`
- 关联 Demo：`demos/demo-iptv-badge-layout-2026-09-02.html`（旧↔新布局对比）
- 构建：`npm run build` 通过。

---

## 8. 删除 IPTV 代理调试日志

- 日期：2026-09-02
- 问题：`[IPTV 代理调试]` 标签的 `console.debug`（构造播放地址等）随包输出，属测试残留。
- 旧逻辑：`useIPTVChannelInit.ts` / `iptvService.ts` 多处 `console.debug('[IPTV 代理调试] …')`。
- 新逻辑：全部删除（共 7+ 处），`grep '[IPTV 代理调试]'` 全仓无残留。
- 涉及文件：`src/hooks/useIPTVChannelInit.ts`、`src/services/iptvService.ts`
- 构建：`npm run build` 通过。

---

## 9. 历史/收藏页：CMS 源未启用时拦截跳转（居中 Modal）

- 日期：2026-09-02
- 问题：历史/收藏页（直链搜索后收藏）点卡片跳 `/play`，若所选 CMS 源未在设置勾选启用，跳转后无可用源、白跳。
- 旧逻辑：RecordCard/VideoCard 直接 `<Link>` 跳转，无源启用校验。
- 新逻辑：
  - 新增 `src/hooks/useCmsSourceGuard.ts`：基于持久化 `settings.videoSourceIds`（` :30`）判源是否启用，返回 `{blocked, sourceName, modalVisible, ...}`；用持久化启用列表而非运行时 sourceManager bootstrap，避免单独进历史/收藏页误拦。
  - 新增 `src/components/common/CmsSourceBlockedModal.tsx` + `.css`：Radix Dialog 居中弹窗，提示源名 +「前往设置」。
  - `CollectionRecord` 扩 `cmsSourceId`/`cmsSourceName`；`VideoCard` 收藏时经 `getVideoSources()[sourceIndex]` 异步解析写入。
  - `RecordCard`/`VideoCard` 加 `onBeforeNavigate?: () => boolean`，返回 `false` 时 `preventDefault` 拦截 Link 导航（`HIS-016` / `COL-016`）。
- 涉及文件：`src/hooks/useCmsSourceGuard.ts`、`src/components/common/CmsSourceBlockedModal.tsx`、`.css`、`src/components/RecordCard/RecordCard.tsx`、`src/components/VideoCard/VideoCard.tsx`、`src/pages/History/index.tsx`、`src/pages/Collections/index.tsx`、`src/stores/useUserStore.ts`、`src/types/store.ts`
- 构建：`npm run build` 通过；Vitest 383 通过；history/collections/iptv E2E 全绿。

---

## 10. 播放页侧栏骨架抖动（variant 判定随 TMDB 响应突变）

- 日期：2026-09-02
- 问题：>1280px 进 play 页播无缓存视频，骨架先按 movie（2 面板）渲染，TMDB 剧集详情返回后突变 tv（3 面板），肉眼抖动；用户感知「等 TMDB 响应后才有右边距 / 总宽度随栏目数变化」。
- 旧逻辑：`Player/index.tsx` 侧栏变体判定 `seasons.length > 1 ? 'tv' : 'movie'`，且多处复用；tmdb-tv 无缓存时 `d=null` → `seasons=[]` → 骨架 movie，TMDB 返回瞬间突变 tv。
- 新逻辑（`Player/index.tsx:620`~`:622`）：统一变体判定
  ```ts
  const hasSeasonPanel = cmsSeasons.length > 1 || (cmsSeasons.length === 0 && id?.startsWith('tmdb-tv-'));
  const playerVariant: 'tv' | 'movie' = hasSeasonPanel ? 'tv' : 'movie';
  ```
  替换全部 6 处 `seasons.length > 1 ? 'tv' : 'movie'` + 3 处 `seasonAvailable={seasons.length > 1}`。tmdb-tv 骨架入场即 tv，TMDB 响应零变化。
- 涉及文件：`src/pages/Player/index.tsx`
- 构建：`npm run build` 通过；新增 PLAYER-090（tmdb-tv 无缓存进入 → 骨架入场即 tv 3 面板、响应前不突变），`scripts/player.spec.ts` 全绿。
- 提交：`ae295ab`

---

## 11. 播放页「右边距变化」真根因 = 滚动条槽位未预留

- 日期：2026-09-02
- 问题：上一轮只治了 variant 突变，用户反馈骨架阶段仍无右边距、TMDB 返回后才挤出右边距。
- 旧逻辑：桌面端真正滚动容器 `.player-page`（`height:100dvh; overflow-y:auto`）未预留滚动条槽位；骨架不溢出 → 无滚动条 → 只 6px padding；TMDB 详情入列后溢出 → 滚动条出现 → clientWidth 瞬间窄 15px → `.player-main`（margin 出血 + padding）左移 15px。headless 默认 `--hide-scrollbars` 掩盖此问题。
- 新逻辑（`Player.css:1104`，`@media (width>=1280px)` 块内 `.player-page` 加 `scrollbar-gutter: stable`，附根因注释）：槽位从入场第一帧存在，两阶段几何一致；顺带免疫展开全文/面板折叠跨阈值抖动。
- 涉及文件：`src/pages/Player/Player.css`
- 构建：`npm run build` 通过；PLAYER-091 断言 `.player-page` computed `overflowY==='auto' && scrollbarGutter==='stable'`（反向验证撤掉 `scrollbar-gutter` 后该用例红）。
- 提交：`f4633bb`（防回归用例）+ `ae295ab`（修复单文件 +9 行）

---

## 12. 播放页 VOD「源不可用」自动故障转移（PLAYER-094）

- 日期：2026-09-02
- 问题：历史视频短时能看、久放报「源不可用」。根因实测推翻「签名过期」假设——28 源 17 可达、0 带签名、URL 全静态（`/20260902/<hash>/index.m3u8`），真因 CDN 临时目录实效/IP 限流；重拉 CMS 取不到新签名。
- 旧逻辑：VOD 播放中只 toast 不回调页级 `onError`，吞掉 mid-playback 致命错误（正是用户场景）。
- 新逻辑：
  - `UniversalPlayer.tsx`：`mode==='video'` 致命错误仍 `setHasError + onError?.(error)` 上报；切源成功后 `isPlaying && hasError` 自动清覆盖层。
  - `Player/index.tsx` 新增 `handlePlayerError`（` :248`）：Step1 同源 `usePlayerStore.sources` 下一未尝试线路 → Step2 跨 CMS 源（复用 `cmsResults` 跳过当前/已尝试）→ Step3 全失败保留覆盖层；`MAX_FAILOVER_ATTEMPTS = 16`（` :240`）+ `tried:Set` 去重 + 视频 id 变化复位 `failoverRef`（` :321`）；`<UniversalPlayer onError={handlePlayerError} />`（` :1030`）。
  - `scripts/fixtures/cms-mock.ts` 增 `setCmsSearchResponse()` 覆盖出口 + `fulfillHls` 加 `Access-Control-Allow-Origin:*`。
  - 新增 `scripts/player-failover.spec.ts`：双线路电影（线路1 死 403 / 线路2 活），断言先请求死线、自动切活线、覆盖层最终清除。
- 涉及文件：`src/components/UniversalPlayer/UniversalPlayer.tsx`、`src/pages/Player/index.tsx`、`src/services/cms-mock`（fixtures）
- 构建：`npm run build` 通过；`player.spec.ts` 31 全绿 + `player-failover.spec.ts` 绿。
- 提交：`1f86d02`
- 踩坑：`toHaveCount(0)` 作「等待」是错的（0 是初始合法态会立即通过）；须 `expect.poll(() => seen.includes('cms-mock.local/stream'), {timeout:30000}).toBeTruthy()` 真等转移完成。

---

## 13. 跨页签 IDB 一致性四疑点核实与修复

- 日期：2026-09-02
- 问题：① 收藏跨页签随机主键致双行；② 历史 `flush` 盲写旧脏记录覆盖另一页签进度；③ `initDB` 超时叠层 + 提示打错页签；④ 旧连接 versionchange 未被放行。
- 旧逻辑：`CollectionRecord.id = col-{ts}-{rand}` 随机主键（页面以 videoId 为 key，概念打架）；`addHistory` flush 直接写；`initDB` 每次失败重试 `new openDB` 叠超时 + 留孤儿连接；`blocking()` 未自动 close。
- 新逻辑（`useUserStore.ts`）：
  - ① id 收敛确定性 `col-{videoId}`（` :298`），`addCollectionRecord` 幂等覆盖；`getCollections` 惰性收敛遗留随机 id/双记录。
  - ② `upsertHistoryRecords(records)`（`:169`）单事务写前读 DB、仅 `updatedAt` 较新才写。
  - ③④ 单飞 Promise（并发共享一次 openDB，失败清空可重试）+ 迟到成功连接即时回收 + `blocking()` 自动 close 放行。
- 涉及文件：`src/stores/useUserStore.ts`、`src/services/database.ts`、`src/services/database.test.ts`（新增 7 例）、`scripts/collection-cross-tab.spec.ts`（COL-CROSS-001）
- 构建：Vitest 361 全绿；playwright 273 条。
- 提交：`178387b`

---

## 14. 跨页签内存快照实时同步（BroadcastChannel）

- 日期：2026-09-02
- 问题：DB 层幂等后，另一页签增删不更新本页内存快照（须手动刷新）。
- 旧逻辑：写操作同步发广播，接收端 reload 读旧库（竞态）；频道每次操作新建导致泄漏。
- 新逻辑（`useUserStore.ts`，IndexedDB 落库后才广播）：
  - `CROSS_TAB_CHANNEL = 'kinotv-userdata'`（` :30`）；写操作落库成功后 `postUserDataChange(scope)`（` :60`，历史挂 `flushHistoryRecords` 落库成功后、收藏/删除 `.then(() => postUserDataChange(...))`）。
  - 接收端（` :45`）`new BroadcastChannel` 幂等只建一次；随机 session 过滤自消息；`reload()`（` :54`，只读不写不二次广播，防回环）；`MODE!=='test'` 禁用。
- 涉及文件：`src/stores/useUserStore.ts`、`scripts/user-cross-tab.spec.ts`（USER-CROSS-001）
- 构建：Vitest 361 + 相关 E2E 19 passed；计数表 274 条 / 22 spec。
- 提交：`7ec0223`
- 踩坑：COL-CROSS-001 改用同步 DOM `.click()`（Playwright `.click()` 带 actionability 等待会把第二次点击推到广播后，语义变「取消收藏」→ DB 0 条）；`page.evaluate` 内函数不能用模块级常量。

---

## 15. reload 合并脏集合，防广播回退本页进度

- 日期：2026-09-02
- 问题：接收端 `reload()` 无条件用 DB 快照覆盖内存，本页处于 3s 节流窗口（进度每秒更新未落库）的脏记录被回退、首播未 flush 记录从内存消失。
- 旧逻辑：`reload()` 直接 `set(state)` 整快照覆盖。
- 新逻辑（`useUserStore.ts`）：`reload()` 合并 `historyDirtyRecords`——快照已有按 id 覆盖为内存值，快照没有 append；本地变更以内存为准（last-write-wins 本地优先）。
- 涉及文件：`src/stores/useUserStore.ts`
- 构建：tsc + vite build + vitest 361 + 相关 E2E 19 passed 全绿。
- 提交：`8b9a53f`
- 踩坑（假绿典型）：原 `expect.poll(...).toEqual({42,5})` 抢跑（B 造脏那一刻内存就是目标值，poll 第一次采样在 reload 覆盖前已通过 → 反向验证仍绿）；正确顺序先 poll 广播快照落地（noise 出现=reload 已执行），再断言脏值保留。

---

## 16. reload 脏集合合并补纯单测（快 CI 回归层）

- 日期：2026-09-02
- 问题：脏进度保护只有 E2E（USER-CROSS-001）覆盖，无秒级纯单测层。
- 新逻辑（`useUserStore.test.ts` 新增 describe，DB 层 `vi.mock`）：覆盖① 节流窗口 progress 80 不被 DB 旧值 50 覆盖 ② 首播未 flush 记录 reload 后保留 ③ 无脏不误伤正常 reload（DB 新值 90 正常覆盖）。
- 涉及文件：`src/stores/useUserStore.test.ts`
- 构建：Vitest 361→364 全绿；tsc + vite build 通过。
- 提交：`79acfa4`
- 踩坑：`vi.mock('@/services/database')` 工厂漏 `upsertHistoryRecords`（复数）→ mock 后 undefined → flushHistoryNow 抛 TypeError → catch 恢复脏记录 → 边界用例莫名红；改 store DB 调用面须同步补全 mock 导出。

---

## 17. flushHistoryNow 同名混淆清理 + 注释修正

- 日期：2026-09-02
- 问题：`store 方法 flushHistoryNow: () => { flushHistoryNow(); }` 依赖闭包解析到模块级同名函数，易误读为无限递归。
- 新逻辑（`useUserStore.ts`）：模块级内部函数改名 `flushHistoryDirtyRecordsNow`（`:191`，仅全局监听 pagehide/visibilitychange 内部入口）；store 公开方法名保留（`:456`/` :457`）；修正跨页签同步注释（广播频率被 3s flush 节流约束，落库成功即通知）。
- 涉及文件：`src/stores/useUserStore.ts`
- 构建：tsc + vitest(11) + USER-CROSS-001 全绿。
- 提交：`b0815c7`

---

## 18. IPTV 收藏/播放历史跨页签实时同步（storage 事件）

- 日期：2026-09-02
- 问题：useIPTVStore 收藏/播放历史走 zustand persist 落 localStorage（键 `iptv-store`），同样存在「tab A 收藏/播放 → tab B 内存陈旧」割裂。
- 旧逻辑：仅 useUserStore（IndexedDB）做了跨页签同步；useIPTVStore 无 receiver。
- 新逻辑（`useIPTVStore.ts`）：localStorage 跨页签通知是浏览器原生 `window 'storage'` 事件（无需 BroadcastChannel），仅补 receiver：
  - `IPTV_PERSIST_KEY = 'iptv-store'`（` :17`）消除 clearCache removeItem / persist name / 监听键三处字面量漂移。
  - 常规写入 → `persist.rehydrate()`（`:536`，兼容 void|Promise<void）重读合并 + 按 `favoriteChannelIds` 重派生 `channels.isFavorite`。
  - `e.newValue === null`（` :549`，另一页签 clearCache）→ 收藏/播放历史/筛选归零 + isFavorite 全 false（保留 settings）；幂等守卫 + `MODE=test` 禁用 + 无回环（自身写入不触发自身 storage 事件）。
  - `initIPTVStoreCrossTabSync()`（` :543`）模块级 init（` :567`）。
- 涉及文件：`src/stores/useIPTVStore.ts`、`scripts/iptv-cross-tab.spec.ts`（IPTV-CROSS-001）
- 构建：IPTV-CROSS-001 一把过 2.0s（反向验证注释调用→场景2红→恢复绿）；回归 iptv.spec 14 + 跨页签 spec 16 passed；tsc + vite build 通过；计数表 275 条 / 23 spec。
- 提交：`f012aae`

---

## 19. 设置静态配置跨页签同步（白名单受控合并）

- 日期：2026-09-02
- 问题：useSettingsStore（AES-GCM 随机 IV 加密）无跨页签同步；且随机 IV 加密不能复用 rehydrate 方案（密文每次不同 → 敏感字段非空时无限事件循环 + 双重加密污染）。
- 旧逻辑：iptv-store 用「storage 事件 → rehydrate」即可；useSettingsStore 敏感字段加密，rehydrate 必死循环。
- 新逻辑（`useSettingsStore.ts`，不复用 persist 通道）：
  - storage 事件 → 直接解析 `e.newValue` 载荷 → 白名单逐键「解密/JSON 比对 → 值变才 setState」；接收端没变不 set → 不写回 → 环自动断。
  - `CROSS_TAB_EXCLUDED_KEYS = {tvMode, tvOverscan}`（`:315`，播放布局类每页签保留自己值不参与收敛）。
  - `mergeSettingsFromCrossTab`（`:318`，`export async`）敏感字段先 `decryptText`（`:333`）再比对；`newValue===null` 忽略。
- 涉及文件：`src/stores/useSettingsStore.ts`、`scripts/settings-cross-tab.spec.ts`（SETTINGS-CROSS-001）
- 构建：SETTINGS-CROSS-001 双 page 8.4s 一把过（反向验证注释 init→超时红→恢复绿）；回归 29 passed；vitest settings 4 全绿；tsc + vite build 通过；计数表 276 条 / 24 spec。
- 提交：`801c179`
- 通用规则：localStorage 里 store 能否用 rehydrate，取决于写盘是否确定性（明文→安全 / 加密随机 IV→必须手工受控合并）。

---

## 20. mergeSettingsFromCrossTab 补纯单测（快 CI 回归层）

- 日期：2026-09-02
- 问题：第十轮 merge 逻辑只有 E2E 覆盖，无快 CI 纯单测层。
- 新逻辑（`useSettingsStore.test.ts` 新增 describe「跨页签白名单受控合并」5 例）：① 白名单常规键合入 ② 敏感字段密文→decryptText 明文合入 ③ 排除键 tvMode/tvOverscan 不覆盖本页值、白名单键照常同步 ④ **无变化不 set**（`subscribe` spy 断言不触发，防回环根基，E2E 测不出）⑤ 空敏感值/畸形载荷安全跳过。
- 涉及文件：`src/stores/useSettingsStore.test.ts`
- 构建：Vitest 369 passed / 30 files（364→369）；`npm run build` 通过。
- 提交：`257c60d`

---

## 21. wd 双重编码 + 后台播放冻结/意外暂停修复

- 日期：2026-09-02
- 问题：① wd 源带字面量 `%`（多出的编码层泄漏到目标站）；② 后台页签定时器被 Chrome 强节流 → hls.js 加载停滞/fatal 误报 → 自动故障转移级联（误切线路/意外暂停）→ 冻结后无法续播。
- 旧逻辑：① httpClient 响应拦截器重试复用 `error.config`，其 url 已被请求拦截器包装成「代理前缀+encodeURIComponent(原始URL)」，重试再进请求拦截器二次包装；② 原 `onVisibility` 对 `video.error/paused` 直接 return 不恢复。
- 新逻辑（`httpClient.ts` + 播放器）：
  - ① 请求拦截器 `url.startsWith(proxyPrefix)` 判重跳过（`:101`），service 层 `wd=${encodeURIComponent(title)}` 单编码全部正确。
  - ② adapter.resume()（HLS：resetErrorCount+startLoad 可重入）；`onVisibility` 对 readyState<3/video.error 且未暂停时 resume+play；UniversalPlayer `onError` 后台挂起 `pendingErrorRef`，visible flush 时流已自愈则丢弃。
- 涉及文件：`src/services/httpClient.ts`、`src/components/UniversalPlayer/adapters/*`、`src/components/UniversalPlayer/UniversalPlayer.tsx`、`worker/cors-proxy`
- 构建：Vitest 相关全绿；单测锁 httpClient 重试前后 config.url 一致。
- 提交：`4eb252b`

---

## 22. 窗口级失焦播放冻结修复

- 日期：2026-09-02
- 问题：切到其他软件（浏览器整个失焦，非页签切换）→ 返回画面冻结但音频正常 → 概率性报错重试无效。根因：窗口失焦时 `visibilityState` 恒 `'visible'`，`visibilitychange` 不触发 → 上一轮挂它上面的恢复全部失效；失焦期间帧呈现停止，MSE 背压诱发 hls.js fatal 误报 → 故障转移级联；errorCount 打满引擎停转但 readyState≥3。
- 旧逻辑：恢复链只挂 `visibilitychange`。
- 新逻辑（`usePlayerCore.ts`）：恢复链抽 `recoverForeground`（`:262`）→ `window focus/blur`（`:287`/`:288`）与 `visibilitychange` 双入口（focus 需失焦 >1s 防 alt-tab 微抖 `:283`）；`recoverForeground` **无条件** `adapter.resume()`（`:269`，引擎死转时 readyState 判定会漏）；错误挂起判定扩到 `!document.hasFocus()`，flush 同挂 `visibilitychange` + `window focus`；UniversalPlayer `:457` 注释补齐 `visibilityState` 恒 visible 必须用 `hasFocus()`。
- 涉及文件：`src/components/UniversalPlayer/hooks/usePlayerCore.ts`、`src/components/UniversalPlayer/UniversalPlayer.tsx`
- 构建：`npm run build` 通过；相关 E2E 全绿。
- 提交：`925f3f2`
- 教训：可见性类恢复判定入口不能只看 `visibilityState`，要区分「页签不可见」与「窗口失焦」两种失活形态。
