# 2026-08-20

## 角标配色统一 + 收藏卡移动端布局 + 首页年份缺失 + 暗色 chip 文字可读性

- 日期：2026-08-20
- 问题：① IPTV 卡左上角角标组（LIVE）背景色用了 `var(--color-danger)` 但该变量未在 `variables.css` 定义 → 声明在计算值阶段失效，LIVE 角标实际无背景；② 收藏页 video card 左下角「年份+类型」徽标背景是半透明深灰，与 IPTV 角标风格不统一；③ 收藏卡在移动端类型仍挤在左下角、观看状态与其并排，空间局促；④ 首页 TMDBMovieRow 竖版卡片封面左下角缺年份；⑤ 收藏/历史页暗色模式下选中 chip（融合 Tab 的计数徽标）文本白色叠在白色 primary 背景上，完全看不清。
- 旧逻辑：
  - `variables.css` 两套主题均无 `--color-danger`（仅 `index.css` LIVE 角标与 `Settings.css`（带 fallback）引用）→ 无 fallback 的声明静默失效，LIVE 角标透明。
  - `.video-card-badge-item`（收藏页 status 分支的年份/类型徽标）`background: rgba(0,0,0,0.72) + backdrop-filter`；status 分支桌面/移动同布局（年份+类型合并左下、状态右下）。
  - `TMDBMovieRow/index.tsx` `toVideo()` 硬编码 `year: undefined`，丢弃 TMDB 数据中的年份 → 首页全部竖版卡左下角无年份。
  - `StatusTabs.css` `.status-tab--active .status-tab__count` 文字色硬编码 `#fff`；暗色下 `--color-primary` 反转为白 → 白字白底。
- 新逻辑：
  1. **定义 `--color-danger: #ef4444`**（亮/暗两套主题，与 Settings.css 的 fallback 一致），LIVE 角标、收藏徽标统一引用，一处改全局。
  2. **IPTV 卡**：`.iptv-card-cover__badges` 容器补 `background: var(--color-danger) + padding + border-radius`，左上角 LIVE 角标成红色胶囊。
  3. **收藏页 video card**：`.video-card-badge-item` 背景改 `var(--color-danger)`（白字、去掉不再需要的 backdrop-filter）。
  4. **收藏卡移动端重排（status 分支）**：`.video-card-badges-bl` 撑满底部宽度（left+right 双锚、`max-width:none`），type 徽标 `margin-left:auto` 靠右下；观看状态 `bottom` 按类型徽标高度（`space-xs + space-xs + 2×space-3xs + text-xs×1.3`）叠到类型上方；桌面布局一字未动。含 `html[data-device="app"]` 恒移动副本。
  5. **首页年份**：`TMDBMovieRow.toVideo()/getVideo()` 类型与取值补 `year`，竖版卡片左下角年份恢复（横版 continue 卡无 year 字段，取 undefined，CSS 本就隐藏年份，零影响）。
  6. **暗色 chip 文字**：`.status-tab--active .status-tab__count` 文字色改 `var(--color-text-inverse)`（亮色下仍是白字、暗色下转深色），与 `action-btn.is-active`/`record-filter-chip.is-active`/`record-edit-btn--active` 同款处理。
- 涉及文件：`src/assets/styles/variables.css` / `src/components/IPTVChannelCard/IPTVChannelCard.css` / `src/components/VideoCard/VideoCard.css` / `src/components/StatusTabs/StatusTabs.css` / `src/components/TMDBMovieRow/index.tsx`
- 验证：`npm run build` 通过；`pnpm exec playwright test` 跑 collections(6)+history(11)+iptv(13)+browse(24) 全过；HOME-060 在改动前的基线提交上同样失败，属既有 flaky（30ms 采样时序），与本次改动无关。
- 关联 Demo：无（角标/布局/配色类改动，视觉结果已由各页 E2E 的 DOM/结构断言覆盖；如需预览可跑 `npm run dev` 打开收藏页/IPV 页对照）

## 六项零散 UI 修复（进度条 / 季卡海报 / bio 展开 / 设置深链闪烁 / 停用源延迟 / 设置标题图标）

- 日期：2026-08-20
- 问题：① 历史页视频卡播放进度条过细（原为底边 2px 细线）且无时间文本，进度感弱；② 详情页季卡海报在移动端被拉扁（宽度误用滑块手柄变量 `--comp-thumb-size`，仅 14-28px）；③ 人物页展开「简介」后 hero 高度不随内容增长，bio 被 `max-height + overflow:hidden` 裁掉，且「收起」按钮因不可见无法再折叠；④ 设置页顶部栏点击「个人资料」图标时先闪一下「外观」再切到「个人设置」（深链 effect 在 paint 后才 set 初值）；⑤ 源管理停用源仍显示测速延迟列，造成「停用仍在线」错觉；⑥ 个人设置「配置管理 / 恢复默认配置」标题无图标、且个人资料卡无标题行，与其它设置区块风格不统一。
- 旧逻辑：
  - 进度条：`RecordCard.css` 底边 `2px` 细条，无已看时间/总时长文本。
  - 季卡海报：`.detail-season-poster` 宽高均为 `var(--comp-thumb-size)`（该变量是滑块手柄尺寸，移动端 14-28px），且 `align-items` 未显式设置导致 `stretch` 拉伸。
  - bio 展开：`.person-hero` 有 `max-height: min(50vh, …) + overflow:hidden`，展开仅改内文 `-webkit-line-clamp`，父级高度不增长 → 内容裁剪、折叠按钮不可见。
  - 深链闪烁：`activeTab`/`mobileSubPage` 初值硬编码 `'appearance'`/`null`，深链 effect 在 paint 后才 `setActiveTab` → 首帧渲染「外观」，闪一下再切。
  - 停用源延迟：延迟状态块（测速中/超时/分级延迟）无条件渲染。
  - 设置标题：`PersonalTab` 两个标题为纯文本；`ProfileHeader` 无标题行；`Settings.css` `.settings-card__title` 无 flex 布局。
- 新逻辑：
  1. **进度条**：`RecordCard.tsx` 新增 `formatClock` + `progressLabel`（pct≥90 显示「已看完」，否则 `MM:SS/总时长`），JSX 重构为 `.record-card__progress-overlay > .record-card__progress-bar-wrap > .record-card__progress-bar + .record-card__progress-text`；`RecordCard.css` 进度条高度 `var(--space-xs)`、填充 `#e11d48`、半透明轨道、时间文本右下角带 text-shadow，与横版 VideoCard 风格一致。
  2. **季卡海报**：`Detail.css` 的 `@media (width <= 767px)` 与 `html[data-device="app"]` 恒移动副本中：`.detail-season-card { align-items: flex-start; }` + `.detail-season-poster { width: clamp(3rem, 2.5rem + 4vw, 4.5rem); }`（与 `.detail-source-thumb` 同款流式公式）。
  3. **bio 展开**：`Person/index.tsx` hero 根加条件类 `person-hero--expanded`；`Person.css` 追加 `.person-hero.person-hero--expanded { max-height: none; }` 与 `[data-device="tv"]` 同款覆盖（CSS 无法选中父级，需 React 标记）。
  4. **深链闪烁**：`Settings/index.tsx` 新增 `getDeepLinkTab()` 惰性初始化 `activeTab`/`mobileSubPage`（首帧即正确），深链 effect 改 `useLayoutEffect`（Keep-Alive 二次进入也在 paint 前切换）。
  5. **停用源延迟**：`SourceManager.tsx` 延迟状态块外层包 `item.status.enabled &&`；启用后仍显示上次测速结果（改回 enabled 时天然带旧值）。
  6. **设置标题图标**：`PersonalTab.tsx` 导入 `Settings2`/`RotateCcw` 给「配置管理/恢复默认配置」加图标；`ProfileHeader.tsx` 新增「个人资料」标题行 + User 图标；`Settings.css` `.settings-card__title` 改 flex 布局 + `.settings-card__title-icon` / `.settings-profile-card__title(-icon)` 图标 `var(--color-primary)`，padding 与下方按钮对齐。
- 涉及文件：`src/components/RecordCard/RecordCard.{tsx,css}` / `src/pages/Detail/Detail.css` / `src/pages/Person/index.tsx` + `Person.css` / `src/pages/Settings/index.tsx` + `Settings.css` + `ProfileHeader.tsx` + `tabs/PersonalTab.tsx` / `src/components/SourceManager/SourceManager.tsx`
- 验证：`npm run build` 通过（EXIT=0）；stylelint 全量 383 错误与改动前基线完全一致（新增零错误；本次 CSS 属性顺序已按 order/properties-order 排齐）；eslint 改动文件零新增（Person/index.tsx L146 的 exhaustive-deps 为基线既有）；playwright 跑 history(11)+collections(6)+person(8)+detail(21)+settings(25) 全过。HOME-060 仍为基线既有 flaky，与本次改动无关。
- 关联 Demo：无（布局/配色/进度类改动，视觉结果已由各页 E2E 的 DOM/结构断言覆盖；如需预览可跑 `npm run dev` 打开历史页/详情页/人物页/设置页对照）