# 2026-08-10 改动记录

> v1.8.0 收尾：Browse 移动端筛选区重设计 + Settings 移动端设置页整改 + 测试覆盖 + 文档同步。
> UI 主体改动分布于此前多次提交（afd025e / 6682e48 / a837297 / 0d93041 / 3ab9eec / 7d2365e 等），
> 本日新增对应 E2E 测试用例、校准 AGENTS.md 测试计数表、当前化过时注释、补 KNOWLEDGE.md v1.8.0 条目。

## Browse 移动端筛选区重设计（S3 定稿）+ 测试覆盖
- 日期：2026-08-10
- 问题：移动端筛选区单行命令栏信息密度低、预设横滚冗余、筛选即时触发导致面板内每改一项都发请求。
- 旧逻辑：命令栏单行（模式段 + 筛选按钮 + 结果数横向排布）；`.bmb-presets` 预设横滚常驻；FilterBar `onChange` 即时触发 TMDB 请求；面板顶栏仅「标题 + 关闭」。
- 新逻辑：
  - 命令栏改两行：第一行 `.bmb-mode-row` 模式段居中（`justify:center`），第二行 `.bmb-bar-row`「筛选」小按钮（28px）+ 结果数两端对齐（`space-between`）。
  - 移除 `.bmb-presets` 预设横滚与面板内 `.bmb-rec` 推荐卡（对齐 S3 HTML 示例）；`.bmb-rail` 已选轨无左右 padding。
  - 移动端隐藏 `.browse-sort-bar`（排序入弹窗第 5 分组、结果数入命令栏）。
  - 筛选面板改**完成制**：FilterBar 用 `draft` 草稿值（打开时从 `value` 快照），面板内改条件零请求；点「完成」才 `onChange(draft)`；「重置」重置草稿；返回丢弃草稿。
  - Drawer 新增 `fullscreen`/`onReset` prop：全屏覆盖（`inset:0`）+ 顶栏三栏（左返回箭头 / 中标题居中 / 右重置），动画 `drawer-pg-in`。
- 涉及文件：`src/pages/Browse/BrowseMobileBar.css` / `src/pages/Browse/Browse.css` / `src/components/ui/Drawer.tsx` / `src/components/FilterBar/*`（此前提交）；`scripts/browse.spec.ts`（本日新增 BROWSE-078/079/080）
- 关联 Demo：无独立 Demo（通过 E2E BROWSE-078/079/080 + 实机验收）
- 构建：`npm run build` 通过（此前提交时验证）

## Settings 移动端设置页整改（iOS 分组卡 + 子页全屏顶栏）+ 测试覆盖
- 日期：2026-08-10
- 问题：移动端设置主页菜单平铺无分组、子页沿用全局导航栏导致层级不清、`.list-item` 移动端行式与桌面卡片化不一致。
- 旧逻辑：移动端菜单项平铺；子页在 `.app-shell__scroll` 的 `contain:layout` 包含块内渲染，导航栏常驻；`.list-item` 移动端不卡片化（方案 F 承诺「移动端不卡片化」一刀切）。
- 新逻辑：
  - 入口菜单按 iOS 分组圆角卡组织：`.settings-menu-group`（「通用」4 项 +「账户与信息」2 项），保留 `.settings-menu-item` 类名兼容 SET-090。
  - 子页改用 `createPortal` 挂到 `document.body`（脱离 `contain:layout` 包含块），`position:fixed; inset:0` 覆盖全视口，顶栏（返回/居中标题/右占位）与全局导航栏同高（`--header-height-compact` 48px）视觉替代导航栏。
  - 子页内 `.list-item` / `.settings-profile-card` / `.source-manager` 卡片化（对齐桌面方案 F，间距 `--space-lg`）；入口菜单（`.settings-content`）保持行式不变。
  - `.settings-page:has(.settings-subpage)` 移除 page-transition-enter 残留 transform，避免二次进入闪烁。
- 涉及文件：`src/pages/Settings/Settings.css` / `src/pages/Settings/index.tsx` / `src/pages/Settings/hooks/*`（此前提交）；`scripts/settings.spec.ts`（本日新增 SET-092/093）；`src/components/ui/Drawer.tsx`（注释当前化）
- 关联 Demo：无独立 Demo（通过 E2E SET-092/093 + 实机验收）
- 构建：`npm run build` 通过（此前提交时验证）

## AGENTS.md 测试计数表校准（2026-08-05 → 2026-08-10）
- 日期：2026-08-10
- 问题：AGENTS.md「测试依赖映射」表的 test 数自 2026-08-05 校准后漂移——home（35→40，+5）、browse（21→24，+3）、settings（19→24，+5，含此前未同步的 SET-080~087），导致「精准跑测试」指引数字失真。
- 旧逻辑：页表 home=35 / browse=21 / settings=19；共享组件聚合表 VideoCard=97 / SearchBox=37 / FilterBar=21 / HeroBanner=51 / Toast=19 / tmdbService=85 / videoService=33 / useTMDBStore=77 / useSettingsStore=24；校准日期 2026-08-05。
- 新逻辑：用 `npx playwright test --list | Select-String "›" | Measure-Object` 实测重算——页表 home=40 / browse=24 / settings=24；共享组件聚合表 VideoCard=105 / SearchBox=40 / FilterBar=24 / HeroBanner=56 / Toast=24 / tmdbService=93 / videoService=36 / useTMDBStore=85 / useSettingsStore=29；校准日期 2026-08-10。
- 涉及文件：`AGENTS.md`（L274 校准日期 + L278/279/283 页表三行 + L296~309 共享组件聚合表九行）
- 关联 Demo：无（文档类改动）
- 构建：不涉及（纯文档）

## 注释当前化（Drawer / Settings.css）
- 日期：2026-08-10
- 问题：v1.8.0 改动后部分注释与实现不符。
- 旧逻辑：`Drawer.tsx` onReset 注释写「顶栏右侧重置按钮（位于关闭按钮左侧）」——但 fullscreen 模式下顶栏是三栏（左返回/中标题/右重置），无「关闭按钮左侧」语义；`Settings.css`「方案 F 承诺：移动端不卡片化」一刀切描述与子页内卡片化矛盾。
- 新逻辑：`Drawer.tsx` onReset 注释改为「顶栏三栏布局——左返回箭头、中标题、右重置」；`Settings.css` 方案 F 注释加「入口菜单场景」限定 + 子页内卡片化交叉引用。
- 涉及文件：`src/components/ui/Drawer.tsx` / `src/pages/Settings/Settings.css`
- 关联 Demo：无（注释类改动）
- 构建：不涉及（纯注释）
