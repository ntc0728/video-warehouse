# 4. 播放页 (Player)

> 由 `docs/TEST-CASES.md` 拆分而来（2026-09-09 文档瘦身）。原文件已改为索引页，详情在此；修改时两处同步。

## 4. 播放页 (Player)

**路由**: `/play/:id`
**核心组件**: UniversalPlayer + PlayerSidebar (CMS面板 + 季面板 + 选集面板)
**配置依赖**: CMS 源加载需 Level 2（Token + CORS 代理）；IPTV 播放需 Level 3（全配置）

### 4.1 页面加载

| 编号 | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| PLAYER-001 | 正常加载（CMS 视频） | 输入 CMS 视频 ID | 访问播放页 | 显示全屏 loading → 播放器 + 侧边栏 |
| PLAYER-002 | 正常加载（TMDB 视频） | 输入 `tmdb-movie-xxx` | 访问播放页 | 显示 loading → TMDB 详情 + CMS 搜索 → 播放器 |
| PLAYER-003 | 首次 loading | 进入播放页 | 查看页面 | 显示全屏 AppLoading "加载中…"，直到 hasLoadedOnce=true |
| PLAYER-004 | TMDB 视频 loading | TMDB 视频，CMS 搜索中 | 查看播放器区域 | 播放器区域显示 loading spinner |
| PLAYER-005 | 无视频数据 | 输入有效 ID 但 CMS 无匹配 | 查看播放器区域 | 显示 VideoOff 图标 + "暂无数据" + "请尝试切换其他 CMS 源" |
| PLAYER-006 | API 请求失败 | 网络异常 | 访问播放页 | 显示 AlertTriangle + "某些接口请求失败" + "请稍后重试" + 返回按钮 |

### 4.2 播放器控制

| 编号 | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| PLAYER-010 | 播放/暂停 | 视频正在播放 | 点击播放器 | 视频暂停，显示播放按钮 |
| PLAYER-011 | 恢复播放 | 视频已暂停 | 点击播放器 | 视频继续播放 |
| PLAYER-012 | 进度条拖动 | 视频正在播放 | 拖动进度条 | 视频跳转到对应时间点 |
| PLAYER-013 | 音量控制 | 视频正在播放 | 调节音量 | 音量变化，音量记忆功能生效（如开启） |
| PLAYER-014 | 全屏切换 | 视频正在播放 | 点击全屏按钮 | 播放器进入/退出全屏模式 |
| PLAYER-015 | 返回按钮 | 播放页已加载 | 点击播放器 header 的返回按钮 | 回退到详情页（useSmartBack） |
| PLAYER-016 | 缓冲中暂停可点 | 视频缓冲中 | 点击暂停按钮 | 可正常点击暂停（P0③：`disabled` 仅在缓冲且未播放时生效） |
| PLAYER-017 | 缓冲中禁拖进度条 | 视频缓冲中 | 鼠标移入进度条并拖动 | 不触发 seek（P1④：`beginDrag` 缓冲守卫） |
| PLAYER-018 | 加载失败不显播放图标 | 视频加载失败/切源失败 | 观察播放器中间 | 不闪现中间播放按钮（P1⑤：条件含 `!isPlayerLoading`） |
| PLAYER-019 | 集数切换提示 | 多集剧集 | 切换集数 | 右上角提示「已切换到第 N 集」（replace 覆盖线路名误报） |
| PLAYER-01A | 进度恢复提示 | 有观看历史 | 进入播放页自动恢复 | 提示「已自动跳转到上次观看的位置」 |
| PLAYER-01B | 控制栏图标 hover 跳动 | 视频播放中 | hover 底栏按钮图标 | 图标切换为近似变体（两套图标层叠、opacity/transform 过渡，不卡顿） |
| PLAYER-01C | 下一集预加载（仅 Wi-Fi） | 多集剧集、Wi-Fi 网络 | 播放当前集至 playing | 播放中约 300ms 后发起下一集 manifest + 首分片请求（浏览器网络面板可见），切集时命中缓存秒起播 |
| PLAYER-01D | 蜂窝网络不预加载 | 多集剧集、4G/蜂窝 | 播放当前集 | 不发起下一集预加载请求（isWifiConnection=false） |

### 4.3 选集与换集

| 编号 | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| PLAYER-020 | 选集面板显示 | 剧集视频，有集数数据 | 查看侧边栏 | 显示集数列表，当前播放集高亮 |
| PLAYER-021 | 切换集数 | 选集面板展开 | 点击另一集 | 播放器加载新集的视频源 |
| PLAYER-022 | 上一集/下一集 | 播放多集剧集 | 点击上一集/下一集按钮 | 切换到对应集数 |
| PLAYER-023 | 第一集禁用上一集 | 当前是第一集 | 查看上一集按钮 | 按钮禁用/隐藏 |
| PLAYER-024 | 最后一集禁用下一集 | 当前是最后一集 | 查看下一集按钮 | 按钮禁用/隐藏 |
| PLAYER-025 | 历史记录恢复选集 | 有观看历史 | 进入播放页 | 自动恢复到上次播放的集数和进度 |
| PLAYER-026 | 历史记录恢复线路 | 有观看历史（含 cmsSourceId） | 进入播放页 | 自动使用历史记录中的 CMS 源 |

### 4.4 季切换

| 编号 | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| PLAYER-030 | 季面板显示 | 剧集视频，有季数据 | 查看侧边栏 | 显示季列表，当前季高亮 |
| PLAYER-031 | 切换季 | 季面板展开 | 点击另一季 | 从季映射表查找对应集数，加载新季数据 |
| PLAYER-032 | 切换季保持集数 | 当前第 3 集 | 切换到另一季 | 尝试匹配同集数的集，匹配不到则从第 1 集开始 |
| PLAYER-033 | CMS 季数据优先 | CMS 返回季数据 | 查看季面板 | 使用 CMS 季数据，TMDB 季数据作为 fallback |

### 4.5 CMS 源管理

| 编号 | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| PLAYER-040 | CMS 面板显示 | TMDB 视频 | 查看侧边栏 | 显示 CMS 源列表，已选中的源高亮 |
| PLAYER-041 | 选择 CMS 源 | CMS 面板展开 | 点击某个 CMS 源 | 加载该源的视频数据，更新播放器 |
| PLAYER-042 | CMS 源加载中 | 正在获取 CMS 源数据 | 查看 CMS 面板 | 显示 loading 状态 |
| PLAYER-043 | CMS 源切换动画 | 切换 CMS 源 | 查看播放器 | 显示切换中的加载状态 |
| PLAYER-044 | 从详情页指定源 | 从详情页点击具体 CMS 源播放 | 进入播放页 | 直接使用指定的源（routeSourceIndex），侧边栏只读 |
| PLAYER-045 | CMS 面板折叠/展开 | 播放页已加载 | 点击 CMS 面板标题 | 面板折叠/展开 |

### 4.6 自动连播

| 编号 | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| PLAYER-050 | 自动连播倒计时 | 自动连播开启，播放到片尾 | 视频播放结束 | 显示"X 秒后播放下一集"倒计时 |
| PLAYER-051 | 立即播放下一集 | 自动连播倒计时中 | 点击"立即播放" | 立即切换到下一集 |
| PLAYER-052 | 取消自动连播 | 自动连播倒计时中 | 点击"取消" | 倒计时停止，不自动播放下一集 |
| PLAYER-053 | 自动连播关闭 | 设置中关闭自动连播 | 播放到片尾 | 不显示倒计时，播放结束 |
| PLAYER-054 | 最后一集无连播 | 当前是最后一集 | 播放到片尾 | 不显示自动连播倒计时 |

### 4.7 跳过片头/片尾

| 编号 | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| PLAYER-060 | 跳过片头 | 设置中开启跳过片头（90 秒） | 播放到 90 秒 | 自动跳过到 90 秒后，显示"已跳过片头"提示 |
| PLAYER-061 | 跳过片尾 | 设置中开启跳过片尾（30 秒） | 播放到结尾前 30 秒 | 自动跳到下一集或结束 |
| PLAYER-062 | 跳过提示显示 | 触发跳过 | 查看播放器 | 显示 SkipForward 图标 + "已跳过片头/片尾"文字，2 秒后消失 |
| PLAYER-063 | 手动跳过 | 跳过片头开启 | 点击跳过按钮 | 立即跳到片头结束位置 |

### 4.8 收藏与详情

| 编号 | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| PLAYER-070 | 收藏按钮 | 播放页已加载 | 点击收藏按钮 | 切换收藏状态，图标变化 |
| PLAYER-071 | 详情区域 | 播放页已加载 | 查看播放器下方 | 显示标题、评分、年份、时长、导演、演员、简介 |
| PLAYER-072 | 简介展开/收起 | 简介文本超长 | 点击"展开全文" | 简介展开，按钮变为"收起" |
| PLAYER-073 | 相关推荐 | 有 TMDB 推荐数据 | 查看详情区域下方 | 显示相关推荐 VideoCard 列表 |
| PLAYER-074 | 海报显示 | 有 poster_path | 查看详情区域 | 显示海报图片 |

### 4.9 进度记录

| 编号 | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| PLAYER-080 | 播放进度记录 | 视频正在播放 | 播放一段时间 | 自动记录播放进度到 IndexedDB |
| PLAYER-081 | 进度记录包含集数信息 | 剧集播放中 | 查看历史记录 | 记录包含 episodeLabel（如"第3集"） |
| PLAYER-082 | 进度记录包含 CMS 源信息 | 使用 CMS 源播放 | 查看历史记录 | 记录包含 cmsSourceId 和 cmsSourceName |

### 4.10 面板折叠

| 编号 | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| PLAYER-090 | CMS 面板默认展开 | 播放页已加载 | 查看侧边栏 | CMS、季、选集三个面板默认展开 |
| PLAYER-091 | 折叠面板 | 面板展开中 | 点击面板标题 | 面板内容折叠 |
| PLAYER-092 | 展开面板 | 面板折叠中 | 点击面板标题 | 面板内容展开 |

### 4.11 缓冲优化与格式降级（2026-09-02）

| 编号 | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| PLAYER-01E | 点播裸流降级（FLV/TS） | 点播链接为 `.flv`/`.ts` 裸流 | 进入播放页 | `detectSourceType` 识别为 `flv` → MPEGTSAdapter 播放；若 Native 失败触发 BARE_STREAM 错误，UniversalPlayer 降级 `setDegradedType('flv')` 重试（点播也生效，不再限 `mode==='iptv'`） |
| PLAYER-01F | MPEGTS 点播 seek | 点播 FLV/TS 已加载 | 拖动进度条 | `isLive=false` 分支启用 stash 缓冲 + `seek()` 支持，可跳转 |
| PLAYER-01G | MPEGTS 断流重连 | IPTV 裸流源断开 | 观察播放器 | `ERROR` 监听触发，2s 后重连（最多 5 次），重连期间 toast「源流断开，正在重连…」，LOAD 成功重置计数 |
| PLAYER-01H | HLS 90MB 点播缓冲 | 点播 HLS（约 2Mbps） | 播放并暂停 | 缓冲增长到约 6 分钟后停止下载（`maxBufferSize=90MB`）；暂停时 hls.js 默认继续缓冲到目标；无频繁 `waiting` 卡顿 |
| PLAYER-01I | HLS 直播低延迟 | IPTV HLS 直播 | 播放并对比源站时间 | 延迟 5-10s（`liveSyncDurationCount=3` / `liveMaxLatencyDurationCount=6` / `liveDurationInfinity=true`）；网络抖动恢复后自动追帧到 live edge |
| PLAYER-01J | HLS 直播缓冲收敛 | IPTV HLS 直播 | 观察缓冲 | `LEVEL_LOADED` 直播分支经 `this.hls.config` 写入 `maxBufferLength=60s`/`maxMaxBufferLength=120s`/`backBufferLength=20s`（修正直接赋值 getter 不生效） |
| PLAYER-01K | Dash 缓冲提升 | DASH 源 | 播放 | `bufferTimeDefault=30s` / `bufferTimeAtTopQuality=45s` / `bufferTimeAtTopQualityLongForm=60s`，高码率更充足 |

> **单元保障**：`vodParser.test.ts` 覆盖 `detectSourceType` 的 `m3u8`/`mpd`/`mp4`/`flv`/`ts`/`m2ts` 分支；HLS 90MB config 由 `npm run build` 类型校验 + 手动验证兜底。

---

